openapi: 3.1.0
info:
  description:
    "Public Vortex Payments API for customers, subscriptions, invoices,\
    \ payment requests, checkout, receipts, webhooks, tax."
  title: Vortex Payments API
  version: 2026-08
servers:
  - description: Production
    url: https://api.vortex.nyc
  - description: Local development
    url: http://localhost:3210
tags:
  - description: Billing lifecycle routes exposed through Vortex Payments.
    name: billing
  - description: "Products, prices, and billable catalog state."
    name: catalog
  - description: Hosted checkout session flows.
    name: checkout
  - description: "Credit note, refund, and refund reconciliation flows."
    name: credit-notes
  - description: "Customer balance grants, reversals, and expiration."
    name: customer-balance
  - description: Customer profiles and payment-facing customer state.
    name: customers
  - description: Coupons and applied discount lifecycle.
    name: discounts
  - description: Product access and entitlement state.
    name: entitlements
  - description: Hosted payment and customer-facing flows.
    name: hosted
  - description: "Invoice preview, creation, finalization, collection, and lifecycle."
    name: invoices
  - description: Customer payment method setup and management.
    name: payment-methods
  - description: Payment requests and payment links.
    name: payment-requests
  - description: Payment collection and reconciliation.
    name: payments
  - description: Merchant and account payment readiness.
    name: payments-readiness
  - description: Inbound payment processor webhook boundary.
    name: provider-webhook
  - description: "Quotes, quote versions, order forms, and orders."
    name: quotes
  - description: Receipt lookup and receipt lifecycle.
    name: receipts
  - description: Subscription creation and change lifecycle.
    name: subscriptions
  - description: "Tax quote, finalization, and evidence flows."
    name: tax
  - description: "Outbound webhook endpoints, events, deliveries, and replay."
    name: webhooks
paths:
  /health:
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_health_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security: []
      summary: GET /health
      tags:
        - health
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/health" \
              -X GET \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getHealthExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getHealthExact({
              client: vortex,
            });
  /customer-portal/{tail}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_customer_portal_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: DELETE /customer-portal/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/customer-portal/" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteCustomerPortalPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteCustomerPortalPrefix({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_customer_portal_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: GET /customer-portal/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/customer-portal/" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCustomerPortalPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCustomerPortalPrefix({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_customer_portal_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: PATCH /customer-portal/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/customer-portal/" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchCustomerPortalPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchCustomerPortalPrefix({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_customer_portal_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: POST /customer-portal/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/customer-portal/" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postCustomerPortalPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postCustomerPortalPrefix({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_customer_portal_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: PUT /customer-portal/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/customer-portal/" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putCustomerPortalPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putCustomerPortalPrefix({
              client: vortex,
            });
  /invoice/{tail}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_invoice_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: DELETE /invoice/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/invoice/" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteInvoicePrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteInvoicePrefix({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_invoice_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: GET /invoice/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/invoice/" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getInvoicePrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getInvoicePrefix({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_invoice_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: PATCH /invoice/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/invoice/" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchInvoicePrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchInvoicePrefix({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_invoice_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: POST /invoice/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/invoice/" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postInvoicePrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postInvoicePrefix({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_invoice_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - checkout-token: []
      summary: PUT /invoice/*
      tags:
        - hosted
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/invoice/" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putInvoicePrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putInvoicePrefix({
              client: vortex,
            });
  /webhooks/{provider}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_webhooks_provider_exact
      parameters:
        - explode: false
          in: path
          name: provider
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - provider-signature: []
      summary: DELETE /webhooks/:provider
      tags:
        - provider-webhook
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/webhooks/:provider" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteWebhooksProviderExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteWebhooksProviderExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_webhooks_provider_exact
      parameters:
        - explode: false
          in: path
          name: provider
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - provider-signature: []
      summary: GET /webhooks/:provider
      tags:
        - provider-webhook
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/webhooks/:provider" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getWebhooksProviderExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getWebhooksProviderExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_webhooks_provider_exact
      parameters:
        - explode: false
          in: path
          name: provider
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - provider-signature: []
      summary: PATCH /webhooks/:provider
      tags:
        - provider-webhook
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/webhooks/:provider" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchWebhooksProviderExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchWebhooksProviderExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_webhooks_provider_exact
      parameters:
        - explode: false
          in: path
          name: provider
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - provider-signature: []
      summary: POST /webhooks/:provider
      tags:
        - provider-webhook
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/webhooks/:provider" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postWebhooksProviderExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postWebhooksProviderExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_webhooks_provider_exact
      parameters:
        - explode: false
          in: path
          name: provider
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - provider-signature: []
      summary: PUT /webhooks/:provider
      tags:
        - provider-webhook
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/webhooks/:provider" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putWebhooksProviderExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putWebhooksProviderExact({
              client: vortex,
            });
  /internal/hosted-pay/{tail}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_internal_hosted_pay_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /internal/hosted-pay/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/internal/hosted-pay/" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteInternalHostedPayPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteInternalHostedPayPrefix({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_internal_hosted_pay_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /internal/hosted-pay/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/internal/hosted-pay/" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getInternalHostedPayPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getInternalHostedPayPrefix({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_internal_hosted_pay_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /internal/hosted-pay/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/internal/hosted-pay/" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchInternalHostedPayPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchInternalHostedPayPrefix({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_internal_hosted_pay_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /internal/hosted-pay/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/internal/hosted-pay/" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postInternalHostedPayPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postInternalHostedPayPrefix({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_internal_hosted_pay_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /internal/hosted-pay/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/internal/hosted-pay/" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putInternalHostedPayPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putInternalHostedPayPrefix({
              client: vortex,
            });
  /merchant-onboarding/{tail}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_merchant_onboarding_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /merchant-onboarding/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/merchant-onboarding/" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteMerchantOnboardingPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteMerchantOnboardingPrefix({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_merchant_onboarding_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /merchant-onboarding/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/merchant-onboarding/" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getMerchantOnboardingPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getMerchantOnboardingPrefix({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_merchant_onboarding_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /merchant-onboarding/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/merchant-onboarding/" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchMerchantOnboardingPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchMerchantOnboardingPrefix({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_merchant_onboarding_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /merchant-onboarding/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/merchant-onboarding/" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postMerchantOnboardingPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postMerchantOnboardingPrefix({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_merchant_onboarding_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /merchant-onboarding/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/merchant-onboarding/" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putMerchantOnboardingPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putMerchantOnboardingPrefix({
              client: vortex,
            });
  /order-form/{tail}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_order_form_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /order-form/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/order-form/" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteOrderFormPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteOrderFormPrefix({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_order_form_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /order-form/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/order-form/" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getOrderFormPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getOrderFormPrefix({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_order_form_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /order-form/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/order-form/" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchOrderFormPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchOrderFormPrefix({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_order_form_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /order-form/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/order-form/" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postOrderFormPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postOrderFormPrefix({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_order_form_prefix
      parameters:
        - description:
            Remainder of the prefix-routed path. See route docs/contract
            for exact sub-operation.
          explode: false
          in: path
          name: tail
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /order-form/*
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/order-form/" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putOrderFormPrefix, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putOrderFormPrefix({
              client: vortex,
            });
  /v1/billing-accounts/{billingAccountId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_billing_accounts_billingAccountId_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/billing-accounts/:billingAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1BillingAccountsBillingAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1BillingAccountsBillingAccountIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_billing_accounts_billingAccountId_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/billing-accounts/:billingAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1BillingAccountsBillingAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1BillingAccountsBillingAccountIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_billing_accounts_billingAccountId_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/billing-accounts/:billingAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1BillingAccountsBillingAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1BillingAccountsBillingAccountIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_billing_accounts_billingAccountId_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/billing-accounts/:billingAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1BillingAccountsBillingAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1BillingAccountsBillingAccountIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_billing_accounts_billingAccountId_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/billing-accounts/:billingAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1BillingAccountsBillingAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1BillingAccountsBillingAccountIdExact({
              client: vortex,
            });
  /v1/billing-accounts/{billingAccountId}/dunning-campaign:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_billing_accounts_billingAccountId_dunning_campaign_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/billing-accounts/:billingAccountId/dunning-campaign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId/dunning-campaign" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1BillingAccountsBillingAccountIdDunningCampaignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1BillingAccountsBillingAccountIdDunningCampaignExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_billing_accounts_billingAccountId_dunning_campaign_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/billing-accounts/:billingAccountId/dunning-campaign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId/dunning-campaign" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1BillingAccountsBillingAccountIdDunningCampaignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1BillingAccountsBillingAccountIdDunningCampaignExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_billing_accounts_billingAccountId_dunning_campaign_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/billing-accounts/:billingAccountId/dunning-campaign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId/dunning-campaign" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1BillingAccountsBillingAccountIdDunningCampaignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1BillingAccountsBillingAccountIdDunningCampaignExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_billing_accounts_billingAccountId_dunning_campaign_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/billing-accounts/:billingAccountId/dunning-campaign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId/dunning-campaign" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1BillingAccountsBillingAccountIdDunningCampaignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1BillingAccountsBillingAccountIdDunningCampaignExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_billing_accounts_billingAccountId_dunning_campaign_exact
      parameters:
        - explode: false
          in: path
          name: billingAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/billing-accounts/:billingAccountId/dunning-campaign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing-accounts/:billingAccountId/dunning-campaign" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1BillingAccountsBillingAccountIdDunningCampaignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1BillingAccountsBillingAccountIdDunningCampaignExact({
              client: vortex,
            });
  /v1/billing/collect-invoice-payment:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_billing_collect_invoice_payment_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/billing/collect-invoice-payment
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing/collect-invoice-payment" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1BillingCollectInvoicePaymentExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1BillingCollectInvoicePaymentExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_billing_collect_invoice_payment_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/billing/collect-invoice-payment
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing/collect-invoice-payment" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1BillingCollectInvoicePaymentExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1BillingCollectInvoicePaymentExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_billing_collect_invoice_payment_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/billing/collect-invoice-payment
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing/collect-invoice-payment" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1BillingCollectInvoicePaymentExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1BillingCollectInvoicePaymentExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_billing_collect_invoice_payment_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/billing/collect-invoice-payment
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing/collect-invoice-payment" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1BillingCollectInvoicePaymentExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1BillingCollectInvoicePaymentExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_billing_collect_invoice_payment_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/billing/collect-invoice-payment
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/billing/collect-invoice-payment" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1BillingCollectInvoicePaymentExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1BillingCollectInvoicePaymentExact({
              client: vortex,
            });
  /v1/card-present-payment-intents:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_card_present_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/card-present-payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CardPresentPaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CardPresentPaymentIntentsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_card_present_payment_intents_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/card-present-payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CardPresentPaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CardPresentPaymentIntentsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_card_present_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/card-present-payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CardPresentPaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CardPresentPaymentIntentsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_card_present_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/card-present-payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CardPresentPaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CardPresentPaymentIntentsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_card_present_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/card-present-payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CardPresentPaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CardPresentPaymentIntentsExact({
              client: vortex,
            });
  /v1/card-present-payment-intents/{cardPresentPaymentIntentId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_card_present_payment_intents_cardPresentPaymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/card-present-payment-intents/:cardPresentPaymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_card_present_payment_intents_cardPresentPaymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/card-present-payment-intents/:cardPresentPaymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_card_present_payment_intents_cardPresentPaymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/card-present-payment-intents/:cardPresentPaymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_card_present_payment_intents_cardPresentPaymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/card-present-payment-intents/:cardPresentPaymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_card_present_payment_intents_cardPresentPaymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/card-present-payment-intents/:cardPresentPaymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CardPresentPaymentIntentsCardPresentPaymentIntentIdExact({
              client: vortex,
            });
  /v1/card-present-payment-intents/{cardPresentPaymentIntentId}/collection-outcome:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_card_present_payment_intents_cardPresentPaymentIntentId_collection_outcome_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_card_present_payment_intents_cardPresentPaymentIntentId_collection_outcome_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_card_present_payment_intents_cardPresentPaymentIntentId_collection_outcome_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_card_present_payment_intents_cardPresentPaymentIntentId_collection_outcome_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_card_present_payment_intents_cardPresentPaymentIntentId_collection_outcome_exact
      parameters:
        - explode: false
          in: path
          name: cardPresentPaymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/card-present-payment-intents/:cardPresentPaymentIntentId/collection-outcome" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CardPresentPaymentIntentsCardPresentPaymentIntentIdCollectionOutcomeExact({
              client: vortex,
            });
  /v1/catalog:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_catalog_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/catalog
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/catalog" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CatalogExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CatalogExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_catalog_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/catalog
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/catalog" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CatalogExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CatalogExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_catalog_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/catalog
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/catalog" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CatalogExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CatalogExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_catalog_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/catalog
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/catalog" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CatalogExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CatalogExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_catalog_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/catalog
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/catalog" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CatalogExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CatalogExact({
              client: vortex,
            });
  /v1/checkout/sessions:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_checkout_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/checkout/sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CheckoutSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CheckoutSessionsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_checkout_sessions_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/checkout/sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CheckoutSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CheckoutSessionsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_checkout_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/checkout/sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CheckoutSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CheckoutSessionsExact({
              client: vortex,
            });
    post:
      operationId: createCheckoutSession
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  mode: subscription
                  customerExternalId: cus_123
                  subscriptionExternalId: sub_123
                  collectionMode: automatic
                  lineItems:
                    - priceId: price_core_monthly
                      quantity: 1
                  createdByRef: internal-integration
            schema:
              $ref: "#/components/schemas/CreateCheckoutSessionRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateCheckoutSessionResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a hosted checkout session
      tags:
        - billing
        - checkout
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"mode":"subscription","customerExternalId":"cus_123","subscriptionExternalId":"sub_123","collectionMode":"automatic","lineItems":[{"priceId":"price_core_monthly","quantity":1}],"createdByRef":"internal-integration"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createCheckoutSession, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createCheckoutSession({
              client: vortex,
              body: {"mode":"subscription","customerExternalId":"cus_123","subscriptionExternalId":"sub_123","collectionMode":"automatic","lineItems":[{"priceId":"price_core_monthly","quantity":1}],"createdByRef":"internal-integration"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_checkout_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/checkout/sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CheckoutSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CheckoutSessionsExact({
              client: vortex,
            });
  /v1/checkout/sessions/{checkoutSessionId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_checkout_sessions_checkoutSessionId_exact
      parameters:
        - explode: false
          in: path
          name: checkoutSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/checkout/sessions/:checkoutSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions/:checkoutSessionId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CheckoutSessionsCheckoutSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CheckoutSessionsCheckoutSessionIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_checkout_sessions_checkoutSessionId_exact
      parameters:
        - explode: false
          in: path
          name: checkoutSessionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/checkout/sessions/:checkoutSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions/:checkoutSessionId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CheckoutSessionsCheckoutSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CheckoutSessionsCheckoutSessionIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_checkout_sessions_checkoutSessionId_exact
      parameters:
        - explode: false
          in: path
          name: checkoutSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/checkout/sessions/:checkoutSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions/:checkoutSessionId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CheckoutSessionsCheckoutSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CheckoutSessionsCheckoutSessionIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_checkout_sessions_checkoutSessionId_exact
      parameters:
        - explode: false
          in: path
          name: checkoutSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/checkout/sessions/:checkoutSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions/:checkoutSessionId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CheckoutSessionsCheckoutSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CheckoutSessionsCheckoutSessionIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_checkout_sessions_checkoutSessionId_exact
      parameters:
        - explode: false
          in: path
          name: checkoutSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/checkout/sessions/:checkoutSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/checkout/sessions/:checkoutSessionId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CheckoutSessionsCheckoutSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CheckoutSessionsCheckoutSessionIdExact({
              client: vortex,
            });
  /v1/collection-retries/run:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_collection_retries_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/collection-retries/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/collection-retries/run" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CollectionRetriesRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CollectionRetriesRunExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_collection_retries_run_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/collection-retries/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/collection-retries/run" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CollectionRetriesRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CollectionRetriesRunExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_collection_retries_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/collection-retries/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/collection-retries/run" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CollectionRetriesRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CollectionRetriesRunExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_collection_retries_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/collection-retries/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/collection-retries/run" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CollectionRetriesRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CollectionRetriesRunExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_collection_retries_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/collection-retries/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/collection-retries/run" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CollectionRetriesRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CollectionRetriesRunExact({
              client: vortex,
            });
  /v1/coupons:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_coupons_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/coupons
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CouponsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CouponsExact({
              client: vortex,
            });
    get:
      operationId: listCoupons
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CouponListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List billing coupons
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listCoupons, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listCoupons({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_coupons_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/coupons
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CouponsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CouponsExact({
              client: vortex,
            });
    post:
      operationId: createCoupon
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  couponId: coupon_123
                  code: SAVE25
                  name: Save 25%
                  discountType: percentage
                  percentage: 25
                  duration: forever
                  targets:
                    priceIds:
                      - price_123
            schema:
              $ref: "#/components/schemas/CreateCouponRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CouponResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a billing coupon
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"couponId":"coupon_123","code":"SAVE25","name":"Save 25%","discountType":"percentage","percentage":25,"duration":"forever","targets":{"priceIds":["price_123"]}}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createCoupon, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createCoupon({
              client: vortex,
              body: {"couponId":"coupon_123","code":"SAVE25","name":"Save 25%","discountType":"percentage","percentage":25,"duration":"forever","targets":{"priceIds":["price_123"]}},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_coupons_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/coupons
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CouponsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CouponsExact({
              client: vortex,
            });
  /v1/coupons/{couponId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_coupons_couponId_exact
      parameters:
        - explode: false
          in: path
          name: couponId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/coupons/:couponId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons/:couponId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CouponsCouponIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CouponsCouponIdExact({
              client: vortex,
            });
    get:
      operationId: getCoupon
      parameters:
        - explode: false
          in: path
          name: couponId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CouponResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get one billing coupon
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons/{couponId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCoupon, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCoupon({
              client: vortex,
              path: { couponId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_coupons_couponId_exact
      parameters:
        - explode: false
          in: path
          name: couponId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/coupons/:couponId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons/:couponId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CouponsCouponIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CouponsCouponIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_coupons_couponId_exact
      parameters:
        - explode: false
          in: path
          name: couponId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/coupons/:couponId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons/:couponId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CouponsCouponIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CouponsCouponIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_coupons_couponId_exact
      parameters:
        - explode: false
          in: path
          name: couponId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/coupons/:couponId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons/:couponId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CouponsCouponIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CouponsCouponIdExact({
              client: vortex,
            });
  /v1/credit-notes:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_credit_notes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/credit-notes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CreditNotesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CreditNotesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_credit_notes_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/credit-notes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CreditNotesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CreditNotesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_credit_notes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/credit-notes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CreditNotesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CreditNotesExact({
              client: vortex,
            });
    post:
      operationId: createCreditNote
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  creditNoteId: cnote_123
                  invoiceId: inv_123
                  reason: product_unsatisfactory
                  refundAmount: 2500
                  lines:
                    - invoiceLineId: line_123
                      description: Refund unused service
                      amount: 2500
                      bucket: refund
            schema:
              $ref: "#/components/schemas/CreditNoteCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNoteResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a credit note with explicit line attribution
      tags:
        - billing
        - credit-notes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"creditNoteId":"cnote_123","invoiceId":"inv_123","reason":"product_unsatisfactory","refundAmount":2500,"lines":[{"invoiceLineId":"line_123","description":"Refund unused service","amount":2500,"bucket":"refund"}]}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createCreditNote, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createCreditNote({
              client: vortex,
              body: {"creditNoteId":"cnote_123","invoiceId":"inv_123","reason":"product_unsatisfactory","refundAmount":2500,"lines":[{"invoiceLineId":"line_123","description":"Refund unused service","amount":2500,"bucket":"refund"}]},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_credit_notes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/credit-notes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CreditNotesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CreditNotesExact({
              client: vortex,
            });
  /v1/credit-notes/{creditNoteId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_credit_notes_creditNoteId_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/credit-notes/:creditNoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CreditNotesCreditNoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CreditNotesCreditNoteIdExact({
              client: vortex,
            });
    get:
      operationId: getCreditNote
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNoteResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect a credit note and its tax reversal evidence
      tags:
        - billing
        - credit-notes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/{creditNoteId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCreditNote, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCreditNote({
              client: vortex,
              path: { creditNoteId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_credit_notes_creditNoteId_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/credit-notes/:creditNoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CreditNotesCreditNoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CreditNotesCreditNoteIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_credit_notes_creditNoteId_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/credit-notes/:creditNoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CreditNotesCreditNoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CreditNotesCreditNoteIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_credit_notes_creditNoteId_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/credit-notes/:creditNoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CreditNotesCreditNoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CreditNotesCreditNoteIdExact({
              client: vortex,
            });
  /v1/credit-notes/{creditNoteId}/refund:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_credit_notes_creditNoteId_refund_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/credit-notes/:creditNoteId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId/refund" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CreditNotesCreditNoteIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CreditNotesCreditNoteIdRefundExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_credit_notes_creditNoteId_refund_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/credit-notes/:creditNoteId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId/refund" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CreditNotesCreditNoteIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CreditNotesCreditNoteIdRefundExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_credit_notes_creditNoteId_refund_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/credit-notes/:creditNoteId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId/refund" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CreditNotesCreditNoteIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CreditNotesCreditNoteIdRefundExact({
              client: vortex,
            });
    post:
      operationId: refundCreditNote
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  paymentId: pay_123
            schema:
              $ref: "#/components/schemas/CreditNoteRefundRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNoteResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Execute or record a credit-note refund
      tags:
        - billing
        - credit-notes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/{creditNoteId}/refund" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"paymentId":"pay_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { refundCreditNote, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await refundCreditNote({
              client: vortex,
              path: { creditNoteId: "..." },
              body: {"paymentId":"pay_123"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_credit_notes_creditNoteId_refund_exact
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/credit-notes/:creditNoteId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/:creditNoteId/refund" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CreditNotesCreditNoteIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CreditNotesCreditNoteIdRefundExact({
              client: vortex,
            });
  /v1/custom-fields:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_custom_fields_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomFieldsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_custom_fields_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomFieldsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_custom_fields_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomFieldsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_custom_fields_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomFieldsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_custom_fields_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomFieldsExact({
              client: vortex,
            });
  /v1/custom-fields/{customFieldId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_custom_fields_customFieldId_exact
      parameters:
        - explode: false
          in: path
          name: customFieldId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/custom-fields/:customFieldId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields/:customFieldId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomFieldsCustomFieldIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomFieldsCustomFieldIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_custom_fields_customFieldId_exact
      parameters:
        - explode: false
          in: path
          name: customFieldId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/custom-fields/:customFieldId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields/:customFieldId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomFieldsCustomFieldIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomFieldsCustomFieldIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_custom_fields_customFieldId_exact
      parameters:
        - explode: false
          in: path
          name: customFieldId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/custom-fields/:customFieldId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields/:customFieldId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomFieldsCustomFieldIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomFieldsCustomFieldIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_custom_fields_customFieldId_exact
      parameters:
        - explode: false
          in: path
          name: customFieldId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/custom-fields/:customFieldId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields/:customFieldId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomFieldsCustomFieldIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomFieldsCustomFieldIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_custom_fields_customFieldId_exact
      parameters:
        - explode: false
          in: path
          name: customFieldId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/custom-fields/:customFieldId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/custom-fields/:customFieldId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomFieldsCustomFieldIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomFieldsCustomFieldIdExact({
              client: vortex,
            });
  /v1/customers:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersExact({
              client: vortex,
            });
  /v1/customers/{customerId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersCustomerIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdExact({
              client: vortex,
            });
    put:
      operationId: upsertCustomer
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  email: billing@example.com
                  name: Acme Inc.
                  billingCurrency: USD
            schema:
              $ref: "#/components/schemas/CustomerUpsertRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create or update a billing customer
      tags:
        - billing
        - customers
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"email":"billing@example.com","name":"Acme Inc.","billingCurrency":"USD"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { upsertCustomer, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await upsertCustomer({
              client: vortex,
              path: { customerId: "..." },
              body: {"email":"billing@example.com","name":"Acme Inc.","billingCurrency":"USD"},
            });
  /v1/customers/{customerId}/balance:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_balance_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/balance" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdBalanceExact({
              client: vortex,
            });
    get:
      operationId: getCustomerBalance
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerBalanceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Return a customer's prepaid balance ledger
      tags:
        - billing
        - customer-balance
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/balance" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCustomerBalance, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCustomerBalance({
              client: vortex,
              path: { customerId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_balance_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/balance" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdBalanceExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_balance_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/balance" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdBalanceExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_balance_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/balance" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdBalanceExact({
              client: vortex,
            });
  /v1/customers/{customerId}/billing-accounts:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_billing_accounts_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/billing-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/billing-accounts" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdBillingAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdBillingAccountsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_customerId_billing_accounts_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers/:customerId/billing-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/billing-accounts" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersCustomerIdBillingAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersCustomerIdBillingAccountsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_billing_accounts_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/billing-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/billing-accounts" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdBillingAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdBillingAccountsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_billing_accounts_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/billing-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/billing-accounts" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdBillingAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdBillingAccountsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_billing_accounts_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/billing-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/billing-accounts" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdBillingAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdBillingAccountsExact({
              client: vortex,
            });
  /v1/customers/{customerId}/entitlements:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_entitlements_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/entitlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/entitlements" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdEntitlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdEntitlementsExact({
              client: vortex,
            });
    get:
      operationId: getCustomerEntitlements
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerEntitlementsResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get effective customer entitlements
      tags:
        - billing
        - entitlements
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/entitlements" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCustomerEntitlements, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCustomerEntitlements({
              client: vortex,
              path: { customerId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_entitlements_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/entitlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/entitlements" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdEntitlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdEntitlementsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_entitlements_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/entitlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/entitlements" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdEntitlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdEntitlementsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_entitlements_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/entitlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/entitlements" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdEntitlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdEntitlementsExact({
              client: vortex,
            });
  /v1/customers/{customerId}/invoices:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/invoices" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdInvoicesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_customerId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers/:customerId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/invoices" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersCustomerIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersCustomerIdInvoicesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/invoices" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdInvoicesExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/invoices" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdInvoicesExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/invoices" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdInvoicesExact({
              client: vortex,
            });
  /v1/customers/{customerId}/portal-links:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_portal_links_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/portal-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdPortalLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdPortalLinksExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_customerId_portal_links_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers/:customerId/portal-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersCustomerIdPortalLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersCustomerIdPortalLinksExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_portal_links_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/portal-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdPortalLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdPortalLinksExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_portal_links_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/portal-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdPortalLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdPortalLinksExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_portal_links_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/portal-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdPortalLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdPortalLinksExact({
              client: vortex,
            });
  /v1/customers/{customerId}/portal-links/{portalLinkId}/revoke:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_portal_links_portalLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: portalLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/portal-links/:portalLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links/:portalLinkId/revoke" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_customerId_portal_links_portalLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: portalLinkId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers/:customerId/portal-links/:portalLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links/:portalLinkId/revoke" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_portal_links_portalLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: portalLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/portal-links/:portalLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links/:portalLinkId/revoke" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_portal_links_portalLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: portalLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/portal-links/:portalLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links/:portalLinkId/revoke" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_portal_links_portalLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: portalLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/portal-links/:portalLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/portal-links/:portalLinkId/revoke" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdPortalLinksPortalLinkIdRevokeExact({
              client: vortex,
            });
  /v1/customers/{customerId}/subscriptions:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_subscriptions_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/subscriptions" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdSubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdSubscriptionsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_customerId_subscriptions_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers/:customerId/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/subscriptions" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersCustomerIdSubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersCustomerIdSubscriptionsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_subscriptions_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/subscriptions" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdSubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdSubscriptionsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_subscriptions_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/subscriptions" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdSubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdSubscriptionsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_subscriptions_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/subscriptions" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdSubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdSubscriptionsExact({
              client: vortex,
            });
  /v1/customers/{customerId}/wallets:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_customers_customerId_wallets_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/customers/:customerId/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/wallets" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1CustomersCustomerIdWalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1CustomersCustomerIdWalletsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_customers_customerId_wallets_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/customers/:customerId/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/wallets" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1CustomersCustomerIdWalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1CustomersCustomerIdWalletsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_customers_customerId_wallets_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/customers/:customerId/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/wallets" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1CustomersCustomerIdWalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1CustomersCustomerIdWalletsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_customers_customerId_wallets_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/customers/:customerId/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/wallets" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1CustomersCustomerIdWalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1CustomersCustomerIdWalletsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_customers_customerId_wallets_exact
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/customers/:customerId/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/:customerId/wallets" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1CustomersCustomerIdWalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1CustomersCustomerIdWalletsExact({
              client: vortex,
            });
  /v1/disputes:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_disputes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/disputes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1DisputesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1DisputesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_disputes_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/disputes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1DisputesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1DisputesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_disputes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/disputes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1DisputesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1DisputesExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_disputes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/disputes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1DisputesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1DisputesExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_disputes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/disputes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1DisputesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1DisputesExact({
              client: vortex,
            });
  /v1/disputes/{disputeId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_disputes_disputeId_exact
      parameters:
        - explode: false
          in: path
          name: disputeId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/disputes/:disputeId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes/:disputeId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1DisputesDisputeIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1DisputesDisputeIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_disputes_disputeId_exact
      parameters:
        - explode: false
          in: path
          name: disputeId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/disputes/:disputeId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes/:disputeId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1DisputesDisputeIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1DisputesDisputeIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_disputes_disputeId_exact
      parameters:
        - explode: false
          in: path
          name: disputeId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/disputes/:disputeId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes/:disputeId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1DisputesDisputeIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1DisputesDisputeIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_disputes_disputeId_exact
      parameters:
        - explode: false
          in: path
          name: disputeId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/disputes/:disputeId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes/:disputeId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1DisputesDisputeIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1DisputesDisputeIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_disputes_disputeId_exact
      parameters:
        - explode: false
          in: path
          name: disputeId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/disputes/:disputeId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/disputes/:disputeId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1DisputesDisputeIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1DisputesDisputeIdExact({
              client: vortex,
            });
  /v1/dunning-campaigns:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_dunning_campaigns_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/dunning-campaigns
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1DunningCampaignsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1DunningCampaignsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_dunning_campaigns_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/dunning-campaigns
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1DunningCampaignsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1DunningCampaignsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_dunning_campaigns_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/dunning-campaigns
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1DunningCampaignsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1DunningCampaignsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_dunning_campaigns_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/dunning-campaigns
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1DunningCampaignsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1DunningCampaignsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_dunning_campaigns_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/dunning-campaigns
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1DunningCampaignsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1DunningCampaignsExact({
              client: vortex,
            });
  /v1/dunning-campaigns/{dunningCampaignId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_dunning_campaigns_dunningCampaignId_exact
      parameters:
        - explode: false
          in: path
          name: dunningCampaignId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/dunning-campaigns/:dunningCampaignId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns/:dunningCampaignId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1DunningCampaignsDunningCampaignIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1DunningCampaignsDunningCampaignIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_dunning_campaigns_dunningCampaignId_exact
      parameters:
        - explode: false
          in: path
          name: dunningCampaignId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/dunning-campaigns/:dunningCampaignId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns/:dunningCampaignId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1DunningCampaignsDunningCampaignIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1DunningCampaignsDunningCampaignIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_dunning_campaigns_dunningCampaignId_exact
      parameters:
        - explode: false
          in: path
          name: dunningCampaignId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/dunning-campaigns/:dunningCampaignId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns/:dunningCampaignId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1DunningCampaignsDunningCampaignIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1DunningCampaignsDunningCampaignIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_dunning_campaigns_dunningCampaignId_exact
      parameters:
        - explode: false
          in: path
          name: dunningCampaignId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/dunning-campaigns/:dunningCampaignId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns/:dunningCampaignId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1DunningCampaignsDunningCampaignIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1DunningCampaignsDunningCampaignIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_dunning_campaigns_dunningCampaignId_exact
      parameters:
        - explode: false
          in: path
          name: dunningCampaignId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/dunning-campaigns/:dunningCampaignId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning-campaigns/:dunningCampaignId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1DunningCampaignsDunningCampaignIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1DunningCampaignsDunningCampaignIdExact({
              client: vortex,
            });
  /v1/dunning/run:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_dunning_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/dunning/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning/run" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1DunningRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1DunningRunExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_dunning_run_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/dunning/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning/run" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1DunningRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1DunningRunExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_dunning_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/dunning/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning/run" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1DunningRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1DunningRunExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_dunning_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/dunning/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning/run" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1DunningRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1DunningRunExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_dunning_run_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/dunning/run
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/dunning/run" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1DunningRunExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1DunningRunExact({
              client: vortex,
            });
  /v1/email/transactional:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_email_transactional_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/email/transactional
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/email/transactional" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1EmailTransactionalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1EmailTransactionalExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_email_transactional_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/email/transactional
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/email/transactional" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1EmailTransactionalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1EmailTransactionalExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_email_transactional_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/email/transactional
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/email/transactional" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1EmailTransactionalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1EmailTransactionalExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_email_transactional_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/email/transactional
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/email/transactional" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1EmailTransactionalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1EmailTransactionalExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_email_transactional_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/email/transactional
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/email/transactional" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1EmailTransactionalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1EmailTransactionalExact({
              client: vortex,
            });
  /v1/entitlements/grant:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_entitlements_grant_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/entitlements/grant
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/grant" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1EntitlementsGrantExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1EntitlementsGrantExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_entitlements_grant_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/entitlements/grant
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/grant" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1EntitlementsGrantExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1EntitlementsGrantExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_entitlements_grant_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/entitlements/grant
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/grant" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1EntitlementsGrantExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1EntitlementsGrantExact({
              client: vortex,
            });
    post:
      operationId: grantEntitlement
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  customerExternalId: cus_123
                  entitlementKey: pro
                  source: operator
                  sourceRef: launch-proof
            schema:
              $ref: "#/components/schemas/GrantEntitlementRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntitlementResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Grant a customer entitlement
      tags:
        - billing
        - entitlements
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/grant" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"customerExternalId":"cus_123","entitlementKey":"pro","source":"operator","sourceRef":"launch-proof"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { grantEntitlement, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await grantEntitlement({
              client: vortex,
              body: {"customerExternalId":"cus_123","entitlementKey":"pro","source":"operator","sourceRef":"launch-proof"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_entitlements_grant_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/entitlements/grant
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/grant" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1EntitlementsGrantExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1EntitlementsGrantExact({
              client: vortex,
            });
  /v1/entitlements/revoke:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_entitlements_revoke_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/entitlements/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/revoke" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1EntitlementsRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1EntitlementsRevokeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_entitlements_revoke_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/entitlements/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/revoke" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1EntitlementsRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1EntitlementsRevokeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_entitlements_revoke_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/entitlements/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/revoke" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1EntitlementsRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1EntitlementsRevokeExact({
              client: vortex,
            });
    post:
      operationId: revokeEntitlement
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  customerExternalId: cus_123
                  entitlementKey: pro
                  source: operator
                  sourceRef: launch-proof
                  reason: downgrade
            schema:
              $ref: "#/components/schemas/RevokeEntitlementRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EntitlementResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Revoke a customer entitlement
      tags:
        - billing
        - entitlements
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/revoke" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"customerExternalId":"cus_123","entitlementKey":"pro","source":"operator","sourceRef":"launch-proof","reason":"downgrade"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { revokeEntitlement, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await revokeEntitlement({
              client: vortex,
              body: {"customerExternalId":"cus_123","entitlementKey":"pro","source":"operator","sourceRef":"launch-proof","reason":"downgrade"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_entitlements_revoke_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/entitlements/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/entitlements/revoke" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1EntitlementsRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1EntitlementsRevokeExact({
              client: vortex,
            });
  /v1/invoices:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesExact({
              client: vortex,
            });
    post:
      operationId: createInvoice
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  customerExternalId: cus_123
                  subscriptionExternalId: sub_123
            schema:
              $ref: "#/components/schemas/InvoiceCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a draft invoice
      tags:
        - billing
        - invoices
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"customerExternalId":"cus_123","subscriptionExternalId":"sub_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createInvoice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createInvoice({
              client: vortex,
              body: {"customerExternalId":"cus_123","subscriptionExternalId":"sub_123"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdExact({
              client: vortex,
            });
    get:
      operationId: getInvoice
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get an invoice
      tags:
        - billing
        - invoices
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getInvoice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getInvoice({
              client: vortex,
              path: { invoiceId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/collect:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_collect_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collect" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdCollectExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_collect_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collect" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdCollectExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_collect_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collect" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdCollectExact({
              client: vortex,
            });
    post:
      operationId: collectInvoice
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  paymentMethodId: pm_123
            schema:
              $ref: "#/components/schemas/InvoiceCollectRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Collect an open invoice
      tags:
        - billing
        - payments
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/collect" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"paymentMethodId":"pm_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { collectInvoice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await collectInvoice({
              client: vortex,
              path: { invoiceId: "..." },
              body: {"paymentMethodId":"pm_123"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_collect_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collect" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdCollectExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/collection-retries:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_collection_retries_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/collection-retries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection-retries" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdCollectionRetriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdCollectionRetriesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_collection_retries_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/collection-retries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection-retries" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdCollectionRetriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdCollectionRetriesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_collection_retries_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/collection-retries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection-retries" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdCollectionRetriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdCollectionRetriesExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_collection_retries_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/collection-retries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection-retries" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdCollectionRetriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdCollectionRetriesExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_collection_retries_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/collection-retries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection-retries" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdCollectionRetriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdCollectionRetriesExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/collection:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_collection_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/collection
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdCollectionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdCollectionExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_collection_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/collection
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdCollectionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdCollectionExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_collection_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/collection
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdCollectionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdCollectionExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_collection_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/collection
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdCollectionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdCollectionExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_collection_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/collection
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/collection" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdCollectionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdCollectionExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/finalize:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/finalize" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdFinalizeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/finalize" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdFinalizeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/finalize" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdFinalizeExact({
              client: vortex,
            });
    post:
      operationId: finalizeInvoice
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  finalizedByRef: internal-integration
            schema:
              $ref: "#/components/schemas/InvoiceFinalizeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Finalize a draft invoice
      tags:
        - billing
        - invoices
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/finalize" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"finalizedByRef":"internal-integration"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { finalizeInvoice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await finalizeInvoice({
              client: vortex,
              path: { invoiceId: "..." },
              body: {"finalizedByRef":"internal-integration"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/finalize" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdFinalizeExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/hosted-link:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdHostedLinkExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdHostedLinkExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdHostedLinkExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdHostedLinkExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdHostedLinkExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/hosted-link/{hostedLinkId}/revoke:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/hosted-link/:hostedLinkId/revoke" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/mark-past-due:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_mark_past_due_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/mark-past-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-past-due" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdMarkPastDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdMarkPastDueExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_mark_past_due_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/mark-past-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-past-due" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdMarkPastDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdMarkPastDueExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_mark_past_due_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/mark-past-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-past-due" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdMarkPastDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdMarkPastDueExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_mark_past_due_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/mark-past-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-past-due" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdMarkPastDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdMarkPastDueExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_mark_past_due_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/mark-past-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-past-due" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdMarkPastDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdMarkPastDueExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/mark-uncollectible:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_mark_uncollectible_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/mark-uncollectible
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-uncollectible" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdMarkUncollectibleExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdMarkUncollectibleExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_mark_uncollectible_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/mark-uncollectible
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-uncollectible" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdMarkUncollectibleExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdMarkUncollectibleExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_mark_uncollectible_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/mark-uncollectible
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-uncollectible" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdMarkUncollectibleExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdMarkUncollectibleExact({
              client: vortex,
            });
    post:
      operationId: markInvoiceUncollectible
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  markedUncollectibleByRef: internal-integration
                  reason: collection_exhausted
            schema:
              $ref: "#/components/schemas/InvoiceMarkUncollectibleRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Mark an invoice uncollectible
      tags:
        - billing
        - invoices
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/mark-uncollectible" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"markedUncollectibleByRef":"internal-integration","reason":"collection_exhausted"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { markInvoiceUncollectible, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await markInvoiceUncollectible({
              client: vortex,
              path: { invoiceId: "..." },
              body: {"markedUncollectibleByRef":"internal-integration","reason":"collection_exhausted"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_mark_uncollectible_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/mark-uncollectible
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/mark-uncollectible" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdMarkUncollectibleExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdMarkUncollectibleExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/payment-receipts:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_payment_receipts_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/payment-receipts" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdPaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdPaymentReceiptsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_payment_receipts_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/payment-receipts" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdPaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdPaymentReceiptsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_payment_receipts_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/payment-receipts" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdPaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdPaymentReceiptsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_payment_receipts_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/payment-receipts" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdPaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdPaymentReceiptsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_payment_receipts_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/payment-receipts" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdPaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdPaymentReceiptsExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/refund:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_refund_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/refund" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdRefundExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_refund_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/refund" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdRefundExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_refund_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/refund" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdRefundExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_refund_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/refund" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdRefundExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_refund_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/refund
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/refund" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdRefundExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdRefundExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/tax/finalize:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_invoices_invoiceId_tax_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/invoices/:invoiceId/tax/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/tax/finalize" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1InvoicesInvoiceIdTaxFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1InvoicesInvoiceIdTaxFinalizeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_tax_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/tax/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/tax/finalize" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdTaxFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdTaxFinalizeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_tax_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/tax/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/tax/finalize" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdTaxFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdTaxFinalizeExact({
              client: vortex,
            });
    post:
      operationId: finalizeInvoiceTax
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  customerTaxAddress:
                    line1: 897 Broadway
                    city: New York
                    region: NY
                    postalCode: "10003"
                    country: US
                  taxBehaviorPolicy:
                    behavior: exclusive
                  finalizedByRef: internal-integration
            schema:
              $ref: "#/components/schemas/TaxFinalizeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenApiInvoiceTaxResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Finalize tax evidence for an invoice
      tags:
        - billing
        - tax
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/tax/finalize" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"customerTaxAddress":{"line1":"897 Broadway","city":"New York","region":"NY","postalCode":"10003","country":"US"},"taxBehaviorPolicy":{"behavior":"exclusive"},"finalizedByRef":"internal-integration"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { finalizeInvoiceTax, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await finalizeInvoiceTax({
              client: vortex,
              path: { invoiceId: "..." },
              body: {"customerTaxAddress":{"line1":"897 Broadway","city":"New York","region":"NY","postalCode":"10003","country":"US"},"taxBehaviorPolicy":{"behavior":"exclusive"},"finalizedByRef":"internal-integration"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_tax_finalize_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/tax/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/tax/finalize" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdTaxFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdTaxFinalizeExact({
              client: vortex,
            });
  /v1/invoices/{invoiceId}/void:
    delete:
      operationId: voidInvoice
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  voidedByRef: internal-integration
                  reason: customer_requested
            schema:
              $ref: "#/components/schemas/InvoiceVoidRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Void a draft or open invoice
      tags:
        - billing
        - invoices
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/void" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"voidedByRef":"internal-integration","reason":"customer_requested"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { voidInvoice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await voidInvoice({
              client: vortex,
              path: { invoiceId: "..." },
              body: {"voidedByRef":"internal-integration","reason":"customer_requested"},
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_invoices_invoiceId_void_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/invoices/:invoiceId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/void" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1InvoicesInvoiceIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1InvoicesInvoiceIdVoidExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_invoices_invoiceId_void_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/invoices/:invoiceId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/void" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1InvoicesInvoiceIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1InvoicesInvoiceIdVoidExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_invoices_invoiceId_void_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/invoices/:invoiceId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/void" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1InvoicesInvoiceIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1InvoicesInvoiceIdVoidExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_invoices_invoiceId_void_exact
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/invoices/:invoiceId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/:invoiceId/void" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1InvoicesInvoiceIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1InvoicesInvoiceIdVoidExact({
              client: vortex,
            });
  /v1/merchant-accounts:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsExact({
              client: vortex,
            });
    post:
      operationId: createMerchantAccount
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  environment: sandbox
                  externalMerchantRef: seal_org_1
                  displayName: Acme Inc.
                  legalEntityType: CORPORATION
                  country: USA
                  merchantMode: processing
                  defaultCurrency: USD
                  metadata:
                    sourceSystem: seal
                    sealOrganizationId: seal_org_1
            schema:
              $ref: "#/components/schemas/MerchantAccountCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantAccountResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a sub-merchant account
      tags:
        - billing
        - payments-readiness
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"environment":"sandbox","externalMerchantRef":"seal_org_1","displayName":"Acme Inc.","legalEntityType":"CORPORATION","country":"USA","merchantMode":"processing","defaultCurrency":"USD","metadata":{"sourceSystem":"seal","sealOrganizationId":"seal_org_1"}}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createMerchantAccount, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createMerchantAccount({
              client: vortex,
              body: {"environment":"sandbox","externalMerchantRef":"seal_org_1","displayName":"Acme Inc.","legalEntityType":"CORPORATION","country":"USA","merchantMode":"processing","defaultCurrency":"USD","metadata":{"sourceSystem":"seal","sealOrganizationId":"seal_org_1"}},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/can-accept-payments:
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_can_accept_payments_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/can-accept-payments
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/can-accept-payments" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCanAcceptPaymentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCanAcceptPaymentsExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/capabilities:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_capabilities_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/capabilities
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/capabilities" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCapabilitiesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCapabilitiesExact({
              client: vortex,
            });
    get:
      operationId: getMerchantAccountCapabilities
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantAccountCapabilitiesResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get merchant payments capability state
      tags:
        - billing
        - payments-readiness
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/capabilities?environment=sandbox" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getMerchantAccountCapabilities, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getMerchantAccountCapabilities({
              client: vortex,
              path: { merchantAccountId: "..." },
              query: { environment: "sandbox" },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_capabilities_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/capabilities
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/capabilities" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCapabilitiesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCapabilitiesExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_capabilities_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/capabilities
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/capabilities" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCapabilitiesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCapabilitiesExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_capabilities_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/capabilities
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/capabilities" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCapabilitiesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCapabilitiesExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers/{customerId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_customerId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers/:customerId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers/{customerId}/payment-methods:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers/{customerId}/payment-methods/{paymentMethodId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers/{customerId}/payment-methods/{paymentMethodId}/archive:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_archive_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_archive_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_archive_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_archive_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_archive_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/archive" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdArchiveExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers/{customerId}/payment-methods/{paymentMethodId}/disable:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_disable_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_disable_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_disable_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_disable_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_paymentMethodId_disable_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/:paymentMethodId/disable" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsPaymentMethodIdDisableExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers/{customerId}/payment-methods/default:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_default_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_default_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_default_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_default_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_methods_default_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-methods/default" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentMethodsDefaultExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/customers/{customerId}/payment-state:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_customers_customerId_payment_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/customers/:customerId/payment-state" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdCustomersCustomerIdPaymentStateExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding-link:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_link_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding-link" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingLinkExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_link_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding-link" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingLinkExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_link_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding-link" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingLinkExact({
              client: vortex,
            });
    post:
      operationId: createMerchantOnboardingLink
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  environment: sandbox
                  createdByRef: seal:org_1
            schema:
              $ref: "#/components/schemas/MerchantOnboardingLinkRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantOnboardingLinkResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a hosted merchant onboarding link
      tags:
        - billing
        - payments-readiness
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/onboarding-link" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"environment":"sandbox","createdByRef":"seal:org_1"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createMerchantOnboardingLink, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createMerchantOnboardingLink({
              client: vortex,
              path: { merchantAccountId: "..." },
              body: {"environment":"sandbox","createdByRef":"seal:org_1"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_link_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding-link" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingLinkExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/links:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/links/{linkId}/revoke:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/links/:linkId/revoke" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdLinksLinkIdRevokeExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/refresh:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/refresh" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRefreshExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/requirements:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/requirements/{requirementId}/documents:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/requirements/{requirementId}/documents/{documentId}/upload:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_documentId_upload_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: documentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_documentId_upload_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: documentId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_documentId_upload_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: documentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_documentId_upload_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: documentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_documents_documentId_upload_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: documentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/documents/:documentId/upload" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdDocumentsDocumentIdUploadExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/requirements/{requirementId}/refresh:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_refresh_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/refresh" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdRefreshExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/requirements/{requirementId}/upload-links:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_links_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-links" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadLinksExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/requirements/{requirementId}/upload-status:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_status_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_status_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_status_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_status_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_requirementId_upload_status_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: requirementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/:requirementId/upload-status" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsRequirementIdUploadStatusExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/{onboardingId}/requirements/submit:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_onboardingId_requirements_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: onboardingId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/:onboardingId/requirements/submit" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingOnboardingIdRequirementsSubmitExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/onboarding/submit:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_onboarding_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/onboarding/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/submit" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_onboarding_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/onboarding/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/submit" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_onboarding_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/onboarding/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/submit" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_onboarding_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/onboarding/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/submit" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_onboarding_submit_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/onboarding/submit
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/onboarding/submit" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdOnboardingSubmitExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/payout-profile:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_payout_profile_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/payout-profile
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/payout-profile" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdPayoutProfileExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdPayoutProfileExact({
              client: vortex,
            });
    get:
      operationId: getMerchantAccountPayoutProfile
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantAccountPayoutProfileResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get merchant payout profile
      tags:
        - billing
        - payments-readiness
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/payout-profile?environment=sandbox" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getMerchantAccountPayoutProfile, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getMerchantAccountPayoutProfile({
              client: vortex,
              path: { merchantAccountId: "..." },
              query: { environment: "sandbox" },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_payout_profile_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/payout-profile
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/payout-profile" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdPayoutProfileExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdPayoutProfileExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_payout_profile_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/payout-profile
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/payout-profile" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdPayoutProfileExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdPayoutProfileExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_payout_profile_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/payout-profile
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/payout-profile" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdPayoutProfileExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdPayoutProfileExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/settlements/{settlementId}/funding-timeline:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_settlements_settlementId_funding_timeline_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_settlements_settlementId_funding_timeline_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_settlements_settlementId_funding_timeline_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_settlements_settlementId_funding_timeline_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_settlements_settlementId_funding_timeline_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/funding-timeline" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdFundingTimelineExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/settlements/{settlementId}/payout-readiness:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_settlements_settlementId_payout_readiness_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_merchant_accounts_merchantAccountId_settlements_settlementId_payout_readiness_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_settlements_settlementId_payout_readiness_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_settlements_settlementId_payout_readiness_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_settlements_settlementId_payout_readiness_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/settlements/:settlementId/payout-readiness" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdSettlementsSettlementIdPayoutReadinessExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/state:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/state" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdStateExact({
              client: vortex,
            });
    get:
      operationId: getMerchantAccountState
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantAccountStateResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get merchant payments readiness state
      tags:
        - billing
        - payments-readiness
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/state?environment=sandbox" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getMerchantAccountState, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getMerchantAccountState({
              client: vortex,
              path: { merchantAccountId: "..." },
              query: { environment: "sandbox" },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/state" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdStateExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/state" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdStateExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_merchant_accounts_merchantAccountId_state_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/merchant-accounts/:merchantAccountId/state
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/state" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MerchantAccountsMerchantAccountIdStateExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MerchantAccountsMerchantAccountIdStateExact({
              client: vortex,
            });
  /v1/merchant-accounts/{merchantAccountId}/tax-profile:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_merchant_accounts_merchantAccountId_tax_profile_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/merchant-accounts/:merchantAccountId/tax-profile
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/tax-profile" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MerchantAccountsMerchantAccountIdTaxProfileExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MerchantAccountsMerchantAccountIdTaxProfileExact({
              client: vortex,
            });
    get:
      operationId: getMerchantAccountTaxProfile
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantTaxProfileResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get merchant tax profile
      tags:
        - billing
        - tax
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/tax-profile?environment=sandbox" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getMerchantAccountTaxProfile, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getMerchantAccountTaxProfile({
              client: vortex,
              path: { merchantAccountId: "..." },
              query: { environment: "sandbox" },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_merchant_accounts_merchantAccountId_tax_profile_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/merchant-accounts/:merchantAccountId/tax-profile
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/tax-profile" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MerchantAccountsMerchantAccountIdTaxProfileExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MerchantAccountsMerchantAccountIdTaxProfileExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_merchant_accounts_merchantAccountId_tax_profile_exact
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/merchant-accounts/:merchantAccountId/tax-profile
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/:merchantAccountId/tax-profile" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MerchantAccountsMerchantAccountIdTaxProfileExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MerchantAccountsMerchantAccountIdTaxProfileExact({
              client: vortex,
            });
    put:
      operationId: upsertMerchantAccountTaxProfile
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  environment: sandbox
                  originAddress:
                    line1: 1 MetroTech Center
                    city: Brooklyn
                    region: NY
                    postalCode: "11201"
                    country: US
                  registrations:
                    - country: US
                      region: NY
            schema:
              $ref: "#/components/schemas/MerchantTaxProfileRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantTaxProfileResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create or update merchant tax profile
      tags:
        - billing
        - tax
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/tax-profile" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"environment":"sandbox","originAddress":{"line1":"1 MetroTech Center","city":"Brooklyn","region":"NY","postalCode":"11201","country":"US"},"registrations":[{"country":"US","region":"NY"}]}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { upsertMerchantAccountTaxProfile, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await upsertMerchantAccountTaxProfile({
              client: vortex,
              path: { merchantAccountId: "..." },
              body: {"environment":"sandbox","originAddress":{"line1":"1 MetroTech Center","city":"Brooklyn","region":"NY","postalCode":"11201","country":"US"},"registrations":[{"country":"US","region":"NY"}]},
            });
  /v1/meters:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_meters_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/meters
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MetersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MetersExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_meters_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/meters
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MetersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MetersExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_meters_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/meters
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MetersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MetersExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_meters_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/meters
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MetersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MetersExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_meters_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/meters
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MetersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MetersExact({
              client: vortex,
            });
  /v1/meters/{meterId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_meters_meterId_exact
      parameters:
        - explode: false
          in: path
          name: meterId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/meters/:meterId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters/:meterId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1MetersMeterIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1MetersMeterIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_meters_meterId_exact
      parameters:
        - explode: false
          in: path
          name: meterId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/meters/:meterId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters/:meterId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1MetersMeterIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1MetersMeterIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_meters_meterId_exact
      parameters:
        - explode: false
          in: path
          name: meterId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/meters/:meterId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters/:meterId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1MetersMeterIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1MetersMeterIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_meters_meterId_exact
      parameters:
        - explode: false
          in: path
          name: meterId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/meters/:meterId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters/:meterId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1MetersMeterIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1MetersMeterIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_meters_meterId_exact
      parameters:
        - explode: false
          in: path
          name: meterId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/meters/:meterId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/meters/:meterId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1MetersMeterIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1MetersMeterIdExact({
              client: vortex,
            });
  /v1/order-forms:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsExact({
              client: vortex,
            });
    get:
      operationId: listBillingOrderForms
      parameters:
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: customerId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: status
          required: false
          schema:
            type: string
          style: form
        - description:
            "Page size, 1-100. Omitting both limit and cursor returns the\
            \ full unpaginated list."
          explode: true
          in: query
          name: limit
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
          style: form
        - description:
            Opaque cursor from the previous page's nextCursor. Pass it back
            verbatim to fetch the next page.
          explode: true
          in: query
          name: cursor
          required: false
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderFormListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List order forms by customer or status
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms?environment=sandbox&merchantAccountId=...&customerId=...&status=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listBillingOrderForms, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listBillingOrderForms({
              client: vortex,
              query: { environment: "sandbox", merchantAccountId: "...", customerId: "...", status: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_order_forms_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/order-forms
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1OrderFormsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1OrderFormsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsExact({
              client: vortex,
            });
  /v1/order-forms/{orderFormId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_orderFormId_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms/:orderFormId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsOrderFormIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsOrderFormIdExact({
              client: vortex,
            });
    get:
      operationId: getBillingOrderForm
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderFormResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect an order form with its quote and version
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/{orderFormId}?environment=sandbox&merchantAccountId=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getBillingOrderForm, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getBillingOrderForm({
              client: vortex,
              path: { orderFormId: "..." },
              query: { environment: "sandbox", merchantAccountId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_orderFormId_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms/:orderFormId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsOrderFormIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsOrderFormIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_order_forms_orderFormId_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/order-forms/:orderFormId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1OrderFormsOrderFormIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1OrderFormsOrderFormIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_orderFormId_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms/:orderFormId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsOrderFormIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsOrderFormIdExact({
              client: vortex,
            });
  /v1/order-forms/{orderFormId}/hosted-link:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_orderFormId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms/:orderFormId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsOrderFormIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsOrderFormIdHostedLinkExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_order_forms_orderFormId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/order-forms/:orderFormId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrderFormsOrderFormIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrderFormsOrderFormIdHostedLinkExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_orderFormId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms/:orderFormId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsOrderFormIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsOrderFormIdHostedLinkExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_order_forms_orderFormId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/order-forms/:orderFormId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1OrderFormsOrderFormIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1OrderFormsOrderFormIdHostedLinkExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_orderFormId_hosted_link_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms/:orderFormId/hosted-link
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsOrderFormIdHostedLinkExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsOrderFormIdHostedLinkExact({
              client: vortex,
            });
  /v1/order-forms/{orderFormId}/hosted-link/{hostedLinkId}/revoke:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_orderFormId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_order_forms_orderFormId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_orderFormId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_order_forms_orderFormId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_orderFormId_hosted_link_hostedLinkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: hostedLinkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/hosted-link/:hostedLinkId/revoke" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsOrderFormIdHostedLinkHostedLinkIdRevokeExact({
              client: vortex,
            });
  /v1/order-forms/{orderFormId}/sign:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_orderFormId_sign_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms/:orderFormId/sign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/sign" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsOrderFormIdSignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsOrderFormIdSignExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_order_forms_orderFormId_sign_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/order-forms/:orderFormId/sign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/sign" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrderFormsOrderFormIdSignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrderFormsOrderFormIdSignExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_orderFormId_sign_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms/:orderFormId/sign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/sign" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsOrderFormIdSignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsOrderFormIdSignExact({
              client: vortex,
            });
    post:
      operationId: markBillingOrderFormSigned
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  signedDocumentRef: vortexsign://envelope/env_123
                  executionMode: execute_in_vortex
            schema:
              $ref: "#/components/schemas/BillingOrderFormSignRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderFormSignResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary:
        "Mark an order form signed, attach a signed document reference, and\
        \ create the order"
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/{orderFormId}/sign" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"signedDocumentRef":"vortexsign://envelope/env_123","executionMode":"execute_in_vortex"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { markBillingOrderFormSigned, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await markBillingOrderFormSigned({
              client: vortex,
              path: { orderFormId: "..." },
              body: {"signedDocumentRef":"vortexsign://envelope/env_123","executionMode":"execute_in_vortex"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_orderFormId_sign_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms/:orderFormId/sign
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/sign" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsOrderFormIdSignExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsOrderFormIdSignExact({
              client: vortex,
            });
  /v1/order-forms/{orderFormId}/signature-request:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_orderFormId_signature_request_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms/:orderFormId/signature-request
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/signature-request" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsOrderFormIdSignatureRequestExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsOrderFormIdSignatureRequestExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_order_forms_orderFormId_signature_request_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/order-forms/:orderFormId/signature-request
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/signature-request" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrderFormsOrderFormIdSignatureRequestExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrderFormsOrderFormIdSignatureRequestExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_orderFormId_signature_request_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms/:orderFormId/signature-request
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/signature-request" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsOrderFormIdSignatureRequestExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsOrderFormIdSignatureRequestExact({
              client: vortex,
            });
    post:
      operationId: registerBillingOrderFormSignatureRequest
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  signatureRequestRef: vortexsign://envelope/env_123
            schema:
              $ref: "#/components/schemas/BillingOrderFormSignatureRequestRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderFormResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary:
        Register an external signature request (envelope/session) reference
        on a generated order form
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/{orderFormId}/signature-request" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"signatureRequestRef":"vortexsign://envelope/env_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { registerBillingOrderFormSignatureRequest, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await registerBillingOrderFormSignatureRequest({
              client: vortex,
              path: { orderFormId: "..." },
              body: {"signatureRequestRef":"vortexsign://envelope/env_123"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_orderFormId_signature_request_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms/:orderFormId/signature-request
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/signature-request" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsOrderFormIdSignatureRequestExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsOrderFormIdSignatureRequestExact({
              client: vortex,
            });
  /v1/order-forms/{orderFormId}/void:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_orderFormId_void_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms/:orderFormId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/void" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsOrderFormIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsOrderFormIdVoidExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_order_forms_orderFormId_void_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/order-forms/:orderFormId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/void" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrderFormsOrderFormIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrderFormsOrderFormIdVoidExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_orderFormId_void_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms/:orderFormId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/void" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsOrderFormIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsOrderFormIdVoidExact({
              client: vortex,
            });
    post:
      operationId: voidBillingOrderForm
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  reason: customer declined
            schema:
              $ref: "#/components/schemas/BillingOrderFormVoidRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderFormResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Void a generated order form and cascade-void its quote version
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/{orderFormId}/void" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"reason":"customer declined"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { voidBillingOrderForm, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await voidBillingOrderForm({
              client: vortex,
              path: { orderFormId: "..." },
              body: {"reason":"customer declined"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_orderFormId_void_exact
      parameters:
        - explode: false
          in: path
          name: orderFormId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms/:orderFormId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/:orderFormId/void" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsOrderFormIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsOrderFormIdVoidExact({
              client: vortex,
            });
  /v1/order-forms/expire-due:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_order_forms_expire_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/order-forms/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/expire-due" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrderFormsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrderFormsExpireDueExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_order_forms_expire_due_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/order-forms/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/expire-due" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrderFormsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrderFormsExpireDueExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_order_forms_expire_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/order-forms/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/expire-due" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrderFormsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrderFormsExpireDueExact({
              client: vortex,
            });
    post:
      operationId: expireDueBillingOrderForms
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  limit: 50
            schema:
              $ref: "#/components/schemas/BillingOrderFormsExpireDueRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderFormListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary:
        Expire generated order forms past their expiry and cascade-void their
        versions
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/expire-due" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"limit":50}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { expireDueBillingOrderForms, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await expireDueBillingOrderForms({
              client: vortex,
              body: {"limit":50},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_order_forms_expire_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/order-forms/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/order-forms/expire-due" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrderFormsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrderFormsExpireDueExact({
              client: vortex,
            });
  /v1/orders:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrdersExact({
              client: vortex,
            });
    get:
      operationId: listBillingOrders
      parameters:
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: customerId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: status
          required: false
          schema:
            type: string
          style: form
        - description:
            "Page size, 1-100. Omitting both limit and cursor returns the\
            \ full unpaginated list."
          explode: true
          in: query
          name: limit
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
          style: form
        - description:
            Opaque cursor from the previous page's nextCursor. Pass it back
            verbatim to fetch the next page.
          explode: true
          in: query
          name: cursor
          required: false
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List orders by customer or execution status
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders?environment=sandbox&merchantAccountId=...&customerId=...&status=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listBillingOrders, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listBillingOrders({
              client: vortex,
              query: { environment: "sandbox", merchantAccountId: "...", customerId: "...", status: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrdersExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1OrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1OrdersExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrdersExact({
              client: vortex,
            });
  /v1/orders/{orderId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_orders_orderId_exact
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/orders/:orderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/:orderId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrdersOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrdersOrderIdExact({
              client: vortex,
            });
    get:
      operationId: getBillingOrder
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect an order with its execution record
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/{orderId}?environment=sandbox&merchantAccountId=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getBillingOrder, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getBillingOrder({
              client: vortex,
              path: { orderId: "..." },
              query: { environment: "sandbox", merchantAccountId: "..." },
            });
    patch:
      operationId: updateBillingOrder
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  executionMode: execute_in_vortex
                  executeAt: 2026-10-15T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/BillingOrderUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Set or change execution settings on a created order
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/{orderId}" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"executionMode":"execute_in_vortex","executeAt":"2026-10-15T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { updateBillingOrder, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await updateBillingOrder({
              client: vortex,
              path: { orderId: "..." },
              body: {"executionMode":"execute_in_vortex","executeAt":"2026-10-15T00:00:00.000Z"},
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_orders_orderId_exact
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/orders/:orderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/:orderId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1OrdersOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1OrdersOrderIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_orders_orderId_exact
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/orders/:orderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/:orderId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrdersOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrdersOrderIdExact({
              client: vortex,
            });
  /v1/orders/{orderId}/execute:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_orders_orderId_execute_exact
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/orders/:orderId/execute
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/:orderId/execute" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrdersOrderIdExecuteExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrdersOrderIdExecuteExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_orders_orderId_execute_exact
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/orders/:orderId/execute
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/:orderId/execute" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrdersOrderIdExecuteExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrdersOrderIdExecuteExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_orders_orderId_execute_exact
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/orders/:orderId/execute
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/:orderId/execute" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrdersOrderIdExecuteExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrdersOrderIdExecuteExact({
              client: vortex,
            });
    post:
      operationId: executeBillingOrder
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value: {}
            schema:
              $ref: "#/components/schemas/BillingOrderExecuteRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary:
        Execute a created order into an invoice or subscription per its order
        type
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/{orderId}/execute" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { executeBillingOrder, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await executeBillingOrder({
              client: vortex,
              path: { orderId: "..." },
              body: {},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_orders_orderId_execute_exact
      parameters:
        - explode: false
          in: path
          name: orderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/orders/:orderId/execute
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/:orderId/execute" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrdersOrderIdExecuteExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrdersOrderIdExecuteExact({
              client: vortex,
            });
  /v1/orders/execute-due:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_orders_execute_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/orders/execute-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/execute-due" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1OrdersExecuteDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1OrdersExecuteDueExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_orders_execute_due_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/orders/execute-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/execute-due" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1OrdersExecuteDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1OrdersExecuteDueExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_orders_execute_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/orders/execute-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/execute-due" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1OrdersExecuteDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1OrdersExecuteDueExact({
              client: vortex,
            });
    post:
      operationId: executeDueBillingOrders
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  limit: 50
            schema:
              $ref: "#/components/schemas/BillingOrdersExecuteDueRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingOrderListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Execute created orders whose scheduled execution time has passed
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/execute-due" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"limit":50}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { executeDueBillingOrders, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await executeDueBillingOrders({
              client: vortex,
              body: {"limit":50},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_orders_execute_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/orders/execute-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/orders/execute-due" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1OrdersExecuteDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1OrdersExecuteDueExact({
              client: vortex,
            });
  /v1/payables:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payables_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payables
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PayablesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PayablesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payables_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payables
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PayablesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PayablesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payables_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payables
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PayablesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PayablesExact({
              client: vortex,
            });
    post:
      operationId: createPayable
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  sourceType: document_payment_field
                  sourceId: field_1
                  documentId: doc_1
                  paymentFieldId: field_1
                  customerExternalId: vtx_cust_seal_org_1
                  billingAccountId: bacc_1
                  collectionIntent: manual
                  feePolicy: {}
                  lineItems: []
                  metadata: {}
            schema:
              $ref: "#/components/schemas/CreatePayableRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayableResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a one-time document payable
      tags:
        - billing
        - payables
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","collectionIntent":"manual","feePolicy":{},"lineItems":[],"metadata":{}}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createPayable, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createPayable({
              client: vortex,
              body: {"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","collectionIntent":"manual","feePolicy":{},"lineItems":[],"metadata":{}},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payables_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payables
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PayablesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PayablesExact({
              client: vortex,
            });
  /v1/payables/{payableId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payables_payableId_exact
      parameters:
        - explode: false
          in: path
          name: payableId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payables/:payableId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables/:payableId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PayablesPayableIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PayablesPayableIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payables_payableId_exact
      parameters:
        - explode: false
          in: path
          name: payableId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payables/:payableId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables/:payableId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PayablesPayableIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PayablesPayableIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payables_payableId_exact
      parameters:
        - explode: false
          in: path
          name: payableId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payables/:payableId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables/:payableId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PayablesPayableIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PayablesPayableIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payables_payableId_exact
      parameters:
        - explode: false
          in: path
          name: payableId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payables/:payableId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables/:payableId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PayablesPayableIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PayablesPayableIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payables_payableId_exact
      parameters:
        - explode: false
          in: path
          name: payableId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payables/:payableId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payables/:payableId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PayablesPayableIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PayablesPayableIdExact({
              client: vortex,
            });
  /v1/payment-hardware-orders:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_hardware_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-hardware-orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentHardwareOrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentHardwareOrdersExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_hardware_orders_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-hardware-orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentHardwareOrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentHardwareOrdersExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_hardware_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-hardware-orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentHardwareOrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentHardwareOrdersExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_hardware_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-hardware-orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentHardwareOrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentHardwareOrdersExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_hardware_orders_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-hardware-orders
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentHardwareOrdersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentHardwareOrdersExact({
              client: vortex,
            });
  /v1/payment-hardware-orders/{paymentHardwareOrderId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_hardware_orders_paymentHardwareOrderId_exact
      parameters:
        - explode: false
          in: path
          name: paymentHardwareOrderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-hardware-orders/:paymentHardwareOrderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders/:paymentHardwareOrderId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentHardwareOrdersPaymentHardwareOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentHardwareOrdersPaymentHardwareOrderIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_hardware_orders_paymentHardwareOrderId_exact
      parameters:
        - explode: false
          in: path
          name: paymentHardwareOrderId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-hardware-orders/:paymentHardwareOrderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders/:paymentHardwareOrderId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentHardwareOrdersPaymentHardwareOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentHardwareOrdersPaymentHardwareOrderIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_hardware_orders_paymentHardwareOrderId_exact
      parameters:
        - explode: false
          in: path
          name: paymentHardwareOrderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-hardware-orders/:paymentHardwareOrderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders/:paymentHardwareOrderId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentHardwareOrdersPaymentHardwareOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentHardwareOrdersPaymentHardwareOrderIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_hardware_orders_paymentHardwareOrderId_exact
      parameters:
        - explode: false
          in: path
          name: paymentHardwareOrderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-hardware-orders/:paymentHardwareOrderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders/:paymentHardwareOrderId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentHardwareOrdersPaymentHardwareOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentHardwareOrdersPaymentHardwareOrderIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_hardware_orders_paymentHardwareOrderId_exact
      parameters:
        - explode: false
          in: path
          name: paymentHardwareOrderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-hardware-orders/:paymentHardwareOrderId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-orders/:paymentHardwareOrderId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentHardwareOrdersPaymentHardwareOrderIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentHardwareOrdersPaymentHardwareOrderIdExact({
              client: vortex,
            });
  /v1/payment-hardware-skus:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_hardware_skus_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-hardware-skus
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-skus" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentHardwareSkusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentHardwareSkusExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_hardware_skus_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-hardware-skus
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-skus" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentHardwareSkusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentHardwareSkusExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_hardware_skus_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-hardware-skus
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-skus" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentHardwareSkusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentHardwareSkusExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_hardware_skus_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-hardware-skus
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-skus" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentHardwareSkusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentHardwareSkusExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_hardware_skus_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-hardware-skus
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-hardware-skus" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentHardwareSkusExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentHardwareSkusExact({
              client: vortex,
            });
  /v1/payment-intents:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentIntentsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_intents_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentIntentsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentIntentsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentIntentsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_intents_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-intents
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentIntentsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentIntentsExact({
              client: vortex,
            });
  /v1/payment-intents/{paymentIntentId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_intents_paymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-intents/:paymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentIntentsPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentIntentsPaymentIntentIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_intents_paymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-intents/:paymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentIntentsPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentIntentsPaymentIntentIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_intents_paymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-intents/:paymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentIntentsPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentIntentsPaymentIntentIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_intents_paymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-intents/:paymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentIntentsPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentIntentsPaymentIntentIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_intents_paymentIntentId_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-intents/:paymentIntentId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentIntentsPaymentIntentIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentIntentsPaymentIntentIdExact({
              client: vortex,
            });
  /v1/payment-intents/{paymentIntentId}/cancel:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_intents_paymentIntentId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-intents/:paymentIntentId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/cancel" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentIntentsPaymentIntentIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentIntentsPaymentIntentIdCancelExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_intents_paymentIntentId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-intents/:paymentIntentId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/cancel" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentIntentsPaymentIntentIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentIntentsPaymentIntentIdCancelExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_intents_paymentIntentId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-intents/:paymentIntentId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/cancel" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentIntentsPaymentIntentIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentIntentsPaymentIntentIdCancelExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_intents_paymentIntentId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-intents/:paymentIntentId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/cancel" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentIntentsPaymentIntentIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentIntentsPaymentIntentIdCancelExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_intents_paymentIntentId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-intents/:paymentIntentId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/cancel" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentIntentsPaymentIntentIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentIntentsPaymentIntentIdCancelExact({
              client: vortex,
            });
  /v1/payment-intents/{paymentIntentId}/capture:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_intents_paymentIntentId_capture_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-intents/:paymentIntentId/capture
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/capture" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentIntentsPaymentIntentIdCaptureExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentIntentsPaymentIntentIdCaptureExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_intents_paymentIntentId_capture_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-intents/:paymentIntentId/capture
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/capture" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentIntentsPaymentIntentIdCaptureExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentIntentsPaymentIntentIdCaptureExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_intents_paymentIntentId_capture_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-intents/:paymentIntentId/capture
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/capture" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentIntentsPaymentIntentIdCaptureExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentIntentsPaymentIntentIdCaptureExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_intents_paymentIntentId_capture_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-intents/:paymentIntentId/capture
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/capture" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentIntentsPaymentIntentIdCaptureExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentIntentsPaymentIntentIdCaptureExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_intents_paymentIntentId_capture_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-intents/:paymentIntentId/capture
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/capture" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentIntentsPaymentIntentIdCaptureExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentIntentsPaymentIntentIdCaptureExact({
              client: vortex,
            });
  /v1/payment-intents/{paymentIntentId}/retry:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_intents_paymentIntentId_retry_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-intents/:paymentIntentId/retry
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/retry" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentIntentsPaymentIntentIdRetryExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentIntentsPaymentIntentIdRetryExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_intents_paymentIntentId_retry_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-intents/:paymentIntentId/retry
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/retry" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentIntentsPaymentIntentIdRetryExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentIntentsPaymentIntentIdRetryExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_intents_paymentIntentId_retry_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-intents/:paymentIntentId/retry
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/retry" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentIntentsPaymentIntentIdRetryExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentIntentsPaymentIntentIdRetryExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_intents_paymentIntentId_retry_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-intents/:paymentIntentId/retry
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/retry" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentIntentsPaymentIntentIdRetryExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentIntentsPaymentIntentIdRetryExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_intents_paymentIntentId_retry_exact
      parameters:
        - explode: false
          in: path
          name: paymentIntentId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-intents/:paymentIntentId/retry
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-intents/:paymentIntentId/retry" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentIntentsPaymentIntentIdRetryExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentIntentsPaymentIntentIdRetryExact({
              client: vortex,
            });
  /v1/payment-method-setup-sessions:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_method_setup_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-method-setup-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentMethodSetupSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentMethodSetupSessionsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_method_setup_sessions_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-method-setup-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentMethodSetupSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentMethodSetupSessionsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_method_setup_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-method-setup-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentMethodSetupSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentMethodSetupSessionsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_method_setup_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-method-setup-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentMethodSetupSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentMethodSetupSessionsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_method_setup_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-method-setup-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentMethodSetupSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentMethodSetupSessionsExact({
              client: vortex,
            });
  /v1/payment-method-setup-sessions/{paymentMethodSetupSessionId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExact({
              client: vortex,
            });
  /v1/payment-method-setup-sessions/{paymentMethodSetupSessionId}/exchange:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exchange_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exchange_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exchange_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exchange_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_method_setup_sessions_paymentMethodSetupSessionId_exchange_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodSetupSessionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-method-setup-sessions/:paymentMethodSetupSessionId/exchange" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentMethodSetupSessionsPaymentMethodSetupSessionIdExchangeExact({
              client: vortex,
            });
  /v1/payment-methods:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_methods_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-methods" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentMethodsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_methods_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-methods" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentMethodsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_methods_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-methods" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentMethodsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_methods_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-methods" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentMethodsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_methods_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-methods
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-methods" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentMethodsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentMethodsExact({
              client: vortex,
            });
  /v1/payment-methods/{paymentMethodId}:
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_methods_paymentMethodId_exact
      parameters:
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-methods/:paymentMethodId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-methods/:paymentMethodId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentMethodsPaymentMethodIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentMethodsPaymentMethodIdExact({
              client: vortex,
            });
  /v1/payment-receipts:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_receipts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentReceiptsExact({
              client: vortex,
            });
    get:
      operationId: listPaymentReceipts
      parameters:
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: customer
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: customerExternalId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: billingAccountId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: invoice
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: invoiceId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: paymentId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: paymentIntentId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: status
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: limit
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: offset
          required: false
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List payment receipts
      tags:
        - billing
        - receipts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts?environment=sandbox&merchantAccountId=...&customer=...&customerExternalId=...&billingAccountId=...&invoice=...&invoiceId=...&paymentId=...&paymentIntentId=...&status=...&limit=...&offset=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listPaymentReceipts, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listPaymentReceipts({
              client: vortex,
              query: { environment: "sandbox", merchantAccountId: "...", customer: "...", customerExternalId: "...", billingAccountId: "...", invoice: "...", invoiceId: "...", paymentId: "...", paymentIntentId: "...", status: "...", limit: "...", offset: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_receipts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentReceiptsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_receipts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentReceiptsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_receipts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-receipts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentReceiptsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentReceiptsExact({
              client: vortex,
            });
  /v1/payment-receipts/{paymentReceiptId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_receipts_paymentReceiptId_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-receipts/:paymentReceiptId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentReceiptsPaymentReceiptIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentReceiptsPaymentReceiptIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_receipts_paymentReceiptId_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-receipts/:paymentReceiptId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentReceiptsPaymentReceiptIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentReceiptsPaymentReceiptIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_receipts_paymentReceiptId_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-receipts/:paymentReceiptId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentReceiptsPaymentReceiptIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentReceiptsPaymentReceiptIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_receipts_paymentReceiptId_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-receipts/:paymentReceiptId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentReceiptsPaymentReceiptIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentReceiptsPaymentReceiptIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_receipts_paymentReceiptId_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-receipts/:paymentReceiptId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentReceiptsPaymentReceiptIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentReceiptsPaymentReceiptIdExact({
              client: vortex,
            });
  /v1/payment-receipts/{paymentReceiptId}/mark-generated:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_receipts_paymentReceiptId_mark_generated_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-receipts/:paymentReceiptId/mark-generated
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId/mark-generated" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_receipts_paymentReceiptId_mark_generated_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-receipts/:paymentReceiptId/mark-generated
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId/mark-generated" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_receipts_paymentReceiptId_mark_generated_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-receipts/:paymentReceiptId/mark-generated
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId/mark-generated" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_receipts_paymentReceiptId_mark_generated_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-receipts/:paymentReceiptId/mark-generated
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId/mark-generated" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_receipts_paymentReceiptId_mark_generated_exact
      parameters:
        - explode: false
          in: path
          name: paymentReceiptId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-receipts/:paymentReceiptId/mark-generated
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/:paymentReceiptId/mark-generated" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentReceiptsPaymentReceiptIdMarkGeneratedExact({
              client: vortex,
            });
  /v1/payment-requests:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsExact({
              client: vortex,
            });
    get:
      operationId: listPaymentRequests
      parameters:
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: customer
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: customerExternalId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: billingAccountId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: invoice
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: invoiceId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: paymentIntentId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: paymentId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: createdByRef
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: status
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: lastAttemptStatus
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: limit
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: offset
          required: false
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentRequestListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary:
        "List payment requests by customer, account, request status, or latest\
        \ attempt status"
      tags:
        - billing
        - payment-requests
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests?environment=sandbox&merchantAccountId=...&customer=...&customerExternalId=...&billingAccountId=...&invoice=...&invoiceId=...&paymentIntentId=...&paymentId=...&createdByRef=...&status=...&lastAttemptStatus=...&limit=...&offset=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listPaymentRequests, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listPaymentRequests({
              client: vortex,
              query: { environment: "sandbox", merchantAccountId: "...", customer: "...", customerExternalId: "...", billingAccountId: "...", invoice: "...", invoiceId: "...", paymentIntentId: "...", paymentId: "...", createdByRef: "...", status: "...", lastAttemptStatus: "...", limit: "...", offset: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsExact({
              client: vortex,
            });
    post:
      operationId: createPaymentRequest
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  paymentRequestId: preq_failed_invoice_123
                  invoiceNumbers:
                    - inv_123
                  createdByRef: dunning-recovery
                  expiresAt: 2026-06-08T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/PaymentRequestCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentRequestResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a hosted payment request for one or more recoverable invoices
      tags:
        - billing
        - payment-requests
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"paymentRequestId":"preq_failed_invoice_123","invoiceNumbers":["inv_123"],"createdByRef":"dunning-recovery","expiresAt":"2026-06-08T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createPaymentRequest, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createPaymentRequest({
              client: vortex,
              body: {"paymentRequestId":"preq_failed_invoice_123","invoiceNumbers":["inv_123"],"createdByRef":"dunning-recovery","expiresAt":"2026-06-08T00:00:00.000Z"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsExact({
              client: vortex,
            });
  /v1/payment-requests/{paymentRequestId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_paymentRequestId_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests/:paymentRequestId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsPaymentRequestIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsPaymentRequestIdExact({
              client: vortex,
            });
    get:
      operationId: getPaymentRequest
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentRequestResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect a payment request and its grouped invoices
      tags:
        - billing
        - payment-requests
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/{paymentRequestId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getPaymentRequest, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getPaymentRequest({
              client: vortex,
              path: { paymentRequestId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_paymentRequestId_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests/:paymentRequestId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsPaymentRequestIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsPaymentRequestIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_requests_paymentRequestId_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-requests/:paymentRequestId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentRequestsPaymentRequestIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentRequestsPaymentRequestIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_paymentRequestId_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests/:paymentRequestId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsPaymentRequestIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsPaymentRequestIdExact({
              client: vortex,
            });
  /v1/payment-requests/{paymentRequestId}/cancel:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_paymentRequestId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests/:paymentRequestId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/cancel" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsPaymentRequestIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsPaymentRequestIdCancelExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_requests_paymentRequestId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-requests/:paymentRequestId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/cancel" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentRequestsPaymentRequestIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentRequestsPaymentRequestIdCancelExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_paymentRequestId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests/:paymentRequestId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/cancel" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsPaymentRequestIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsPaymentRequestIdCancelExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_requests_paymentRequestId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-requests/:paymentRequestId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/cancel" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentRequestsPaymentRequestIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentRequestsPaymentRequestIdCancelExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_paymentRequestId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests/:paymentRequestId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/cancel" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsPaymentRequestIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsPaymentRequestIdCancelExact({
              client: vortex,
            });
  /v1/payment-requests/{paymentRequestId}/collect:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_paymentRequestId_collect_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests/:paymentRequestId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/collect" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsPaymentRequestIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsPaymentRequestIdCollectExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_requests_paymentRequestId_collect_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-requests/:paymentRequestId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/collect" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentRequestsPaymentRequestIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentRequestsPaymentRequestIdCollectExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_paymentRequestId_collect_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests/:paymentRequestId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/collect" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsPaymentRequestIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsPaymentRequestIdCollectExact({
              client: vortex,
            });
    post:
      operationId: collectPaymentRequest
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  paymentMethodId: pm_123
            schema:
              $ref: "#/components/schemas/PaymentRequestCollectRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentRequestResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Collect all eligible invoices in a payment request
      tags:
        - billing
        - payment-requests
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/{paymentRequestId}/collect" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"paymentMethodId":"pm_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { collectPaymentRequest, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await collectPaymentRequest({
              client: vortex,
              path: { paymentRequestId: "..." },
              body: {"paymentMethodId":"pm_123"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_paymentRequestId_collect_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests/:paymentRequestId/collect
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/collect" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsPaymentRequestIdCollectExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsPaymentRequestIdCollectExact({
              client: vortex,
            });
  /v1/payment-requests/{paymentRequestId}/expire:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_paymentRequestId_expire_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests/:paymentRequestId/expire
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/expire" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsPaymentRequestIdExpireExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsPaymentRequestIdExpireExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_requests_paymentRequestId_expire_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-requests/:paymentRequestId/expire
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/expire" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentRequestsPaymentRequestIdExpireExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentRequestsPaymentRequestIdExpireExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_paymentRequestId_expire_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests/:paymentRequestId/expire
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/expire" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsPaymentRequestIdExpireExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsPaymentRequestIdExpireExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_requests_paymentRequestId_expire_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-requests/:paymentRequestId/expire
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/expire" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentRequestsPaymentRequestIdExpireExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentRequestsPaymentRequestIdExpireExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_paymentRequestId_expire_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests/:paymentRequestId/expire
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/expire" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsPaymentRequestIdExpireExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsPaymentRequestIdExpireExact({
              client: vortex,
            });
  /v1/payment-requests/{paymentRequestId}/links:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_paymentRequestId_links_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests/:paymentRequestId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsPaymentRequestIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsPaymentRequestIdLinksExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_requests_paymentRequestId_links_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-requests/:paymentRequestId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentRequestsPaymentRequestIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentRequestsPaymentRequestIdLinksExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_paymentRequestId_links_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests/:paymentRequestId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsPaymentRequestIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsPaymentRequestIdLinksExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_requests_paymentRequestId_links_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-requests/:paymentRequestId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentRequestsPaymentRequestIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentRequestsPaymentRequestIdLinksExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_paymentRequestId_links_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests/:paymentRequestId/links
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsPaymentRequestIdLinksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsPaymentRequestIdLinksExact({
              client: vortex,
            });
  /v1/payment-requests/{paymentRequestId}/links/{linkId}/revoke:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_paymentRequestId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests/:paymentRequestId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links/:linkId/revoke" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_requests_paymentRequestId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-requests/:paymentRequestId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links/:linkId/revoke" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_paymentRequestId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests/:paymentRequestId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links/:linkId/revoke" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_requests_paymentRequestId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-requests/:paymentRequestId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links/:linkId/revoke" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_paymentRequestId_links_linkId_revoke_exact
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: linkId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests/:paymentRequestId/links/:linkId/revoke
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/:paymentRequestId/links/:linkId/revoke" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsPaymentRequestIdLinksLinkIdRevokeExact({
              client: vortex,
            });
  /v1/payment-requests/expire-due:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payment_requests_expire_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payment-requests/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/expire-due" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PaymentRequestsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PaymentRequestsExpireDueExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payment_requests_expire_due_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payment-requests/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/expire-due" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PaymentRequestsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PaymentRequestsExpireDueExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payment_requests_expire_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payment-requests/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/expire-due" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PaymentRequestsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PaymentRequestsExpireDueExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payment_requests_expire_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payment-requests/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/expire-due" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PaymentRequestsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PaymentRequestsExpireDueExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payment_requests_expire_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payment-requests/expire-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/expire-due" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PaymentRequestsExpireDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PaymentRequestsExpireDueExact({
              client: vortex,
            });
  /v1/payouts:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payouts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payouts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PayoutsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PayoutsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payouts_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payouts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PayoutsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PayoutsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payouts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payouts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PayoutsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PayoutsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payouts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payouts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PayoutsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PayoutsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payouts_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payouts
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PayoutsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PayoutsExact({
              client: vortex,
            });
  /v1/payouts/{payoutId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_payouts_payoutId_exact
      parameters:
        - explode: false
          in: path
          name: payoutId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/payouts/:payoutId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts/:payoutId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PayoutsPayoutIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PayoutsPayoutIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_payouts_payoutId_exact
      parameters:
        - explode: false
          in: path
          name: payoutId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/payouts/:payoutId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts/:payoutId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PayoutsPayoutIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PayoutsPayoutIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_payouts_payoutId_exact
      parameters:
        - explode: false
          in: path
          name: payoutId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/payouts/:payoutId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts/:payoutId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PayoutsPayoutIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PayoutsPayoutIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_payouts_payoutId_exact
      parameters:
        - explode: false
          in: path
          name: payoutId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/payouts/:payoutId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts/:payoutId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PayoutsPayoutIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PayoutsPayoutIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_payouts_payoutId_exact
      parameters:
        - explode: false
          in: path
          name: payoutId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/payouts/:payoutId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payouts/:payoutId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PayoutsPayoutIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PayoutsPayoutIdExact({
              client: vortex,
            });
  /v1/prices:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_prices_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/prices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PricesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PricesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_prices_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/prices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PricesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PricesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_prices_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/prices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PricesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PricesExact({
              client: vortex,
            });
    post:
      operationId: createPrice
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  priceId: price_core_monthly
                  productId: prod_core
                  name: Core monthly
                  priceType: fixed_recurring
                  currency: USD
                  billingInterval: month
                  billingIntervalCount: 1
                  unitAmount: 4900
            schema:
              $ref: "#/components/schemas/PriceCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PriceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a catalog price
      tags:
        - billing
        - catalog
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"priceId":"price_core_monthly","productId":"prod_core","name":"Core monthly","priceType":"fixed_recurring","currency":"USD","billingInterval":"month","billingIntervalCount":1,"unitAmount":4900}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createPrice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createPrice({
              client: vortex,
              body: {"priceId":"price_core_monthly","productId":"prod_core","name":"Core monthly","priceType":"fixed_recurring","currency":"USD","billingInterval":"month","billingIntervalCount":1,"unitAmount":4900},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_prices_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/prices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PricesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PricesExact({
              client: vortex,
            });
  /v1/prices/{priceId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_prices_priceId_exact
      parameters:
        - explode: false
          in: path
          name: priceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/prices/:priceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices/:priceId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1PricesPriceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1PricesPriceIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_prices_priceId_exact
      parameters:
        - explode: false
          in: path
          name: priceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/prices/:priceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices/:priceId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1PricesPriceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1PricesPriceIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_prices_priceId_exact
      parameters:
        - explode: false
          in: path
          name: priceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/prices/:priceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices/:priceId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1PricesPriceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1PricesPriceIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_prices_priceId_exact
      parameters:
        - explode: false
          in: path
          name: priceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/prices/:priceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices/:priceId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1PricesPriceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1PricesPriceIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_prices_priceId_exact
      parameters:
        - explode: false
          in: path
          name: priceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/prices/:priceId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/prices/:priceId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1PricesPriceIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1PricesPriceIdExact({
              client: vortex,
            });
  /v1/products:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_products_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/products
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1ProductsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1ProductsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_products_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/products
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1ProductsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1ProductsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_products_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/products
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1ProductsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1ProductsExact({
              client: vortex,
            });
    post:
      operationId: createProduct
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  productId: prod_core
                  name: Core
                  description: Core subscription product
            schema:
              $ref: "#/components/schemas/ProductCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a catalog product
      tags:
        - billing
        - catalog
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"productId":"prod_core","name":"Core","description":"Core subscription product"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createProduct, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createProduct({
              client: vortex,
              body: {"productId":"prod_core","name":"Core","description":"Core subscription product"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_products_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/products
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1ProductsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1ProductsExact({
              client: vortex,
            });
  /v1/products/{productId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_products_productId_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/products/:productId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1ProductsProductIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1ProductsProductIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_products_productId_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/products/:productId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1ProductsProductIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1ProductsProductIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_products_productId_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/products/:productId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1ProductsProductIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1ProductsProductIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_products_productId_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/products/:productId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1ProductsProductIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1ProductsProductIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_products_productId_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/products/:productId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1ProductsProductIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1ProductsProductIdExact({
              client: vortex,
            });
  /v1/products/{productId}/custom-fields:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_products_productId_custom_fields_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/products/:productId/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId/custom-fields" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1ProductsProductIdCustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1ProductsProductIdCustomFieldsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_products_productId_custom_fields_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/products/:productId/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId/custom-fields" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1ProductsProductIdCustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1ProductsProductIdCustomFieldsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_products_productId_custom_fields_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/products/:productId/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId/custom-fields" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1ProductsProductIdCustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1ProductsProductIdCustomFieldsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_products_productId_custom_fields_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/products/:productId/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId/custom-fields" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1ProductsProductIdCustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1ProductsProductIdCustomFieldsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_products_productId_custom_fields_exact
      parameters:
        - explode: false
          in: path
          name: productId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/products/:productId/custom-fields
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/products/:productId/custom-fields" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1ProductsProductIdCustomFieldsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1ProductsProductIdCustomFieldsExact({
              client: vortex,
            });
  /v1/quote-versions/{quoteVersionId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_quote_versions_quoteVersionId_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/quote-versions/:quoteVersionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1QuoteVersionsQuoteVersionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1QuoteVersionsQuoteVersionIdExact({
              client: vortex,
            });
    get:
      operationId: getBillingQuoteVersion
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteVersionResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect one quote version with its billing snapshot
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/{quoteVersionId}?environment=sandbox&merchantAccountId=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getBillingQuoteVersion, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getBillingQuoteVersion({
              client: vortex,
              path: { quoteVersionId: "..." },
              query: { environment: "sandbox", merchantAccountId: "..." },
            });
    patch:
      operationId: updateBillingQuoteVersion
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  lines:
                    - priceId: bprice_seats
                      quantity: 12
                      unitAmount: 4700
            schema:
              $ref: "#/components/schemas/BillingQuoteVersionUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteVersionResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: "Edit a draft quote version's lines, terms, or currency"
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/{quoteVersionId}" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"lines":[{"priceId":"bprice_seats","quantity":12,"unitAmount":4700}]}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { updateBillingQuoteVersion, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await updateBillingQuoteVersion({
              client: vortex,
              path: { quoteVersionId: "..." },
              body: {"lines":[{"priceId":"bprice_seats","quantity":12,"unitAmount":4700}]},
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_quote_versions_quoteVersionId_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/quote-versions/:quoteVersionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1QuoteVersionsQuoteVersionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1QuoteVersionsQuoteVersionIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_quote_versions_quoteVersionId_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/quote-versions/:quoteVersionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1QuoteVersionsQuoteVersionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1QuoteVersionsQuoteVersionIdExact({
              client: vortex,
            });
  /v1/quote-versions/{quoteVersionId}/approve:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_quote_versions_quoteVersionId_approve_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/quote-versions/:quoteVersionId/approve
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/approve" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1QuoteVersionsQuoteVersionIdApproveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1QuoteVersionsQuoteVersionIdApproveExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_quote_versions_quoteVersionId_approve_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/quote-versions/:quoteVersionId/approve
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/approve" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1QuoteVersionsQuoteVersionIdApproveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1QuoteVersionsQuoteVersionIdApproveExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_quote_versions_quoteVersionId_approve_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/quote-versions/:quoteVersionId/approve
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/approve" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1QuoteVersionsQuoteVersionIdApproveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1QuoteVersionsQuoteVersionIdApproveExact({
              client: vortex,
            });
    post:
      operationId: approveBillingQuoteVersion
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  expiresAt: 2026-10-01T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/BillingQuoteVersionApproveRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteVersionApproveResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Approve a draft quote version and generate its order form
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/{quoteVersionId}/approve" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"expiresAt":"2026-10-01T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { approveBillingQuoteVersion, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await approveBillingQuoteVersion({
              client: vortex,
              path: { quoteVersionId: "..." },
              body: {"expiresAt":"2026-10-01T00:00:00.000Z"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_quote_versions_quoteVersionId_approve_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/quote-versions/:quoteVersionId/approve
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/approve" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1QuoteVersionsQuoteVersionIdApproveExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1QuoteVersionsQuoteVersionIdApproveExact({
              client: vortex,
            });
  /v1/quote-versions/{quoteVersionId}/void:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_quote_versions_quoteVersionId_void_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/quote-versions/:quoteVersionId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/void" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1QuoteVersionsQuoteVersionIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1QuoteVersionsQuoteVersionIdVoidExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_quote_versions_quoteVersionId_void_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/quote-versions/:quoteVersionId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/void" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1QuoteVersionsQuoteVersionIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1QuoteVersionsQuoteVersionIdVoidExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_quote_versions_quoteVersionId_void_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/quote-versions/:quoteVersionId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/void" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1QuoteVersionsQuoteVersionIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1QuoteVersionsQuoteVersionIdVoidExact({
              client: vortex,
            });
    post:
      operationId: voidBillingQuoteVersion
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  reason: manual
            schema:
              $ref: "#/components/schemas/BillingQuoteVersionVoidRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteVersionResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Void a draft quote version
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/{quoteVersionId}/void" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"reason":"manual"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { voidBillingQuoteVersion, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await voidBillingQuoteVersion({
              client: vortex,
              path: { quoteVersionId: "..." },
              body: {"reason":"manual"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_quote_versions_quoteVersionId_void_exact
      parameters:
        - explode: false
          in: path
          name: quoteVersionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/quote-versions/:quoteVersionId/void
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quote-versions/:quoteVersionId/void" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1QuoteVersionsQuoteVersionIdVoidExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1QuoteVersionsQuoteVersionIdVoidExact({
              client: vortex,
            });
  /v1/quotes:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_quotes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/quotes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1QuotesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1QuotesExact({
              client: vortex,
            });
    get:
      operationId: listBillingQuotes
      parameters:
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: customerId
          required: false
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: billingAccountId
          required: false
          schema:
            type: string
          style: form
        - description:
            "Page size, 1-100. Omitting both limit and cursor returns the\
            \ full unpaginated list."
          explode: true
          in: query
          name: limit
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
          style: form
        - description:
            Opaque cursor from the previous page's nextCursor. Pass it back
            verbatim to fetch the next page.
          explode: true
          in: query
          name: cursor
          required: false
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List billing quotes by customer or account
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes?environment=sandbox&merchantAccountId=...&customerId=...&billingAccountId=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listBillingQuotes, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listBillingQuotes({
              client: vortex,
              query: { environment: "sandbox", merchantAccountId: "...", customerId: "...", billingAccountId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_quotes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/quotes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1QuotesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1QuotesExact({
              client: vortex,
            });
    post:
      operationId: createBillingQuote
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  orderType: one_off
                  customerId: bcust_123
                  billingAccountId: bacc_123
                  currency: USD
                  lines:
                    - priceId: bprice_setup
                      quantity: 1
                    - priceId: bprice_seats
                      quantity: 10
                      unitAmount: 4900
                  terms: Net 30
            schema:
              $ref: "#/components/schemas/BillingQuoteCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary:
        Create a billing quote with its first draft version from catalog price
        lines
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"orderType":"one_off","customerId":"bcust_123","billingAccountId":"bacc_123","currency":"USD","lines":[{"priceId":"bprice_setup","quantity":1},{"priceId":"bprice_seats","quantity":10,"unitAmount":4900}],"terms":"Net 30"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createBillingQuote, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createBillingQuote({
              client: vortex,
              body: {"orderType":"one_off","customerId":"bcust_123","billingAccountId":"bacc_123","currency":"USD","lines":[{"priceId":"bprice_setup","quantity":1},{"priceId":"bprice_seats","quantity":10,"unitAmount":4900}],"terms":"Net 30"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_quotes_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/quotes
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1QuotesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1QuotesExact({
              client: vortex,
            });
  /v1/quotes/{quoteId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_quotes_quoteId_exact
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/quotes/:quoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/:quoteId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1QuotesQuoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1QuotesQuoteIdExact({
              client: vortex,
            });
    get:
      operationId: getBillingQuote
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteDetailResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect a quote with its version history and current version
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/{quoteId}?environment=sandbox&merchantAccountId=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getBillingQuote, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getBillingQuote({
              client: vortex,
              path: { quoteId: "..." },
              query: { environment: "sandbox", merchantAccountId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_quotes_quoteId_exact
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/quotes/:quoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/:quoteId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1QuotesQuoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1QuotesQuoteIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_quotes_quoteId_exact
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/quotes/:quoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/:quoteId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1QuotesQuoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1QuotesQuoteIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_quotes_quoteId_exact
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/quotes/:quoteId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/:quoteId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1QuotesQuoteIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1QuotesQuoteIdExact({
              client: vortex,
            });
  /v1/quotes/{quoteId}/versions:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_quotes_quoteId_versions_exact
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/quotes/:quoteId/versions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/:quoteId/versions" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1QuotesQuoteIdVersionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1QuotesQuoteIdVersionsExact({
              client: vortex,
            });
    get:
      operationId: listBillingQuoteVersions
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - explode: true
          in: query
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteVersionListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List all versions of a quote
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/{quoteId}/versions?environment=sandbox&merchantAccountId=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listBillingQuoteVersions, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listBillingQuoteVersions({
              client: vortex,
              path: { quoteId: "..." },
              query: { environment: "sandbox", merchantAccountId: "..." },
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_quotes_quoteId_versions_exact
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/quotes/:quoteId/versions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/:quoteId/versions" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1QuotesQuoteIdVersionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1QuotesQuoteIdVersionsExact({
              client: vortex,
            });
    post:
      operationId: createBillingQuoteVersion
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  lines:
                    - priceId: bprice_seats
                      quantity: 15
                      unitAmount: 4500
                  terms: "Net 30, revised seat count"
            schema:
              $ref: "#/components/schemas/BillingQuoteVersionCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BillingQuoteVersionResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Open a new draft version for a quote once the prior version is voided
      tags:
        - billing
        - quotes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/{quoteId}/versions" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"lines":[{"priceId":"bprice_seats","quantity":15,"unitAmount":4500}],"terms":"Net 30, revised seat count"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createBillingQuoteVersion, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createBillingQuoteVersion({
              client: vortex,
              path: { quoteId: "..." },
              body: {"lines":[{"priceId":"bprice_seats","quantity":15,"unitAmount":4500}],"terms":"Net 30, revised seat count"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_quotes_quoteId_versions_exact
      parameters:
        - explode: false
          in: path
          name: quoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/quotes/:quoteId/versions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/quotes/:quoteId/versions" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1QuotesQuoteIdVersionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1QuotesQuoteIdVersionsExact({
              client: vortex,
            });
  /v1/refunds:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_refunds_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/refunds
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1RefundsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1RefundsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_refunds_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/refunds
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1RefundsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1RefundsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_refunds_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/refunds
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1RefundsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1RefundsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_refunds_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/refunds
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1RefundsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1RefundsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_refunds_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/refunds
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1RefundsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1RefundsExact({
              client: vortex,
            });
  /v1/refunds/{refundId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_refunds_refundId_exact
      parameters:
        - explode: false
          in: path
          name: refundId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/refunds/:refundId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds/:refundId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1RefundsRefundIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1RefundsRefundIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_refunds_refundId_exact
      parameters:
        - explode: false
          in: path
          name: refundId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/refunds/:refundId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds/:refundId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1RefundsRefundIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1RefundsRefundIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_refunds_refundId_exact
      parameters:
        - explode: false
          in: path
          name: refundId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/refunds/:refundId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds/:refundId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1RefundsRefundIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1RefundsRefundIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_refunds_refundId_exact
      parameters:
        - explode: false
          in: path
          name: refundId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/refunds/:refundId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds/:refundId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1RefundsRefundIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1RefundsRefundIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_refunds_refundId_exact
      parameters:
        - explode: false
          in: path
          name: refundId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/refunds/:refundId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/refunds/:refundId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1RefundsRefundIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1RefundsRefundIdExact({
              client: vortex,
            });
  /v1/settlements:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_settlements_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/settlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SettlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SettlementsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_settlements_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/settlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SettlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SettlementsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_settlements_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/settlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SettlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SettlementsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_settlements_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/settlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SettlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SettlementsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_settlements_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/settlements
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SettlementsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SettlementsExact({
              client: vortex,
            });
  /v1/settlements/{settlementId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_settlements_settlementId_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/settlements/:settlementId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SettlementsSettlementIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SettlementsSettlementIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_settlements_settlementId_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/settlements/:settlementId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SettlementsSettlementIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SettlementsSettlementIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_settlements_settlementId_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/settlements/:settlementId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SettlementsSettlementIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SettlementsSettlementIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_settlements_settlementId_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/settlements/:settlementId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SettlementsSettlementIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SettlementsSettlementIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_settlements_settlementId_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/settlements/:settlementId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SettlementsSettlementIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SettlementsSettlementIdExact({
              client: vortex,
            });
  /v1/settlements/{settlementId}/approval:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_settlements_settlementId_approval_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/settlements/:settlementId/approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId/approval" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SettlementsSettlementIdApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SettlementsSettlementIdApprovalExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_settlements_settlementId_approval_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/settlements/:settlementId/approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId/approval" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SettlementsSettlementIdApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SettlementsSettlementIdApprovalExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_settlements_settlementId_approval_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/settlements/:settlementId/approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId/approval" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SettlementsSettlementIdApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SettlementsSettlementIdApprovalExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_settlements_settlementId_approval_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/settlements/:settlementId/approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId/approval" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SettlementsSettlementIdApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SettlementsSettlementIdApprovalExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_settlements_settlementId_approval_exact
      parameters:
        - explode: false
          in: path
          name: settlementId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/settlements/:settlementId/approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/:settlementId/approval" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SettlementsSettlementIdApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SettlementsSettlementIdApprovalExact({
              client: vortex,
            });
  /v1/settlements/awaiting-approval:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_settlements_awaiting_approval_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/settlements/awaiting-approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/awaiting-approval" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SettlementsAwaitingApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SettlementsAwaitingApprovalExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_settlements_awaiting_approval_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/settlements/awaiting-approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/awaiting-approval" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SettlementsAwaitingApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SettlementsAwaitingApprovalExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_settlements_awaiting_approval_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/settlements/awaiting-approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/awaiting-approval" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SettlementsAwaitingApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SettlementsAwaitingApprovalExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_settlements_awaiting_approval_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/settlements/awaiting-approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/awaiting-approval" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SettlementsAwaitingApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SettlementsAwaitingApprovalExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_settlements_awaiting_approval_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/settlements/awaiting-approval
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/settlements/awaiting-approval" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SettlementsAwaitingApprovalExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SettlementsAwaitingApprovalExact({
              client: vortex,
            });
  /v1/subscriptions:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_subscriptions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SubscriptionsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_subscriptions_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SubscriptionsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_subscriptions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SubscriptionsExact({
              client: vortex,
            });
    post:
      operationId: createSubscription
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  customerExternalId: cus_123
                  subscriptionExternalId: sub_123
                  planCode: price_core_monthly
                  collectionMode: automatic
            schema:
              $ref: "#/components/schemas/SubscriptionCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a subscription
      tags:
        - billing
        - subscriptions
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"customerExternalId":"cus_123","subscriptionExternalId":"sub_123","planCode":"price_core_monthly","collectionMode":"automatic"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createSubscription, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createSubscription({
              client: vortex,
              body: {"customerExternalId":"cus_123","subscriptionExternalId":"sub_123","planCode":"price_core_monthly","collectionMode":"automatic"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_subscriptions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/subscriptions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SubscriptionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SubscriptionsExact({
              client: vortex,
            });
  /v1/subscriptions/{subscriptionId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_subscriptions_subscriptionId_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/subscriptions/:subscriptionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SubscriptionsSubscriptionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SubscriptionsSubscriptionIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_subscriptions_subscriptionId_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/subscriptions/:subscriptionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SubscriptionsSubscriptionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SubscriptionsSubscriptionIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_subscriptions_subscriptionId_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/subscriptions/:subscriptionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SubscriptionsSubscriptionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SubscriptionsSubscriptionIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_subscriptions_subscriptionId_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/subscriptions/:subscriptionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SubscriptionsSubscriptionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SubscriptionsSubscriptionIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_subscriptions_subscriptionId_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/subscriptions/:subscriptionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SubscriptionsSubscriptionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SubscriptionsSubscriptionIdExact({
              client: vortex,
            });
  /v1/subscriptions/{subscriptionId}/cancel:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_subscriptions_subscriptionId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/subscriptions/:subscriptionId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/cancel" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SubscriptionsSubscriptionIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SubscriptionsSubscriptionIdCancelExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_subscriptions_subscriptionId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/subscriptions/:subscriptionId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/cancel" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SubscriptionsSubscriptionIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SubscriptionsSubscriptionIdCancelExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_subscriptions_subscriptionId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/subscriptions/:subscriptionId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/cancel" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SubscriptionsSubscriptionIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SubscriptionsSubscriptionIdCancelExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_subscriptions_subscriptionId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/subscriptions/:subscriptionId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/cancel" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SubscriptionsSubscriptionIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SubscriptionsSubscriptionIdCancelExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_subscriptions_subscriptionId_cancel_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/subscriptions/:subscriptionId/cancel
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/cancel" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SubscriptionsSubscriptionIdCancelExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SubscriptionsSubscriptionIdCancelExact({
              client: vortex,
            });
  /v1/subscriptions/{subscriptionId}/invoices:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_subscriptions_subscriptionId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/subscriptions/:subscriptionId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/invoices" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SubscriptionsSubscriptionIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SubscriptionsSubscriptionIdInvoicesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_subscriptions_subscriptionId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/subscriptions/:subscriptionId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/invoices" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SubscriptionsSubscriptionIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SubscriptionsSubscriptionIdInvoicesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_subscriptions_subscriptionId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/subscriptions/:subscriptionId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/invoices" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SubscriptionsSubscriptionIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SubscriptionsSubscriptionIdInvoicesExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_subscriptions_subscriptionId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/subscriptions/:subscriptionId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/invoices" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SubscriptionsSubscriptionIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SubscriptionsSubscriptionIdInvoicesExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_subscriptions_subscriptionId_invoices_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/subscriptions/:subscriptionId/invoices
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/invoices" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SubscriptionsSubscriptionIdInvoicesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SubscriptionsSubscriptionIdInvoicesExact({
              client: vortex,
            });
  /v1/subscriptions/{subscriptionId}/pause:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_subscriptions_subscriptionId_pause_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/subscriptions/:subscriptionId/pause
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/pause" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SubscriptionsSubscriptionIdPauseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SubscriptionsSubscriptionIdPauseExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_subscriptions_subscriptionId_pause_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/subscriptions/:subscriptionId/pause
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/pause" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SubscriptionsSubscriptionIdPauseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SubscriptionsSubscriptionIdPauseExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_subscriptions_subscriptionId_pause_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/subscriptions/:subscriptionId/pause
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/pause" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SubscriptionsSubscriptionIdPauseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SubscriptionsSubscriptionIdPauseExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_subscriptions_subscriptionId_pause_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/subscriptions/:subscriptionId/pause
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/pause" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SubscriptionsSubscriptionIdPauseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SubscriptionsSubscriptionIdPauseExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_subscriptions_subscriptionId_pause_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/subscriptions/:subscriptionId/pause
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/pause" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SubscriptionsSubscriptionIdPauseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SubscriptionsSubscriptionIdPauseExact({
              client: vortex,
            });
  /v1/subscriptions/{subscriptionId}/resume:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_subscriptions_subscriptionId_resume_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/subscriptions/:subscriptionId/resume
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/resume" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SubscriptionsSubscriptionIdResumeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SubscriptionsSubscriptionIdResumeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_subscriptions_subscriptionId_resume_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/subscriptions/:subscriptionId/resume
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/resume" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SubscriptionsSubscriptionIdResumeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SubscriptionsSubscriptionIdResumeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_subscriptions_subscriptionId_resume_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/subscriptions/:subscriptionId/resume
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/resume" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SubscriptionsSubscriptionIdResumeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SubscriptionsSubscriptionIdResumeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_subscriptions_subscriptionId_resume_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/subscriptions/:subscriptionId/resume
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/resume" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SubscriptionsSubscriptionIdResumeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SubscriptionsSubscriptionIdResumeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_subscriptions_subscriptionId_resume_exact
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/subscriptions/:subscriptionId/resume
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/:subscriptionId/resume" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SubscriptionsSubscriptionIdResumeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SubscriptionsSubscriptionIdResumeExact({
              client: vortex,
            });
  /v1/subscriptions/activate-due:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_subscriptions_activate_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/subscriptions/activate-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/activate-due" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SubscriptionsActivateDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SubscriptionsActivateDueExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_subscriptions_activate_due_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/subscriptions/activate-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/activate-due" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SubscriptionsActivateDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SubscriptionsActivateDueExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_subscriptions_activate_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/subscriptions/activate-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/activate-due" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SubscriptionsActivateDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SubscriptionsActivateDueExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_subscriptions_activate_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/subscriptions/activate-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/activate-due" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SubscriptionsActivateDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SubscriptionsActivateDueExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_subscriptions_activate_due_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/subscriptions/activate-due
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/activate-due" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SubscriptionsActivateDueExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SubscriptionsActivateDueExact({
              client: vortex,
            });
  /v1/tax-transactions:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_tax_transactions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/tax-transactions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TaxTransactionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TaxTransactionsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_tax_transactions_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/tax-transactions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TaxTransactionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TaxTransactionsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_tax_transactions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/tax-transactions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TaxTransactionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TaxTransactionsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_tax_transactions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/tax-transactions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TaxTransactionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TaxTransactionsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_tax_transactions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/tax-transactions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TaxTransactionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TaxTransactionsExact({
              client: vortex,
            });
  /v1/tax-transactions/{taxTransactionId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_tax_transactions_taxTransactionId_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/tax-transactions/:taxTransactionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TaxTransactionsTaxTransactionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TaxTransactionsTaxTransactionIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_tax_transactions_taxTransactionId_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/tax-transactions/:taxTransactionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TaxTransactionsTaxTransactionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TaxTransactionsTaxTransactionIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_tax_transactions_taxTransactionId_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/tax-transactions/:taxTransactionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TaxTransactionsTaxTransactionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TaxTransactionsTaxTransactionIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_tax_transactions_taxTransactionId_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/tax-transactions/:taxTransactionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TaxTransactionsTaxTransactionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TaxTransactionsTaxTransactionIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_tax_transactions_taxTransactionId_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/tax-transactions/:taxTransactionId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TaxTransactionsTaxTransactionIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TaxTransactionsTaxTransactionIdExact({
              client: vortex,
            });
  /v1/tax-transactions/{taxTransactionId}/finalize:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_tax_transactions_taxTransactionId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/tax-transactions/:taxTransactionId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/finalize" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TaxTransactionsTaxTransactionIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TaxTransactionsTaxTransactionIdFinalizeExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_tax_transactions_taxTransactionId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/tax-transactions/:taxTransactionId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/finalize" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TaxTransactionsTaxTransactionIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TaxTransactionsTaxTransactionIdFinalizeExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_tax_transactions_taxTransactionId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/tax-transactions/:taxTransactionId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/finalize" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TaxTransactionsTaxTransactionIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TaxTransactionsTaxTransactionIdFinalizeExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_tax_transactions_taxTransactionId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/tax-transactions/:taxTransactionId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/finalize" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TaxTransactionsTaxTransactionIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TaxTransactionsTaxTransactionIdFinalizeExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_tax_transactions_taxTransactionId_finalize_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/tax-transactions/:taxTransactionId/finalize
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/finalize" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TaxTransactionsTaxTransactionIdFinalizeExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TaxTransactionsTaxTransactionIdFinalizeExact({
              client: vortex,
            });
  /v1/tax-transactions/{taxTransactionId}/reverse:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_tax_transactions_taxTransactionId_reverse_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/tax-transactions/:taxTransactionId/reverse
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/reverse" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TaxTransactionsTaxTransactionIdReverseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TaxTransactionsTaxTransactionIdReverseExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_tax_transactions_taxTransactionId_reverse_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/tax-transactions/:taxTransactionId/reverse
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/reverse" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TaxTransactionsTaxTransactionIdReverseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TaxTransactionsTaxTransactionIdReverseExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_tax_transactions_taxTransactionId_reverse_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/tax-transactions/:taxTransactionId/reverse
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/reverse" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TaxTransactionsTaxTransactionIdReverseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TaxTransactionsTaxTransactionIdReverseExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_tax_transactions_taxTransactionId_reverse_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/tax-transactions/:taxTransactionId/reverse
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/reverse" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TaxTransactionsTaxTransactionIdReverseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TaxTransactionsTaxTransactionIdReverseExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_tax_transactions_taxTransactionId_reverse_exact
      parameters:
        - explode: false
          in: path
          name: taxTransactionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/tax-transactions/:taxTransactionId/reverse
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/tax-transactions/:taxTransactionId/reverse" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TaxTransactionsTaxTransactionIdReverseExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TaxTransactionsTaxTransactionIdReverseExact({
              client: vortex,
            });
  /v1/terminal-bootstraps:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_terminal_bootstraps_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/terminal-bootstraps
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-bootstraps" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TerminalBootstrapsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TerminalBootstrapsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_terminal_bootstraps_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/terminal-bootstraps
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-bootstraps" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TerminalBootstrapsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TerminalBootstrapsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_terminal_bootstraps_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/terminal-bootstraps
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-bootstraps" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TerminalBootstrapsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TerminalBootstrapsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_terminal_bootstraps_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/terminal-bootstraps
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-bootstraps" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TerminalBootstrapsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TerminalBootstrapsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_terminal_bootstraps_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/terminal-bootstraps
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-bootstraps" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TerminalBootstrapsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TerminalBootstrapsExact({
              client: vortex,
            });
  /v1/terminal-connection-sessions:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_terminal_connection_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/terminal-connection-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-connection-sessions" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TerminalConnectionSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TerminalConnectionSessionsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_terminal_connection_sessions_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/terminal-connection-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-connection-sessions" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TerminalConnectionSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TerminalConnectionSessionsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_terminal_connection_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/terminal-connection-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-connection-sessions" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TerminalConnectionSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TerminalConnectionSessionsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_terminal_connection_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/terminal-connection-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-connection-sessions" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TerminalConnectionSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TerminalConnectionSessionsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_terminal_connection_sessions_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/terminal-connection-sessions
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-connection-sessions" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TerminalConnectionSessionsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TerminalConnectionSessionsExact({
              client: vortex,
            });
  /v1/terminal-locations:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_terminal_locations_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/terminal-locations
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-locations" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TerminalLocationsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TerminalLocationsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_terminal_locations_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/terminal-locations
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-locations" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TerminalLocationsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TerminalLocationsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_terminal_locations_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/terminal-locations
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-locations" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TerminalLocationsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TerminalLocationsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_terminal_locations_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/terminal-locations
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-locations" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TerminalLocationsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TerminalLocationsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_terminal_locations_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/terminal-locations
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-locations" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TerminalLocationsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TerminalLocationsExact({
              client: vortex,
            });
  /v1/terminal-readers:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_terminal_readers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/terminal-readers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TerminalReadersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TerminalReadersExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_terminal_readers_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/terminal-readers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TerminalReadersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TerminalReadersExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_terminal_readers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/terminal-readers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TerminalReadersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TerminalReadersExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_terminal_readers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/terminal-readers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TerminalReadersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TerminalReadersExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_terminal_readers_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/terminal-readers
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TerminalReadersExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TerminalReadersExact({
              client: vortex,
            });
  /v1/terminal-readers/{terminalReaderId}/decommission:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_terminal_readers_terminalReaderId_decommission_exact
      parameters:
        - explode: false
          in: path
          name: terminalReaderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/terminal-readers/:terminalReaderId/decommission
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers/:terminalReaderId/decommission" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TerminalReadersTerminalReaderIdDecommissionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TerminalReadersTerminalReaderIdDecommissionExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_terminal_readers_terminalReaderId_decommission_exact
      parameters:
        - explode: false
          in: path
          name: terminalReaderId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/terminal-readers/:terminalReaderId/decommission
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers/:terminalReaderId/decommission" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TerminalReadersTerminalReaderIdDecommissionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TerminalReadersTerminalReaderIdDecommissionExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_terminal_readers_terminalReaderId_decommission_exact
      parameters:
        - explode: false
          in: path
          name: terminalReaderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/terminal-readers/:terminalReaderId/decommission
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers/:terminalReaderId/decommission" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TerminalReadersTerminalReaderIdDecommissionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TerminalReadersTerminalReaderIdDecommissionExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_terminal_readers_terminalReaderId_decommission_exact
      parameters:
        - explode: false
          in: path
          name: terminalReaderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/terminal-readers/:terminalReaderId/decommission
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers/:terminalReaderId/decommission" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TerminalReadersTerminalReaderIdDecommissionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TerminalReadersTerminalReaderIdDecommissionExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_terminal_readers_terminalReaderId_decommission_exact
      parameters:
        - explode: false
          in: path
          name: terminalReaderId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/terminal-readers/:terminalReaderId/decommission
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/terminal-readers/:terminalReaderId/decommission" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TerminalReadersTerminalReaderIdDecommissionExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TerminalReadersTerminalReaderIdDecommissionExact({
              client: vortex,
            });
  /v1/test-clocks:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_test_clocks_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/test-clocks
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TestClocksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TestClocksExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_test_clocks_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/test-clocks
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TestClocksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TestClocksExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_test_clocks_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/test-clocks
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TestClocksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TestClocksExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_test_clocks_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/test-clocks
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TestClocksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TestClocksExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_test_clocks_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/test-clocks
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TestClocksExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TestClocksExact({
              client: vortex,
            });
  /v1/test-clocks/{testClockId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_test_clocks_testClockId_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/test-clocks/:testClockId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TestClocksTestClockIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TestClocksTestClockIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_test_clocks_testClockId_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/test-clocks/:testClockId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TestClocksTestClockIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TestClocksTestClockIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_test_clocks_testClockId_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/test-clocks/:testClockId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TestClocksTestClockIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TestClocksTestClockIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_test_clocks_testClockId_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/test-clocks/:testClockId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TestClocksTestClockIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TestClocksTestClockIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_test_clocks_testClockId_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/test-clocks/:testClockId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TestClocksTestClockIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TestClocksTestClockIdExact({
              client: vortex,
            });
  /v1/test-clocks/{testClockId}/advance:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_test_clocks_testClockId_advance_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/test-clocks/:testClockId/advance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId/advance" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1TestClocksTestClockIdAdvanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1TestClocksTestClockIdAdvanceExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_test_clocks_testClockId_advance_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/test-clocks/:testClockId/advance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId/advance" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1TestClocksTestClockIdAdvanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1TestClocksTestClockIdAdvanceExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_test_clocks_testClockId_advance_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/test-clocks/:testClockId/advance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId/advance" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1TestClocksTestClockIdAdvanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1TestClocksTestClockIdAdvanceExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_test_clocks_testClockId_advance_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/test-clocks/:testClockId/advance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId/advance" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1TestClocksTestClockIdAdvanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1TestClocksTestClockIdAdvanceExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_test_clocks_testClockId_advance_exact
      parameters:
        - explode: false
          in: path
          name: testClockId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/test-clocks/:testClockId/advance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/test-clocks/:testClockId/advance" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1TestClocksTestClockIdAdvanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1TestClocksTestClockIdAdvanceExact({
              client: vortex,
            });
  /v1/usage-events:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_usage_events_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/usage-events
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/usage-events" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1UsageEventsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1UsageEventsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_usage_events_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/usage-events
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/usage-events" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1UsageEventsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1UsageEventsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_usage_events_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/usage-events
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/usage-events" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1UsageEventsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1UsageEventsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_usage_events_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/usage-events
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/usage-events" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1UsageEventsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1UsageEventsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_usage_events_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/usage-events
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/usage-events" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1UsageEventsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1UsageEventsExact({
              client: vortex,
            });
  /v1/wallet-recurring-rules/{ruleId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_wallet_recurring_rules_ruleId_exact
      parameters:
        - explode: false
          in: path
          name: ruleId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/wallet-recurring-rules/:ruleId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallet-recurring-rules/:ruleId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WalletRecurringRulesRuleIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WalletRecurringRulesRuleIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_wallet_recurring_rules_ruleId_exact
      parameters:
        - explode: false
          in: path
          name: ruleId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/wallet-recurring-rules/:ruleId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallet-recurring-rules/:ruleId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WalletRecurringRulesRuleIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WalletRecurringRulesRuleIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_wallet_recurring_rules_ruleId_exact
      parameters:
        - explode: false
          in: path
          name: ruleId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/wallet-recurring-rules/:ruleId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallet-recurring-rules/:ruleId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WalletRecurringRulesRuleIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WalletRecurringRulesRuleIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_wallet_recurring_rules_ruleId_exact
      parameters:
        - explode: false
          in: path
          name: ruleId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/wallet-recurring-rules/:ruleId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallet-recurring-rules/:ruleId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WalletRecurringRulesRuleIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WalletRecurringRulesRuleIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_wallet_recurring_rules_ruleId_exact
      parameters:
        - explode: false
          in: path
          name: ruleId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/wallet-recurring-rules/:ruleId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallet-recurring-rules/:ruleId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WalletRecurringRulesRuleIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WalletRecurringRulesRuleIdExact({
              client: vortex,
            });
  /v1/wallets:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_wallets_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WalletsExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_wallets_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WalletsExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_wallets_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WalletsExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_wallets_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WalletsExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_wallets_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/wallets
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WalletsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WalletsExact({
              client: vortex,
            });
  /v1/wallets/{walletId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_wallets_walletId_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/wallets/:walletId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WalletsWalletIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WalletsWalletIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_wallets_walletId_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/wallets/:walletId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WalletsWalletIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WalletsWalletIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_wallets_walletId_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/wallets/:walletId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WalletsWalletIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WalletsWalletIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_wallets_walletId_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/wallets/:walletId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WalletsWalletIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WalletsWalletIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_wallets_walletId_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/wallets/:walletId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WalletsWalletIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WalletsWalletIdExact({
              client: vortex,
            });
  /v1/wallets/{walletId}/balance:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_wallets_walletId_balance_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/wallets/:walletId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/balance" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WalletsWalletIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WalletsWalletIdBalanceExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_wallets_walletId_balance_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/wallets/:walletId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/balance" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WalletsWalletIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WalletsWalletIdBalanceExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_wallets_walletId_balance_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/wallets/:walletId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/balance" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WalletsWalletIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WalletsWalletIdBalanceExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_wallets_walletId_balance_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/wallets/:walletId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/balance" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WalletsWalletIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WalletsWalletIdBalanceExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_wallets_walletId_balance_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/wallets/:walletId/balance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/balance" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WalletsWalletIdBalanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WalletsWalletIdBalanceExact({
              client: vortex,
            });
  /v1/wallets/{walletId}/recurring-rules:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_wallets_walletId_recurring_rules_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/wallets/:walletId/recurring-rules
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/recurring-rules" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WalletsWalletIdRecurringRulesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WalletsWalletIdRecurringRulesExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_wallets_walletId_recurring_rules_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/wallets/:walletId/recurring-rules
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/recurring-rules" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WalletsWalletIdRecurringRulesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WalletsWalletIdRecurringRulesExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_wallets_walletId_recurring_rules_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/wallets/:walletId/recurring-rules
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/recurring-rules" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WalletsWalletIdRecurringRulesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WalletsWalletIdRecurringRulesExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_wallets_walletId_recurring_rules_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/wallets/:walletId/recurring-rules
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/recurring-rules" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WalletsWalletIdRecurringRulesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WalletsWalletIdRecurringRulesExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_wallets_walletId_recurring_rules_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/wallets/:walletId/recurring-rules
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/recurring-rules" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WalletsWalletIdRecurringRulesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WalletsWalletIdRecurringRulesExact({
              client: vortex,
            });
  /v1/wallets/{walletId}/top-up:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_wallets_walletId_top_up_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/wallets/:walletId/top-up
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/top-up" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WalletsWalletIdTopUpExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WalletsWalletIdTopUpExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_wallets_walletId_top_up_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/wallets/:walletId/top-up
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/top-up" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WalletsWalletIdTopUpExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WalletsWalletIdTopUpExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_wallets_walletId_top_up_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/wallets/:walletId/top-up
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/top-up" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WalletsWalletIdTopUpExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WalletsWalletIdTopUpExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_wallets_walletId_top_up_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/wallets/:walletId/top-up
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/top-up" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WalletsWalletIdTopUpExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WalletsWalletIdTopUpExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_wallets_walletId_top_up_exact
      parameters:
        - explode: false
          in: path
          name: walletId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/wallets/:walletId/top-up
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/:walletId/top-up" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WalletsWalletIdTopUpExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WalletsWalletIdTopUpExact({
              client: vortex,
            });
  /v1/wallets/maintenance:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_wallets_maintenance_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/wallets/maintenance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/maintenance" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WalletsMaintenanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WalletsMaintenanceExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_wallets_maintenance_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/wallets/maintenance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/maintenance" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WalletsMaintenanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WalletsMaintenanceExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_wallets_maintenance_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/wallets/maintenance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/maintenance" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WalletsMaintenanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WalletsMaintenanceExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_wallets_maintenance_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/wallets/maintenance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/maintenance" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WalletsMaintenanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WalletsMaintenanceExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_wallets_maintenance_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/wallets/maintenance
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/wallets/maintenance" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WalletsMaintenanceExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WalletsMaintenanceExact({
              client: vortex,
            });
  /v1/webhook-deliveries:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_webhook_deliveries_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/webhook-deliveries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WebhookDeliveriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WebhookDeliveriesExact({
              client: vortex,
            });
    get:
      operationId: listWebhookDeliveries
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookDeliveryListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List outbound webhook deliveries
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listWebhookDeliveries, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listWebhookDeliveries({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_webhook_deliveries_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/webhook-deliveries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WebhookDeliveriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WebhookDeliveriesExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_webhook_deliveries_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/webhook-deliveries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WebhookDeliveriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WebhookDeliveriesExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_webhook_deliveries_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/webhook-deliveries
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WebhookDeliveriesExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WebhookDeliveriesExact({
              client: vortex,
            });
  /v1/webhook-deliveries/{webhookDeliveryId}:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_webhook_deliveries_webhookDeliveryId_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/webhook-deliveries/:webhookDeliveryId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WebhookDeliveriesWebhookDeliveryIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WebhookDeliveriesWebhookDeliveryIdExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_webhook_deliveries_webhookDeliveryId_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/webhook-deliveries/:webhookDeliveryId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WebhookDeliveriesWebhookDeliveryIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WebhookDeliveriesWebhookDeliveryIdExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_webhook_deliveries_webhookDeliveryId_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/webhook-deliveries/:webhookDeliveryId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WebhookDeliveriesWebhookDeliveryIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WebhookDeliveriesWebhookDeliveryIdExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_webhook_deliveries_webhookDeliveryId_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/webhook-deliveries/:webhookDeliveryId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WebhookDeliveriesWebhookDeliveryIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WebhookDeliveriesWebhookDeliveryIdExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_webhook_deliveries_webhookDeliveryId_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/webhook-deliveries/:webhookDeliveryId
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WebhookDeliveriesWebhookDeliveryIdExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WebhookDeliveriesWebhookDeliveryIdExact({
              client: vortex,
            });
  /v1/webhook-deliveries/{webhookDeliveryId}/replay:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_webhook_deliveries_webhookDeliveryId_replay_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/webhook-deliveries/:webhookDeliveryId/replay
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId/replay" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WebhookDeliveriesWebhookDeliveryIdReplayExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WebhookDeliveriesWebhookDeliveryIdReplayExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_webhook_deliveries_webhookDeliveryId_replay_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/webhook-deliveries/:webhookDeliveryId/replay
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId/replay" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WebhookDeliveriesWebhookDeliveryIdReplayExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WebhookDeliveriesWebhookDeliveryIdReplayExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_webhook_deliveries_webhookDeliveryId_replay_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/webhook-deliveries/:webhookDeliveryId/replay
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId/replay" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WebhookDeliveriesWebhookDeliveryIdReplayExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WebhookDeliveriesWebhookDeliveryIdReplayExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_webhook_deliveries_webhookDeliveryId_replay_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: POST /v1/webhook-deliveries/:webhookDeliveryId/replay
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId/replay" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1WebhookDeliveriesWebhookDeliveryIdReplayExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1WebhookDeliveriesWebhookDeliveryIdReplayExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_webhook_deliveries_webhookDeliveryId_replay_exact
      parameters:
        - explode: false
          in: path
          name: webhookDeliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/webhook-deliveries/:webhookDeliveryId/replay
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/:webhookDeliveryId/replay" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WebhookDeliveriesWebhookDeliveryIdReplayExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WebhookDeliveriesWebhookDeliveryIdReplayExact({
              client: vortex,
            });
  /v1/webhook-deliveries/dispatch:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_webhook_deliveries_dispatch_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/webhook-deliveries/dispatch
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/dispatch" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WebhookDeliveriesDispatchExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WebhookDeliveriesDispatchExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_webhook_deliveries_dispatch_exact
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: GET /v1/webhook-deliveries/dispatch
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/dispatch" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1WebhookDeliveriesDispatchExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1WebhookDeliveriesDispatchExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_webhook_deliveries_dispatch_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/webhook-deliveries/dispatch
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/dispatch" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WebhookDeliveriesDispatchExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WebhookDeliveriesDispatchExact({
              client: vortex,
            });
    post:
      operationId: dispatchWebhookDeliveries
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  limit: 10
            schema:
              $ref: "#/components/schemas/WebhookDeliveriesDispatchRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookDeliveryDispatchResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Dispatch pending outbound webhook deliveries
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/dispatch" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"limit":10}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { dispatchWebhookDeliveries, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await dispatchWebhookDeliveries({
              client: vortex,
              body: {"limit":10},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_webhook_deliveries_dispatch_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/webhook-deliveries/dispatch
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/dispatch" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WebhookDeliveriesDispatchExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WebhookDeliveriesDispatchExact({
              client: vortex,
            });
  /v1/webhook-endpoints:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_webhook_endpoints_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: DELETE /v1/webhook-endpoints
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1WebhookEndpointsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1WebhookEndpointsExact({
              client: vortex,
            });
    get:
      operationId: listWebhookEndpoints
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List outbound webhook endpoints
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listWebhookEndpoints, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listWebhookEndpoints({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_webhook_endpoints_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PATCH /v1/webhook-endpoints
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1WebhookEndpointsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1WebhookEndpointsExact({
              client: vortex,
            });
    post:
      operationId: createWebhookEndpoint
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  url: https://app.example.com/vortex/webhooks
                  eventTypes:
                    - invoice.paid
                  description: Production consumer app webhook endpoint
            schema:
              $ref: "#/components/schemas/WebhookEndpointCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create an outbound webhook endpoint
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"url":"https://app.example.com/vortex/webhooks","eventTypes":["invoice.paid"],"description":"Production consumer app webhook endpoint"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createWebhookEndpoint, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createWebhookEndpoint({
              client: vortex,
              body: {"url":"https://app.example.com/vortex/webhooks","eventTypes":["invoice.paid"],"description":"Production consumer app webhook endpoint"},
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_webhook_endpoints_exact
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: PUT /v1/webhook-endpoints
      tags:
        - public
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1WebhookEndpointsExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1WebhookEndpointsExact({
              client: vortex,
            });
  /v1/signup:
    delete:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: delete_v1_signup_exact
      parameters: []
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Created
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - session-cookie: []
      summary: DELETE /v1/signup
      tags:
        - session
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/signup" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteV1SignupExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteV1SignupExact({
              client: vortex,
            });
    get:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: get_v1_signup_exact
      parameters: []
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Created
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - session-cookie: []
      summary: GET /v1/signup
      tags:
        - session
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/signup" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getV1SignupExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getV1SignupExact({
              client: vortex,
            });
    patch:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: patch_v1_signup_exact
      parameters: []
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Created
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - session-cookie: []
      summary: PATCH /v1/signup
      tags:
        - session
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/signup" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { patchV1SignupExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await patchV1SignupExact({
              client: vortex,
            });
    post:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: post_v1_signup_exact
      parameters: []
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Created
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - session-cookie: []
      summary: POST /v1/signup
      tags:
        - session
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/signup" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { postV1SignupExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await postV1SignupExact({
              client: vortex,
            });
    put:
      description:
        "Generated route-discovery operation. Promoted operations in this\
        \ spec carry exact request and response schemas; use this route entry for\
        \ auth, path, idempotency, and error semantics until the route is promoted."
      operationId: put_v1_signup_exact
      parameters: []
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiSuccess"
          description: Created
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - session-cookie: []
      summary: PUT /v1/signup
      tags:
        - session
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/signup" \
              -X PUT \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { putV1SignupExact, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await putV1SignupExact({
              client: vortex,
            });
  /v1/recurring-payables:
    post:
      operationId: createRecurringPayable
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  sourceType: document_payment_field
                  sourceId: field_1
                  documentId: doc_1
                  paymentFieldId: field_1
                  customerExternalId: vtx_cust_seal_org_1
                  billingAccountId: bacc_1
                  currency: USD
                  lineItems: []
                  taxMode: not_taxable
                  collectionIntent: manual
                  feePolicy: {}
                  cadence:
                    interval: month
                    intervalCount: 1
                  endPolicy:
                    mode: never
                  startAt: 2026-08-01T00:00:00Z
                  metadata: {}
            schema:
              $ref: "#/components/schemas/CreateRecurringPayableRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayableResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a recurring document payable
      tags:
        - billing
        - payables
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/recurring-payables" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","currency":"USD","lineItems":[],"taxMode":"not_taxable","collectionIntent":"manual","feePolicy":{},"cadence":{"interval":"month","intervalCount":1},"endPolicy":{"mode":"never"},"startAt":"2026-08-01T00:00:00Z","metadata":{}}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createRecurringPayable, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createRecurringPayable({
              client: vortex,
              body: {"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","currency":"USD","lineItems":[],"taxMode":"not_taxable","collectionIntent":"manual","feePolicy":{},"cadence":{"interval":"month","intervalCount":1},"endPolicy":{"mode":"never"},"startAt":"2026-08-01T00:00:00Z","metadata":{}},
            });
  /v1/installment-payables:
    post:
      operationId: createInstallmentPayable
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  sourceType: document_payment_field
                  sourceId: field_1
                  documentId: doc_1
                  paymentFieldId: field_1
                  customerExternalId: vtx_cust_seal_org_1
                  billingAccountId: bacc_1
                  currency: USD
                  taxMode: not_taxable
                  collectionIntent: manual
                  feePolicy: {}
                  installments: []
                  metadata: {}
            schema:
              $ref: "#/components/schemas/CreateInstallmentPayableRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayableResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create an installment document payable
      tags:
        - billing
        - payables
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/installment-payables" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","currency":"USD","taxMode":"not_taxable","collectionIntent":"manual","feePolicy":{},"installments":[],"metadata":{}}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createInstallmentPayable, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createInstallmentPayable({
              client: vortex,
              body: {"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","currency":"USD","taxMode":"not_taxable","collectionIntent":"manual","feePolicy":{},"installments":[],"metadata":{}},
            });
  /v1/deposit-balance-payables:
    post:
      operationId: createDepositBalancePayable
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  sourceType: document_payment_field
                  sourceId: field_1
                  documentId: doc_1
                  paymentFieldId: field_1
                  customerExternalId: vtx_cust_seal_org_1
                  billingAccountId: bacc_1
                  currency: USD
                  taxMode: not_taxable
                  collectionIntent: manual
                  feePolicy: {}
                  deposit:
                    dueAt: 2026-08-01T00:00:00Z
                    amountDue: 5000
                    lineItems: []
                  balance:
                    dueAt: 2026-09-01T00:00:00Z
                    amountDue: 5000
                    lineItems: []
                  metadata: {}
            schema:
              $ref: "#/components/schemas/CreateDepositBalancePayableRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayableResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a deposit + balance document payable
      tags:
        - billing
        - payables
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/deposit-balance-payables" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","currency":"USD","taxMode":"not_taxable","collectionIntent":"manual","feePolicy":{},"deposit":{"dueAt":"2026-08-01T00:00:00Z","amountDue":5000,"lineItems":[]},"balance":{"dueAt":"2026-09-01T00:00:00Z","amountDue":5000,"lineItems":[]},"metadata":{}}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createDepositBalancePayable, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createDepositBalancePayable({
              client: vortex,
              body: {"sourceType":"document_payment_field","sourceId":"field_1","documentId":"doc_1","paymentFieldId":"field_1","customerExternalId":"vtx_cust_seal_org_1","billingAccountId":"bacc_1","currency":"USD","taxMode":"not_taxable","collectionIntent":"manual","feePolicy":{},"deposit":{"dueAt":"2026-08-01T00:00:00Z","amountDue":5000,"lineItems":[]},"balance":{"dueAt":"2026-09-01T00:00:00Z","amountDue":5000,"lineItems":[]},"metadata":{}},
            });
  /v1/subscriptions/{subscriptionId}/change/preview:
    post:
      operationId: previewSubscriptionItemChange
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  effectiveAt: 2026-06-01T00:00:00.000Z
                  items:
                    - priceId: price_pro_monthly
                      quantity: 1
                  requestedByRef: internal-integration
            schema:
              $ref: "#/components/schemas/SubscriptionItemChangePreviewRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionItemChangePreviewResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Preview a subscription item change
      tags:
        - billing
        - subscriptions
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/{subscriptionId}/change/preview" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"effectiveAt":"2026-06-01T00:00:00.000Z","items":[{"priceId":"price_pro_monthly","quantity":1}],"requestedByRef":"internal-integration"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { previewSubscriptionItemChange, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await previewSubscriptionItemChange({
              client: vortex,
              path: { subscriptionId: "..." },
              body: {"effectiveAt":"2026-06-01T00:00:00.000Z","items":[{"priceId":"price_pro_monthly","quantity":1}],"requestedByRef":"internal-integration"},
            });
  /v1/subscriptions/{subscriptionId}/change:
    post:
      operationId: changeSubscription
      parameters:
        - explode: false
          in: path
          name: subscriptionId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  action: update_items
                  effectiveAt: 2026-06-01T00:00:00.000Z
                  items:
                    - priceId: price_pro_monthly
                      quantity: 1
                  requestedByRef: internal-integration
            schema:
              $ref: "#/components/schemas/SubscriptionChangeRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionChangeResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Change subscription status or items
      tags:
        - billing
        - subscriptions
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/subscriptions/{subscriptionId}/change" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"action":"update_items","effectiveAt":"2026-06-01T00:00:00.000Z","items":[{"priceId":"price_pro_monthly","quantity":1}],"requestedByRef":"internal-integration"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { changeSubscription, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await changeSubscription({
              client: vortex,
              path: { subscriptionId: "..." },
              body: {"action":"update_items","effectiveAt":"2026-06-01T00:00:00.000Z","items":[{"priceId":"price_pro_monthly","quantity":1}],"requestedByRef":"internal-integration"},
            });
  /v1/customers/{customerId}/payment-methods:
    get:
      operationId: listCustomerPaymentMethods
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPaymentMethodsResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List customer payment methods
      tags:
        - billing
        - payment-methods
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/payment-methods" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listCustomerPaymentMethods, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listCustomerPaymentMethods({
              client: vortex,
              path: { customerId: "..." },
            });
    post:
      operationId: attachCustomerPaymentMethod
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  paymentMethodSetupSessionId: pmset_123
                  setupToken: tok_123
                  setAsDefault: true
            schema:
              $ref: "#/components/schemas/CustomerPaymentMethodAttachRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPaymentMethodResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Attach a customer payment method from a setup session
      tags:
        - billing
        - payment-methods
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/payment-methods" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"paymentMethodSetupSessionId":"pmset_123","setupToken":"tok_123","setAsDefault":true}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { attachCustomerPaymentMethod, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await attachCustomerPaymentMethod({
              client: vortex,
              path: { customerId: "..." },
              body: {"paymentMethodSetupSessionId":"pmset_123","setupToken":"tok_123","setAsDefault":true},
            });
  /v1/customers/{customerId}/payment-method-setup:
    post:
      operationId: createCustomerPaymentMethodSetup
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  methodType: card
                  setAsDefault: true
            schema:
              $ref: "#/components/schemas/CustomerPaymentMethodSetupRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPaymentMethodSetupResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a customer payment method setup session
      tags:
        - billing
        - payment-methods
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/payment-method-setup" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"methodType":"card","setAsDefault":true}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createCustomerPaymentMethodSetup, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createCustomerPaymentMethodSetup({
              client: vortex,
              path: { customerId: "..." },
              body: {"methodType":"card","setAsDefault":true},
            });
  /v1/customers/{customerId}/default-payment-method:
    patch:
      operationId: setCustomerDefaultPaymentMethod
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  defaultPaymentMethodId: pm_123
            schema:
              $ref: "#/components/schemas/CustomerDefaultPaymentMethodRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPaymentMethodResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Set a customer default payment method
      tags:
        - billing
        - payment-methods
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/default-payment-method" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"defaultPaymentMethodId":"pm_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { setCustomerDefaultPaymentMethod, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await setCustomerDefaultPaymentMethod({
              client: vortex,
              path: { customerId: "..." },
              body: {"defaultPaymentMethodId":"pm_123"},
            });
  /v1/customers/{customerId}/payment-methods/{paymentMethodId}/archive:
    post:
      operationId: archiveCustomerPaymentMethod
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: false
          in: path
          name: paymentMethodId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  reason: customer_requested
            schema:
              $ref: "#/components/schemas/CustomerPaymentMethodArchiveRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPaymentMethodResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Archive a customer payment method
      tags:
        - billing
        - payment-methods
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/payment-methods/{paymentMethodId}/archive" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"reason":"customer_requested"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { archiveCustomerPaymentMethod, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await archiveCustomerPaymentMethod({
              client: vortex,
              path: { customerId: "...", paymentMethodId: "..." },
              body: {"reason":"customer_requested"},
            });
  /v1/customers/{customerId}/payment-state:
    get:
      operationId: getCustomerPaymentState
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPaymentStateResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get customer payment readiness
      tags:
        - billing
        - payment-methods
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/payment-state" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCustomerPaymentState, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCustomerPaymentState({
              client: vortex,
              path: { customerId: "..." },
            });
  /v1/customers/{customerId}/state:
    get:
      operationId: getCustomerState
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerStateResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get product-facing customer billing and access state
      tags:
        - billing
        - entitlements
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/state" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCustomerState, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCustomerState({
              client: vortex,
              path: { customerId: "..." },
            });
  /v1/customers/{customerId}/access:
    get:
      operationId: checkCustomerAccess
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: key
          required: true
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerAccessResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Check product-facing customer access for one entitlement key
      tags:
        - billing
        - entitlements
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/access?key=..." \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { checkCustomerAccess, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await checkCustomerAccess({
              client: vortex,
              path: { customerId: "..." },
              query: { key: "..." },
            });
  /v1/merchant-accounts/{merchantAccountId}/settlements:
    get:
      operationId: getMerchantAccountSettlements
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - description:
            "Page size, 1-100. Omitting both limit and cursor returns the\
            \ full unpaginated list."
          explode: true
          in: query
          name: limit
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
          style: form
        - description:
            Opaque cursor from the previous page's nextCursor. Pass it back
            verbatim to fetch the next page.
          explode: true
          in: query
          name: cursor
          required: false
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantAccountSettlementsResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List merchant settlements
      tags:
        - billing
        - payments-readiness
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/settlements?environment=sandbox" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getMerchantAccountSettlements, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getMerchantAccountSettlements({
              client: vortex,
              path: { merchantAccountId: "..." },
              query: { environment: "sandbox" },
            });
  /v1/merchant-accounts/{merchantAccountId}/payouts:
    get:
      operationId: getMerchantAccountPayouts
      parameters:
        - explode: false
          in: path
          name: merchantAccountId
          required: true
          schema:
            type: string
          style: simple
        - explode: true
          in: query
          name: environment
          required: true
          schema:
            type: string
          style: form
        - description:
            "Page size, 1-100. Omitting both limit and cursor returns the\
            \ full unpaginated list."
          explode: true
          in: query
          name: limit
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
          style: form
        - description:
            Opaque cursor from the previous page's nextCursor. Pass it back
            verbatim to fetch the next page.
          explode: true
          in: query
          name: cursor
          required: false
          schema:
            type: string
          style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantAccountPayoutsResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List merchant payouts
      tags:
        - billing
        - payments-readiness
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/merchant-accounts/{merchantAccountId}/payouts?environment=sandbox" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getMerchantAccountPayouts, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getMerchantAccountPayouts({
              client: vortex,
              path: { merchantAccountId: "..." },
              query: { environment: "sandbox" },
            });
  /v1/webhook-event-types:
    get:
      operationId: listWebhookEventTypes
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEventTypesResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List outbound webhook event types
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-event-types" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listWebhookEventTypes, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listWebhookEventTypes({
              client: vortex,
            });
  /v1/webhook-endpoints/{endpointId}:
    delete:
      operationId: deleteWebhookEndpoint
      parameters:
        - explode: false
          in: path
          name: endpointId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value: {}
            schema:
              $ref: "#/components/schemas/WebhookEmptyRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Delete an outbound webhook endpoint
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints/{endpointId}" \
              -X DELETE \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { deleteWebhookEndpoint, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await deleteWebhookEndpoint({
              client: vortex,
              path: { endpointId: "..." },
              body: {},
            });
    patch:
      operationId: updateWebhookEndpoint
      parameters:
        - explode: false
          in: path
          name: endpointId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  eventTypes:
                    - invoice.paid
                    - invoice.payment_failed
                  status: active
            schema:
              $ref: "#/components/schemas/WebhookEndpointUpdateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Update an outbound webhook endpoint
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints/{endpointId}" \
              -X PATCH \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"eventTypes":["invoice.paid","invoice.payment_failed"],"status":"active"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { updateWebhookEndpoint, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await updateWebhookEndpoint({
              client: vortex,
              path: { endpointId: "..." },
              body: {"eventTypes":["invoice.paid","invoice.payment_failed"],"status":"active"},
            });
  /v1/webhook-endpoints/{endpointId}/test:
    post:
      operationId: testWebhookEndpoint
      parameters:
        - explode: false
          in: path
          name: endpointId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  eventType: invoice.paid
                  dispatch: true
            schema:
              $ref: "#/components/schemas/WebhookEndpointTestRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointTestResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create and optionally dispatch a test webhook delivery
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints/{endpointId}/test" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"eventType":"invoice.paid","dispatch":true}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { testWebhookEndpoint, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await testWebhookEndpoint({
              client: vortex,
              path: { endpointId: "..." },
              body: {"eventType":"invoice.paid","dispatch":true},
            });
  /v1/webhook-endpoints/{endpointId}/rotate-secret:
    post:
      operationId: rotateWebhookEndpointSecret
      parameters:
        - explode: false
          in: path
          name: endpointId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  gracePeriodSeconds: 3600
            schema:
              $ref: "#/components/schemas/WebhookEndpointSecretRotateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Rotate an outbound webhook signing secret
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-endpoints/{endpointId}/rotate-secret" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"gracePeriodSeconds":3600}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { rotateWebhookEndpointSecret, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await rotateWebhookEndpointSecret({
              client: vortex,
              path: { endpointId: "..." },
              body: {"gracePeriodSeconds":3600},
            });
  /v1/events:
    get:
      operationId: listWebhookEvents
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEventListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List outbound webhook events
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/events" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listWebhookEvents, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listWebhookEvents({
              client: vortex,
            });
  /v1/events/{eventId}:
    get:
      operationId: getWebhookEvent
      parameters:
        - explode: false
          in: path
          name: eventId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEventResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect an outbound webhook event
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/events/{eventId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getWebhookEvent, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getWebhookEvent({
              client: vortex,
              path: { eventId: "..." },
            });
  /v1/events/{eventId}/resend:
    post:
      operationId: resendWebhookEvent
      parameters:
        - explode: false
          in: path
          name: eventId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value: {}
            schema:
              $ref: "#/components/schemas/WebhookEventResendRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEventResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create a new delivery for an existing webhook event
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/events/{eventId}/resend" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { resendWebhookEvent, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await resendWebhookEvent({
              client: vortex,
              path: { eventId: "..." },
              body: {},
            });
  /v1/webhook-deliveries/{deliveryId}:
    get:
      operationId: getWebhookDelivery
      parameters:
        - explode: false
          in: path
          name: deliveryId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookDeliveryResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect an outbound webhook delivery
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/{deliveryId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getWebhookDelivery, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getWebhookDelivery({
              client: vortex,
              path: { deliveryId: "..." },
            });
  /v1/webhook-deliveries/{deliveryId}/replay:
    post:
      operationId: replayWebhookDelivery
      parameters:
        - explode: false
          in: path
          name: deliveryId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  replayedByRef: operator
                  dispatch: true
            schema:
              $ref: "#/components/schemas/WebhookDeliveryReplayRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookDeliveryResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Replay a failed outbound webhook delivery
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/{deliveryId}/replay" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"replayedByRef":"operator","dispatch":true}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { replayWebhookDelivery, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await replayWebhookDelivery({
              client: vortex,
              path: { deliveryId: "..." },
              body: {"replayedByRef":"operator","dispatch":true},
            });
  /v1/webhook-deliveries/redrive:
    post:
      operationId: redriveWebhookDeliveries
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  failureKind: client_error
                  limit: 10
                  replayedByRef: operator
                  dispatch: true
            schema:
              $ref: "#/components/schemas/WebhookDeliveriesRedriveRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookDeliveryRedriveResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Redrive failed outbound webhook deliveries
      tags:
        - billing
        - webhooks
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/webhook-deliveries/redrive" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"failureKind":"client_error","limit":10,"replayedByRef":"operator","dispatch":true}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { redriveWebhookDeliveries, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await redriveWebhookDeliveries({
              client: vortex,
              body: {"failureKind":"client_error","limit":10,"replayedByRef":"operator","dispatch":true},
            });
  /v1/coupons/{couponId}/apply:
    post:
      operationId: applyCoupon
      parameters:
        - explode: false
          in: path
          name: couponId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  appliedCouponId: acpn_123
                  customerExternalId: cus_123
                  subscriptionExternalId: sub_123
                  appliedAt: 2026-06-01T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/ApplyCouponRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CouponApplicationResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: "Apply a coupon to a customer, account, or subscription"
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons/{couponId}/apply" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"appliedCouponId":"acpn_123","customerExternalId":"cus_123","subscriptionExternalId":"sub_123","appliedAt":"2026-06-01T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { applyCoupon, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await applyCoupon({
              client: vortex,
              path: { couponId: "..." },
              body: {"appliedCouponId":"acpn_123","customerExternalId":"cus_123","subscriptionExternalId":"sub_123","appliedAt":"2026-06-01T00:00:00.000Z"},
            });
  /v1/coupons/{couponId}/terminate:
    post:
      operationId: terminateCoupon
      parameters:
        - explode: false
          in: path
          name: couponId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  terminatedAt: 2026-06-15T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/TerminateCouponRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CouponResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Terminate a coupon
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/coupons/{couponId}/terminate" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"terminatedAt":"2026-06-15T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { terminateCoupon, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await terminateCoupon({
              client: vortex,
              path: { couponId: "..." },
              body: {"terminatedAt":"2026-06-15T00:00:00.000Z"},
            });
  /v1/applied-coupons:
    get:
      operationId: listAppliedCoupons
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppliedCouponListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List applied billing coupons
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/applied-coupons" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listAppliedCoupons, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listAppliedCoupons({
              client: vortex,
            });
  /v1/applied-coupons/{appliedCouponId}:
    get:
      operationId: getAppliedCoupon
      parameters:
        - explode: false
          in: path
          name: appliedCouponId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppliedCouponResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get one applied billing coupon
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/applied-coupons/{appliedCouponId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getAppliedCoupon, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getAppliedCoupon({
              client: vortex,
              path: { appliedCouponId: "..." },
            });
  /v1/applied-coupons/{appliedCouponId}/terminate:
    post:
      operationId: terminateAppliedCoupon
      parameters:
        - explode: false
          in: path
          name: appliedCouponId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  terminatedAt: 2026-06-15T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/TerminateAppliedCouponRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppliedCouponResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Terminate one applied coupon
      tags:
        - billing
        - discounts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/applied-coupons/{appliedCouponId}/terminate" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"terminatedAt":"2026-06-15T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { terminateAppliedCoupon, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await terminateAppliedCoupon({
              client: vortex,
              path: { appliedCouponId: "..." },
              body: {"terminatedAt":"2026-06-15T00:00:00.000Z"},
            });
  /v1/customers/{customerId}/balance/grant:
    post:
      operationId: grantCustomerBalance
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  entryId: cbal_123
                  currency: USD
                  amount: 2500
                  targets:
                    lineTypes:
                      - fixed
                    priceIds:
                      - price_123
                  description: Prepaid service credit
                  effectiveAt: 2026-06-01T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/GrantCustomerBalanceRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerBalanceEntryResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Grant prepaid customer balance
      tags:
        - billing
        - customer-balance
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/balance/grant" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"entryId":"cbal_123","currency":"USD","amount":2500,"targets":{"lineTypes":["fixed"],"priceIds":["price_123"]},"description":"Prepaid service credit","effectiveAt":"2026-06-01T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { grantCustomerBalance, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await grantCustomerBalance({
              client: vortex,
              path: { customerId: "..." },
              body: {"entryId":"cbal_123","currency":"USD","amount":2500,"targets":{"lineTypes":["fixed"],"priceIds":["price_123"]},"description":"Prepaid service credit","effectiveAt":"2026-06-01T00:00:00.000Z"},
            });
  /v1/customers/{customerId}/balance/grant-from-payment:
    post:
      operationId: grantCustomerBalanceFromPayment
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  entryId: cbal_payment_123
                  paymentId: pay_123
                  description: Wallet top-up
                  effectiveAt: 2026-06-01T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/GrantCustomerBalanceFromPaymentRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerBalanceEntryResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Grant prepaid customer balance from a captured payment
      tags:
        - billing
        - customer-balance
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/balance/grant-from-payment" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"entryId":"cbal_payment_123","paymentId":"pay_123","description":"Wallet top-up","effectiveAt":"2026-06-01T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { grantCustomerBalanceFromPayment, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await grantCustomerBalanceFromPayment({
              client: vortex,
              path: { customerId: "..." },
              body: {"entryId":"cbal_payment_123","paymentId":"pay_123","description":"Wallet top-up","effectiveAt":"2026-06-01T00:00:00.000Z"},
            });
  /v1/customers/{customerId}/balance/expire:
    post:
      operationId: expireCustomerBalance
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  entryId: cbal_expired_123
                  sourceEntryId: cbal_123
                  amount: 1000
                  description: Expired unused balance
                  effectiveAt: 2026-06-30T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/ExpireCustomerBalanceRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerBalanceEntryResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Expire prepaid customer balance
      tags:
        - billing
        - customer-balance
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/balance/expire" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"entryId":"cbal_expired_123","sourceEntryId":"cbal_123","amount":1000,"description":"Expired unused balance","effectiveAt":"2026-06-30T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { expireCustomerBalance, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await expireCustomerBalance({
              client: vortex,
              path: { customerId: "..." },
              body: {"entryId":"cbal_expired_123","sourceEntryId":"cbal_123","amount":1000,"description":"Expired unused balance","effectiveAt":"2026-06-30T00:00:00.000Z"},
            });
  /v1/customers/{customerId}/balance/reverse:
    post:
      operationId: reverseCustomerBalance
      parameters:
        - explode: false
          in: path
          name: customerId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  entryId: cbal_reversed_123
                  sourceEntryId: cbal_123
                  amount: 1000
                  description: Manual balance correction
                  effectiveAt: 2026-06-15T00:00:00.000Z
            schema:
              $ref: "#/components/schemas/ReverseCustomerBalanceRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerBalanceEntryResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Reverse a prepaid customer balance ledger entry
      tags:
        - billing
        - customer-balance
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/customers/{customerId}/balance/reverse" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"entryId":"cbal_reversed_123","sourceEntryId":"cbal_123","amount":1000,"description":"Manual balance correction","effectiveAt":"2026-06-15T00:00:00.000Z"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { reverseCustomerBalance, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await reverseCustomerBalance({
              client: vortex,
              path: { customerId: "..." },
              body: {"entryId":"cbal_reversed_123","sourceEntryId":"cbal_123","amount":1000,"description":"Manual balance correction","effectiveAt":"2026-06-15T00:00:00.000Z"},
            });
  /v1/invoices/preview:
    post:
      operationId: previewInvoice
      parameters: []
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  customerExternalId: cus_123
                  subscriptionExternalId: sub_123
            schema:
              $ref: "#/components/schemas/InvoiceCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoicePreviewResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Preview an invoice
      tags:
        - billing
        - invoices
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/preview" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"customerExternalId":"cus_123","subscriptionExternalId":"sub_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { previewInvoice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await previewInvoice({
              client: vortex,
              body: {"customerExternalId":"cus_123","subscriptionExternalId":"sub_123"},
            });
  /v1/invoices/{invoiceId}/refresh:
    post:
      operationId: refreshInvoice
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value: {}
            schema:
              $ref: "#/components/schemas/InvoiceRefreshRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvoiceResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Refresh a draft invoice
      tags:
        - billing
        - invoices
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/refresh" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { refreshInvoice, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await refreshInvoice({
              client: vortex,
              path: { invoiceId: "..." },
              body: {},
            });
  /v1/invoices/{invoiceId}/tax/quote:
    post:
      operationId: quoteInvoiceTax
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  customerTaxAddress:
                    line1: 897 Broadway
                    city: New York
                    region: NY
                    postalCode: "10003"
                    country: US
                  taxBehaviorPolicy:
                    behavior: exclusive
                  requestedByRef: internal-integration
            schema:
              $ref: "#/components/schemas/TaxQuoteRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OpenApiInvoiceTaxResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Quote tax for a draft invoice
      tags:
        - billing
        - tax
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/tax/quote" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"customerTaxAddress":{"line1":"897 Broadway","city":"New York","region":"NY","postalCode":"10003","country":"US"},"taxBehaviorPolicy":{"behavior":"exclusive"},"requestedByRef":"internal-integration"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { quoteInvoiceTax, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await quoteInvoiceTax({
              client: vortex,
              path: { invoiceId: "..." },
              body: {"customerTaxAddress":{"line1":"897 Broadway","city":"New York","region":"NY","postalCode":"10003","country":"US"},"taxBehaviorPolicy":{"behavior":"exclusive"},"requestedByRef":"internal-integration"},
            });
  /v1/invoices/{invoiceId}/payment-lifecycle:
    get:
      operationId: getInvoicePaymentLifecycle
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentLifecycleResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect invoice payment lifecycle
      tags:
        - billing
        - payments
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/payment-lifecycle" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getInvoicePaymentLifecycle, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getInvoicePaymentLifecycle({
              client: vortex,
              path: { invoiceId: "..." },
            });
  /v1/invoices/{invoiceId}/receipts:
    get:
      operationId: listInvoiceReceipts
      parameters:
        - explode: false
          in: path
          name: invoiceId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptListResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: List invoice payment receipts
      tags:
        - billing
        - receipts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/invoices/{invoiceId}/receipts" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { listInvoiceReceipts, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await listInvoiceReceipts({
              client: vortex,
              path: { invoiceId: "..." },
            });
  /v1/payment-receipts/{receiptId}:
    get:
      operationId: getPaymentReceipt
      parameters:
        - explode: false
          in: path
          name: receiptId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReceiptResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Get one payment receipt artifact
      tags:
        - billing
        - receipts
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-receipts/{receiptId}" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getPaymentReceipt, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getPaymentReceipt({
              client: vortex,
              path: { receiptId: "..." },
            });
  /v1/payment-requests/{paymentRequestId}/link:
    post:
      operationId: createPaymentRequestLink
      parameters:
        - explode: false
          in: path
          name: paymentRequestId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  expiresAt: 2026-06-08T00:00:00.000Z
                  createdByRef: dunning-recovery
            schema:
              $ref: "#/components/schemas/PaymentRequestLinkCreateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentRequestLinkResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Create an expiring hosted payment link for a payment request
      tags:
        - billing
        - payment-requests
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/payment-requests/{paymentRequestId}/link" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"expiresAt":"2026-06-08T00:00:00.000Z","createdByRef":"dunning-recovery"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { createPaymentRequestLink, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await createPaymentRequestLink({
              client: vortex,
              path: { paymentRequestId: "..." },
              body: {"expiresAt":"2026-06-08T00:00:00.000Z","createdByRef":"dunning-recovery"},
            });
  /v1/credit-notes/{creditNoteId}/refund-lineage:
    get:
      operationId: getCreditNoteRefundLineage
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNoteRefundLineageResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Inspect billing and payments refund lineage for a credit note
      tags:
        - billing
        - credit-notes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/{creditNoteId}/refund-lineage" \
              -X GET \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { getCreditNoteRefundLineage, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await getCreditNoteRefundLineage({
              client: vortex,
              path: { creditNoteId: "..." },
            });
  /v1/credit-notes/{creditNoteId}/reconcile-refund:
    post:
      operationId: reconcileCreditNoteRefund
      parameters:
        - explode: false
          in: path
          name: creditNoteId
          required: true
          schema:
            type: string
          style: simple
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        content:
          application/json:
            examples:
              default:
                value:
                  paymentsRefundId: refund_123
            schema:
              $ref: "#/components/schemas/CreditNoteRefundReconcileRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNoteRefundLineageResponse"
          description: Success
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationFailure"
        "429":
          $ref: "#/components/responses/RateLimited"
        default:
          $ref: "#/components/responses/ApiError"
      security:
        - api-key: []
      summary: Reconcile credit-note refund linkage after drift or provider replay
      tags:
        - billing
        - credit-notes
      x-codeSamples:
        - lang: Shell
          label: curl
          source: |-
            curl "https://api.vortex.nyc/v1/credit-notes/{creditNoteId}/reconcile-refund" \
              -X POST \
              -H "Authorization: Bearer $VORTEX_API_KEY" \
              -H "Content-Type: application/json" \
              -d '{"paymentsRefundId":"refund_123"}'
        - lang: TypeScript
          label: TypeScript
          source: |-
            import { reconcileCreditNoteRefund, createVortex } from "@vortex-api/payments-sdk/2026-08";

            const vortex = createVortex({ apiKey: process.env.VORTEX_API_KEY });
            const { data } = await reconcileCreditNoteRefund({
              client: vortex,
              path: { creditNoteId: "..." },
              body: {"paymentsRefundId":"refund_123"},
            });
components:
  parameters:
    IdempotencyKey:
      description:
        Required for production-safe retries on mutating routes. Reusing
        a key with a different request body returns 409.
      explode: false
      in: header
      name: Idempotency-Key
      required: false
      schema:
        minLength: 1
        type: string
      style: simple
  responses:
    BadRequest:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: "Bad request, including malformed JSON"
    Unauthorized:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: Missing or invalid API key
    Forbidden:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: Authenticated key cannot access this resource or environment
    NotFound:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: Route or scoped resource was not found
    Conflict:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: "Conflict, including idempotency body drift"
    ValidationFailure:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: Validation failed
    RateLimited:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: API key rate limit exceeded
    ApiError:
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
      description: Error
  schemas:
    MerchantProfileDate:
      additionalProperties: false
      example:
        day: 5.637376656633329
        month: 2.3021358869347655
        year: 7.061401241503109
      properties:
        day:
          type: number
        month:
          type: number
        year:
          type: number
      required:
        - day
        - month
        - year
    MerchantAddress:
      additionalProperties: false
      example:
        line1: line1
        line2: line2
        city: city
        region: region
        postalCode: postalCode
        country: country
      properties:
        line1:
          type: string
        line2:
          type: string
        city:
          type: string
        region:
          type: string
        postalCode:
          type: string
        country:
          type: string
      required:
        - city
        - country
        - line1
        - postalCode
    MerchantSettlementBankAccount:
      additionalProperties: false
      example:
        name: name
        accountNumber: accountNumber
        bankCode: bankCode
        accountType: accountType
        attemptValidationCheck: true
      properties:
        name:
          type: string
        accountNumber:
          type: string
        bankCode:
          type: string
        accountType:
          type: string
        attemptValidationCheck:
          type: boolean
      required:
        - accountNumber
        - accountType
        - bankCode
        - name
    MerchantAssociatedIdentity:
      additionalProperties: false
      example:
        identityRoles:
          - identityRoles
          - identityRoles
        relationType: relationType
        firstName: firstName
        lastName: lastName
        title: title
        email: email
        phone: phone
        taxId: taxId
        principalPercentageOwnership: 9.301444243932576
        dateOfBirth:
          day: 5.637376656633329
          month: 2.3021358869347655
          year: 7.061401241503109
        personalAddress:
          line1: line1
          line2: line2
          city: city
          region: region
          postalCode: postalCode
          country: country
      properties:
        identityRoles:
          items:
            type: string
          type: array
          default: null
        relationType:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        title:
          type: string
        email:
          type: string
        phone:
          type: string
        taxId:
          type: string
        principalPercentageOwnership:
          type: number
        dateOfBirth:
          $ref: "#/components/schemas/MerchantProfileDate"
        personalAddress:
          $ref: "#/components/schemas/MerchantAddress"
      required:
        - identityRoles
    MerchantUnderwritingData:
      additionalProperties: {}
      example:
        annualAchVolume: 3.616076749251911
        averageAchTransferAmount: 2.027123023002322
        averageCardTransferAmount: 4.145608029883936
        businessDescription: businessDescription
        creditCheckAllowed: true
        creditCheckIpAddress: creditCheckIpAddress
        creditCheckTimestamp: creditCheckTimestamp
        creditCheckUserAgent: creditCheckUserAgent
        refundPolicy: refundPolicy
        cardVolumeDistribution:
          cardPresentPercentage: 7.386281948385884
          ecommercePercentage: 1.2315135367772556
          mailOrderTelephoneOrderPercentage: 1.0246457001441578
        volumeDistributionByBusinessType:
          businessToConsumerVolumePercentage: 1.4894159098541704
          consumerToConsumerVolumePercentage: 6.84685269835264
          otherVolumePercentage: 7.457744773683766
      properties:
        annualAchVolume:
          type: number
        averageAchTransferAmount:
          type: number
        averageCardTransferAmount:
          type: number
        businessDescription:
          type: string
        creditCheckAllowed:
          type: boolean
        creditCheckIpAddress:
          type: string
        creditCheckTimestamp:
          type: string
        creditCheckUserAgent:
          type: string
        refundPolicy:
          type: string
        cardVolumeDistribution:
          $ref: "#/components/schemas/MerchantUnderwritingData_cardVolumeDistribution"
        volumeDistributionByBusinessType:
          $ref: "#/components/schemas/MerchantUnderwritingData_volumeDistributionByBusinessType"
    Metadata:
      additionalProperties:
        type: string
    MerchantAccountCreateRequest:
      additionalProperties: false
      example:
        environment: sandbox
        externalMerchantRef: externalMerchantRef
        displayName: displayName
        legalEntityType: legalEntityType
        country: country
        merchantMode: merchantMode
        defaultCurrency: defaultCurrency
        tenantId: tenantId
        doingBusinessAs: doingBusinessAs
        businessPhone: businessPhone
        businessTaxId: businessTaxId
        phone: phone
        taxId: taxId
        email: email
        firstName: firstName
        lastName: lastName
        mcc: mcc
        url: url
        maxTransactionAmount: 0.8008281904610115
        achMaxTransactionAmount: 6.027456183070403
        annualCardVolume: 1.4658129805029452
        principalPercentageOwnership: 5.962133916683182
        hasAcceptedCreditCardsPreviously: true
        dateOfBirth:
          day: 5.637376656633329
          month: 2.3021358869347655
          year: 7.061401241503109
        incorporationDate:
          day: 5.637376656633329
          month: 2.3021358869347655
          year: 7.061401241503109
        businessAddress:
          line1: line1
          line2: line2
          city: city
          region: region
          postalCode: postalCode
          country: country
        personalAddress:
          line1: line1
          line2: line2
          city: city
          region: region
          postalCode: postalCode
          country: country
        settlementBankAccount:
          name: name
          accountNumber: accountNumber
          bankCode: bankCode
          accountType: accountType
          attemptValidationCheck: true
        associatedIdentities:
          - identityRoles:
              - identityRoles
              - identityRoles
            relationType: relationType
            firstName: firstName
            lastName: lastName
            title: title
            email: email
            phone: phone
            taxId: taxId
            principalPercentageOwnership: 9.301444243932576
            dateOfBirth:
              day: 5.637376656633329
              month: 2.3021358869347655
              year: 7.061401241503109
            personalAddress:
              line1: line1
              line2: line2
              city: city
              region: region
              postalCode: postalCode
              country: country
          - identityRoles:
              - identityRoles
              - identityRoles
            relationType: relationType
            firstName: firstName
            lastName: lastName
            title: title
            email: email
            phone: phone
            taxId: taxId
            principalPercentageOwnership: 9.301444243932576
            dateOfBirth:
              day: 5.637376656633329
              month: 2.3021358869347655
              year: 7.061401241503109
            personalAddress:
              line1: line1
              line2: line2
              city: city
              region: region
              postalCode: postalCode
              country: country
        underwriting:
          annualAchVolume: 3.616076749251911
          averageAchTransferAmount: 2.027123023002322
          averageCardTransferAmount: 4.145608029883936
          businessDescription: businessDescription
          creditCheckAllowed: true
          creditCheckIpAddress: creditCheckIpAddress
          creditCheckTimestamp: creditCheckTimestamp
          creditCheckUserAgent: creditCheckUserAgent
          refundPolicy: refundPolicy
          cardVolumeDistribution:
            cardPresentPercentage: 7.386281948385884
            ecommercePercentage: 1.2315135367772556
            mailOrderTelephoneOrderPercentage: 1.0246457001441578
          volumeDistributionByBusinessType:
            businessToConsumerVolumePercentage: 1.4894159098541704
            consumerToConsumerVolumePercentage: 6.84685269835264
            otherVolumePercentage: 7.457744773683766
        metadata:
          key: metadata
      properties:
        environment:
          enum:
            - sandbox
            - production
          type: string
        externalMerchantRef:
          description:
            Your stable reference for this merchant. Optional; generated
            when omitted.
          type: string
        displayName:
          type: string
        legalEntityType:
          type: string
        country:
          type: string
        merchantMode:
          type: string
        defaultCurrency:
          type: string
        tenantId:
          description:
            Must match the caller's organization when present; rejected
            otherwise.
          type: string
        doingBusinessAs:
          type: string
        businessPhone:
          type: string
        businessTaxId:
          type: string
        phone:
          type: string
        taxId:
          type: string
        email:
          type: string
        firstName:
          type: string
        lastName:
          type: string
        mcc:
          type: string
        url:
          type: string
        maxTransactionAmount:
          type: number
        achMaxTransactionAmount:
          type: number
        annualCardVolume:
          type: number
        principalPercentageOwnership:
          type: number
        hasAcceptedCreditCardsPreviously:
          type: boolean
        dateOfBirth:
          $ref: "#/components/schemas/MerchantProfileDate"
        incorporationDate:
          $ref: "#/components/schemas/MerchantProfileDate"
        businessAddress:
          $ref: "#/components/schemas/MerchantAddress"
        personalAddress:
          $ref: "#/components/schemas/MerchantAddress"
        settlementBankAccount:
          $ref: "#/components/schemas/MerchantSettlementBankAccount"
        associatedIdentities:
          items:
            $ref: "#/components/schemas/MerchantAssociatedIdentity"
          type: array
          default: null
        underwriting:
          $ref: "#/components/schemas/MerchantUnderwritingData"
        metadata:
          additionalProperties:
            type: string
      required:
        - country
        - defaultCurrency
        - displayName
        - environment
        - legalEntityType
        - merchantMode
    MerchantOnboardingLinkRequest:
      additionalProperties: false
      example:
        environment: sandbox
        createdByRef: createdByRef
      properties:
        environment:
          enum:
            - sandbox
            - production
          type: string
        createdByRef:
          type: string
      required:
        - createdByRef
        - environment
    FeePolicy:
      additionalProperties: true
      description:
        "Adopter fee policy (modeled, not settlement-executed). Structure\
        \ passed through to Vortex Payments."
      type: object
    PayableLineItem:
      additionalProperties: true
      type: object
    CreatePayableRequest:
      additionalProperties: false
      example:
        sourceType: document_payment_field
        sourceId: sourceId
        documentId: documentId
        paymentFieldId: paymentFieldId
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        collectionIntent: manual
        feePolicy:
          key: ""
        dueAt: 2000-01-23T04:56:07.000+00:00
        lineItems:
          - null
          - null
        taxable: true
        taxBehavior: inclusive
        taxClassificationKey: standard_taxable
        metadata:
          key: metadata
      properties:
        sourceType:
          enum:
            - document_payment_field
          type: string
        sourceId:
          type: string
        documentId:
          type: string
        paymentFieldId:
          type: string
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        collectionIntent:
          enum:
            - manual
          type: string
        feePolicy:
          additionalProperties: true
          description:
            "Adopter fee policy (modeled, not settlement-executed). Structure\
            \ passed through to Vortex Payments."
          type: object
        dueAt:
          format: date-time
          type: string
        lineItems:
          items:
            $ref: "#/components/schemas/PayableLineItem"
          type: array
          default: null
        taxable:
          type: boolean
        taxBehavior:
          enum:
            - inclusive
            - exclusive
          type: string
        taxClassificationKey:
          enum:
            - standard_taxable
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - billingAccountId
        - collectionIntent
        - customerExternalId
        - documentId
        - feePolicy
        - lineItems
        - metadata
        - paymentFieldId
        - sourceId
        - sourceType
    CreateRecurringPayableRequest:
      additionalProperties: {}
      example:
        sourceType: document_payment_field
        sourceId: sourceId
        documentId: documentId
        paymentFieldId: paymentFieldId
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        merchantAccountId: merchantAccountId
        currency: USD
        lineItems:
          - null
          - null
        taxMode: not_taxable
        taxable: true
        taxBehavior: inclusive
        taxClassificationKey: standard_taxable
        collectionIntent: manual
        feePolicy:
          key: ""
        cadence:
          interval: week
          intervalCount: 1
        endPolicy:
          key: ""
        startAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        sourceType:
          enum:
            - document_payment_field
          type: string
        sourceId:
          type: string
        documentId:
          type: string
        paymentFieldId:
          type: string
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        merchantAccountId:
          type: string
        currency:
          description:
            ISO 4217 alpha-3; only currencies processable on current rails
            are accepted
          example: USD
          type: string
        lineItems:
          items:
            $ref: "#/components/schemas/PayableLineItem"
          type: array
          default: null
        taxMode:
          enum:
            - not_taxable
            - taxable_requires_evidence
          type: string
        taxable:
          type: boolean
        taxBehavior:
          enum:
            - inclusive
            - exclusive
          type: string
        taxClassificationKey:
          enum:
            - standard_taxable
          type: string
        collectionIntent:
          enum:
            - manual
          type: string
        feePolicy:
          additionalProperties: true
          description:
            "Adopter fee policy (modeled, not settlement-executed). Structure\
            \ passed through to Vortex Payments."
          type: object
        cadence:
          $ref: "#/components/schemas/CreateRecurringPayableRequest_cadence"
        endPolicy:
          additionalProperties: true
          type: object
        startAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - billingAccountId
        - cadence
        - collectionIntent
        - currency
        - customerExternalId
        - documentId
        - endPolicy
        - feePolicy
        - lineItems
        - metadata
        - paymentFieldId
        - sourceId
        - sourceType
        - startAt
        - taxMode
    CreateInstallmentPayableRequest:
      additionalProperties: {}
      example:
        sourceType: document_payment_field
        sourceId: sourceId
        documentId: documentId
        paymentFieldId: paymentFieldId
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        merchantAccountId: merchantAccountId
        currency: USD
        taxMode: not_taxable
        taxable: true
        taxBehavior: inclusive
        taxClassificationKey: standard_taxable
        collectionIntent: manual
        feePolicy:
          key: ""
        installments:
          - key: ""
          - key: ""
        metadata:
          key: metadata
      properties:
        sourceType:
          enum:
            - document_payment_field
          type: string
        sourceId:
          type: string
        documentId:
          type: string
        paymentFieldId:
          type: string
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        merchantAccountId:
          type: string
        currency:
          description:
            ISO 4217 alpha-3; only currencies processable on current rails
            are accepted
          example: USD
          type: string
        taxMode:
          enum:
            - not_taxable
            - taxable_requires_evidence
          type: string
        taxable:
          type: boolean
        taxBehavior:
          enum:
            - inclusive
            - exclusive
          type: string
        taxClassificationKey:
          enum:
            - standard_taxable
          type: string
        collectionIntent:
          enum:
            - manual
          type: string
        feePolicy:
          additionalProperties: true
          description:
            "Adopter fee policy (modeled, not settlement-executed). Structure\
            \ passed through to Vortex Payments."
          type: object
        installments:
          items:
            additionalProperties: true
            type: object
          type: array
          default: null
        metadata:
          additionalProperties:
            type: string
      required:
        - billingAccountId
        - collectionIntent
        - currency
        - customerExternalId
        - documentId
        - feePolicy
        - installments
        - metadata
        - paymentFieldId
        - sourceId
        - sourceType
        - taxMode
    CreateDepositBalancePayableRequest:
      additionalProperties: {}
      example:
        sourceType: document_payment_field
        sourceId: sourceId
        documentId: documentId
        paymentFieldId: paymentFieldId
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        merchantAccountId: merchantAccountId
        currency: USD
        taxMode: not_taxable
        taxable: true
        taxBehavior: inclusive
        taxClassificationKey: standard_taxable
        collectionIntent: manual
        feePolicy:
          key: ""
        deposit:
          key: ""
        balance:
          key: ""
        metadata:
          key: metadata
      properties:
        sourceType:
          enum:
            - document_payment_field
          type: string
        sourceId:
          type: string
        documentId:
          type: string
        paymentFieldId:
          type: string
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        merchantAccountId:
          type: string
        currency:
          description:
            ISO 4217 alpha-3; only currencies processable on current rails
            are accepted
          example: USD
          type: string
        taxMode:
          enum:
            - not_taxable
            - taxable_requires_evidence
          type: string
        taxable:
          type: boolean
        taxBehavior:
          enum:
            - inclusive
            - exclusive
          type: string
        taxClassificationKey:
          enum:
            - standard_taxable
          type: string
        collectionIntent:
          enum:
            - manual
          type: string
        feePolicy:
          additionalProperties: true
          description:
            "Adopter fee policy (modeled, not settlement-executed). Structure\
            \ passed through to Vortex Payments."
          type: object
        deposit:
          additionalProperties: true
          type: object
        balance:
          additionalProperties: true
          type: object
        metadata:
          additionalProperties:
            type: string
      required:
        - balance
        - billingAccountId
        - collectionIntent
        - currency
        - customerExternalId
        - deposit
        - documentId
        - feePolicy
        - metadata
        - paymentFieldId
        - sourceId
        - sourceType
        - taxMode
    CustomerUpsertRequest:
      additionalProperties: false
      example:
        email: email
        name: name
        billingCurrency: USD
        customerProfileId: customerProfileId
        timezone: America/New_York
        metadata:
          key: metadata
      properties:
        email:
          format: email
          type: string
        name:
          type: string
        billingCurrency:
          example: USD
          type: string
        customerProfileId:
          description: Vortex Payments customer profile id. No provider ids.
          type: string
        timezone:
          example: America/New_York
          type: string
        metadata:
          additionalProperties:
            type: string
    ProductCreateRequest:
      additionalProperties: false
      example:
        productId: productId
        name: name
        description: description
        metadata:
          key: metadata
      properties:
        productId:
          type: string
        name:
          type: string
        description:
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - name
        - productId
    PriceCreateRequest:
      additionalProperties: false
      example:
        priceId: priceId
        productId: productId
        name: name
        priceType: fixed_recurring
        currency: USD
        billingInterval: day
        billingIntervalCount: 1
        unitAmount: 0
        meterId: meterId
        metadata:
          key: metadata
      properties:
        priceId:
          type: string
        productId:
          type: string
        name:
          type: string
        priceType:
          enum:
            - fixed_recurring
            - fixed_one_time
            - usage_metered
            - pay_what_you_want
            - free
            - credit_grant
            - credit_top_up
          type: string
        currency:
          example: USD
          type: string
        billingInterval:
          enum:
            - day
            - week
            - month
            - year
          type: string
        billingIntervalCount:
          minimum: 1
          type: integer
        unitAmount:
          minimum: 0
          type: integer
        meterId:
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - currency
        - name
        - priceId
        - priceType
        - productId
    SubscriptionCreateRequest:
      additionalProperties: {}
      example:
        customerExternalId: customerExternalId
        subscriptionExternalId: subscriptionExternalId
        planCode: planCode
        collectionMode: automatic
        items:
          - key: ""
          - key: ""
        trialEndsAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        customerExternalId:
          type: string
        subscriptionExternalId:
          type: string
        planCode:
          type: string
        collectionMode:
          enum:
            - automatic
            - manual
          type: string
        items:
          items:
            additionalProperties: true
            type: object
          type: array
          default: null
        trialEndsAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - customerExternalId
        - planCode
        - subscriptionExternalId
    SubscriptionItemChangeRequestItem:
      additionalProperties: false
      example:
        priceId: priceId
        quantity: 1
        metadata:
          key: metadata
      properties:
        priceId:
          type: string
        quantity:
          minimum: 1
          type: integer
        metadata:
          additionalProperties:
            type: string
      required:
        - priceId
    SubscriptionItemChangePreviewRequest:
      additionalProperties: false
      example:
        effectiveAt: 2000-01-23T04:56:07.000+00:00
        items:
          - priceId: priceId
            quantity: 1
            metadata:
              key: metadata
          - priceId: priceId
            quantity: 1
            metadata:
              key: metadata
        requestedByRef: requestedByRef
      properties:
        effectiveAt:
          format: date-time
          type: string
        items:
          items:
            $ref: "#/components/schemas/SubscriptionItemChangeRequestItem"
          minItems: 1
          type: array
          default: null
        requestedByRef:
          type: string
      required:
        - items
    SubscriptionChangeRequest:
      additionalProperties: false
      example:
        action: pause
        cancelAt: 2000-01-23T04:56:07.000+00:00
        effectiveAt: 2000-01-23T04:56:07.000+00:00
        items:
          - priceId: priceId
            quantity: 1
            metadata:
              key: metadata
          - priceId: priceId
            quantity: 1
            metadata:
              key: metadata
        requestedByRef: requestedByRef
      properties:
        action:
          enum:
            - pause
            - resume
            - cancel
            - update_items
          type: string
        cancelAt:
          format: date-time
          type: string
        effectiveAt:
          format: date-time
          type: string
        items:
          items:
            $ref: "#/components/schemas/SubscriptionItemChangeRequestItem"
          minItems: 1
          type: array
          default: null
        requestedByRef:
          type: string
      required:
        - action
    CustomerPaymentMethodSetupRequest:
      additionalProperties: false
      example:
        billingAccountId: billingAccountId
        methodType: card
        setAsDefault: true
      properties:
        billingAccountId:
          type: string
        methodType:
          enum:
            - card
          type: string
        setAsDefault:
          type: boolean
    CustomerPaymentMethodAttachRequest:
      additionalProperties: false
      example:
        billingAccountId: billingAccountId
        paymentMethodSetupSessionId: paymentMethodSetupSessionId
        setupToken: setupToken
        setAsDefault: true
        idempotencyKey: idempotencyKey
      properties:
        billingAccountId:
          type: string
        paymentMethodSetupSessionId:
          type: string
        setupToken:
          type: string
        setAsDefault:
          type: boolean
        idempotencyKey:
          type: string
      required:
        - paymentMethodSetupSessionId
        - setupToken
    CustomerDefaultPaymentMethodRequest:
      additionalProperties: false
      example:
        billingAccountId: billingAccountId
        defaultPaymentMethodId: defaultPaymentMethodId
      properties:
        billingAccountId:
          type: string
        defaultPaymentMethodId:
          type: string
      required:
        - defaultPaymentMethodId
    CustomerPaymentMethodArchiveRequest:
      additionalProperties: false
      example:
        billingAccountId: billingAccountId
        reason: reason
      properties:
        billingAccountId:
          type: string
        reason:
          type: string
    GrantEntitlementRequest:
      additionalProperties: false
      example:
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        entitlementKey: entitlementKey
        source: subscription
        sourceRef: sourceRef
        metadata:
          key: metadata
      properties:
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        entitlementKey:
          type: string
        source:
          enum:
            - subscription
            - operator
            - promotion
          type: string
        sourceRef:
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - customerExternalId
        - entitlementKey
    RevokeEntitlementRequest:
      additionalProperties: false
      example:
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        entitlementKey: entitlementKey
        source: subscription
        sourceRef: sourceRef
        reason: reason
      properties:
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        entitlementKey:
          type: string
        source:
          enum:
            - subscription
            - operator
            - promotion
          type: string
        sourceRef:
          type: string
        reason:
          type: string
      required:
        - customerExternalId
        - entitlementKey
    WebhookEndpointCreateRequest:
      additionalProperties: false
      example:
        url: https://openapi-generator.tech
        description: description
        eventTypes:
          - eventTypes
          - eventTypes
      properties:
        url:
          format: uri
          type: string
        description:
          type: string
        eventTypes:
          items:
            type: string
          minItems: 1
          type: array
          default: null
      required:
        - eventTypes
        - url
    WebhookEndpointUpdateRequest:
      additionalProperties: false
      example:
        url: https://openapi-generator.tech
        description: description
        eventTypes:
          - eventTypes
          - eventTypes
        status: active
      properties:
        url:
          format: uri
          type: string
        description:
          type: string
        eventTypes:
          items:
            type: string
          minItems: 1
          type: array
          default: null
        status:
          enum:
            - active
            - disabled
          type: string
    WebhookEndpointTestRequest:
      additionalProperties: false
      example:
        eventType: eventType
        dispatch: true
      properties:
        eventType:
          type: string
        dispatch:
          type: boolean
      required:
        - eventType
    WebhookEndpointSecretRotateRequest:
      additionalProperties: false
      example:
        gracePeriodSeconds: 207574
      properties:
        gracePeriodSeconds:
          maximum: 2592000
          minimum: 0
          type: integer
    WebhookEventResendRequest:
      additionalProperties: false
      example:
        endpointId: endpointId
      properties:
        endpointId:
          type: string
    WebhookDeliveryReplayRequest:
      additionalProperties: false
      example:
        replayedByRef: replayedByRef
        dispatch: true
      properties:
        replayedByRef:
          type: string
        dispatch:
          type: boolean
    WebhookDeliveriesDispatchRequest:
      additionalProperties: false
      example:
        deliveryId: deliveryId
        limit: 1
        endpointLimit: 1
      properties:
        deliveryId:
          type: string
        limit:
          minimum: 1
          type: integer
        endpointLimit:
          minimum: 1
          type: integer
    WebhookDeliveriesRedriveRequest:
      additionalProperties: false
      example:
        eventId: eventId
        endpointId: endpointId
        failureKind: endpoint_inactive
        limit: 1
        replayedByRef: replayedByRef
        dispatch: true
      properties:
        eventId:
          type: string
        endpointId:
          type: string
        failureKind:
          enum:
            - endpoint_inactive
            - network_error
            - rate_limited
            - server_error
            - client_error
            - unknown_error
          type: string
        limit:
          minimum: 1
          type: integer
        replayedByRef:
          type: string
        dispatch:
          type: boolean
    WebhookEmptyRequest:
      additionalProperties: false
      properties: {}
      type: object
    InvoiceCreateRequest:
      additionalProperties: false
      example:
        customerExternalId: customerExternalId
        subscriptionExternalId: subscriptionExternalId
        billingAccountId: billingAccountId
        metadata:
          key: metadata
      properties:
        customerExternalId:
          type: string
        subscriptionExternalId:
          type: string
        billingAccountId:
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - customerExternalId
    InvoiceFinalizeRequest:
      additionalProperties: false
      example:
        finalizedByRef: finalizedByRef
      properties:
        finalizedByRef:
          type: string
    TaxAddress:
      additionalProperties: false
      example:
        line1: line1
        line2: line2
        city: city
        region: region
        postalCode: postalCode
        country: country
      properties:
        line1:
          type: string
        line2:
          type: string
        city:
          type: string
        region:
          type: string
        postalCode:
          type: string
        country:
          type: string
      required:
        - city
        - country
        - line1
        - postalCode
        - region
    TaxBehavior:
      enum:
        - exclusive
        - inclusive
        - location_based
      type: string
    TaxBehaviorPolicyRequest:
      additionalProperties: false
      example:
        behavior: exclusive
        locationRef: locationRef
      properties:
        behavior:
          $ref: "#/components/schemas/TaxBehavior"
        locationRef:
          type: string
      required:
        - behavior
    TaxBehaviorPolicy:
      additionalProperties: false
      example:
        behavior: exclusive
        source: request
        amountIncludesTax: true
        locationRef: locationRef
      properties:
        behavior:
          $ref: "#/components/schemas/TaxBehavior"
        source:
          enum:
            - request
            - request_default
            - location_profile
          type: string
        amountIncludesTax:
          type: boolean
        locationRef:
          type: string
      required:
        - amountIncludesTax
        - behavior
        - source
    TaxQuoteRequest:
      additionalProperties: false
      example:
        customerTaxAddress:
          line1: line1
          line2: line2
          city: city
          region: region
          postalCode: postalCode
          country: country
        taxBehaviorPolicy:
          behavior: exclusive
          locationRef: locationRef
        requestedByRef: requestedByRef
      properties:
        customerTaxAddress:
          $ref: "#/components/schemas/TaxAddress"
        taxBehaviorPolicy:
          $ref: "#/components/schemas/TaxBehaviorPolicyRequest"
        requestedByRef:
          type: string
      required:
        - customerTaxAddress
    TaxFinalizeRequest:
      additionalProperties: false
      example:
        customerTaxAddress:
          line1: line1
          line2: line2
          city: city
          region: region
          postalCode: postalCode
          country: country
        taxBehaviorPolicy:
          behavior: exclusive
          locationRef: locationRef
        finalizedByRef: finalizedByRef
      properties:
        customerTaxAddress:
          $ref: "#/components/schemas/TaxAddress"
        taxBehaviorPolicy:
          $ref: "#/components/schemas/TaxBehaviorPolicyRequest"
        finalizedByRef:
          type: string
      required:
        - customerTaxAddress
    TaxLineEvidence:
      additionalProperties: false
      example:
        sourceLineId: sourceLineId
        classificationKey: classificationKey
        classificationLabel: classificationLabel
        classificationVersion: 5
        classificationMappingVersion: 5
        taxableAmount: 2
        taxAmount: 7
        taxBehavior: null
        amountIncludesTax: true
      properties:
        sourceLineId:
          type: string
        classificationKey:
          type: string
        classificationLabel:
          type: string
        classificationVersion:
          type: integer
        classificationMappingVersion:
          type: integer
        taxableAmount:
          type: integer
        taxAmount:
          type: integer
        taxBehavior:
          $ref: "#/components/schemas/TaxBehavior"
        amountIncludesTax:
          type: boolean
      required:
        - amountIncludesTax
        - classificationKey
        - classificationLabel
        - classificationMappingVersion
        - classificationVersion
        - sourceLineId
        - taxAmount
        - taxBehavior
        - taxableAmount
    TaxJurisdictionEvidence:
      additionalProperties: false
      example:
        jurisdictionType: jurisdictionType
        jurisdictionName: jurisdictionName
        taxableAmount: 9
        taxAmount: 3
        rate: 2.027123023002322
      properties:
        jurisdictionType:
          type: string
        jurisdictionName:
          type: string
        taxableAmount:
          type: integer
        taxAmount:
          type: integer
        rate:
          type: number
      required:
        - jurisdictionName
        - jurisdictionType
        - rate
        - taxAmount
        - taxableAmount
    TaxFailure:
      additionalProperties: false
      example:
        code: tax_company_mapping_missing
        message: message
        nextAction: configure_tax_company
      properties:
        code:
          enum:
            - tax_company_mapping_missing
            - tax_classification_missing
            - tax_classification_inactive
            - tax_classification_unmapped
            - tax_location_profile_missing
            - address_missing
            - unsupported_jurisdiction
            - tax_auth_failed
            - tax_timeout
            - tax_uncertain
            - tax_service_error
          type: string
        message:
          type: string
        nextAction:
          enum:
            - configure_tax_company
            - configure_tax_classification
            - configure_tax_location_profile
            - collect_customer_address
            - retry_quote
            - retry_finalize
            - retry_reverse
            - reconcile_tax_state
            - none
          type: string
      required:
        - code
        - message
        - nextAction
    InvoiceTax:
      additionalProperties: false
      example:
        invoiceNumber: invoiceNumber
        status: not_required
        currency: currency
        subtotal: 0
        taxTotal: 6
        total: 1
        evidenceVersion: evidenceVersion
        taxBehaviorPolicy:
          behavior: exclusive
          source: request
          amountIncludesTax: true
          locationRef: locationRef
        quotedAt: 2000-01-23T04:56:07.000+00:00
        finalizedAt: 2000-01-23T04:56:07.000+00:00
        lineEvidence:
          - sourceLineId: sourceLineId
            classificationKey: classificationKey
            classificationLabel: classificationLabel
            classificationVersion: 5
            classificationMappingVersion: 5
            taxableAmount: 2
            taxAmount: 7
            taxBehavior: null
            amountIncludesTax: true
          - sourceLineId: sourceLineId
            classificationKey: classificationKey
            classificationLabel: classificationLabel
            classificationVersion: 5
            classificationMappingVersion: 5
            taxableAmount: 2
            taxAmount: 7
            taxBehavior: null
            amountIncludesTax: true
        jurisdictionEvidence:
          - jurisdictionType: jurisdictionType
            jurisdictionName: jurisdictionName
            taxableAmount: 9
            taxAmount: 3
            rate: 2.027123023002322
          - jurisdictionType: jurisdictionType
            jurisdictionName: jurisdictionName
            taxableAmount: 9
            taxAmount: 3
            rate: 2.027123023002322
        failure:
          code: tax_company_mapping_missing
          message: message
          nextAction: configure_tax_company
      properties:
        invoiceNumber:
          type: string
        status:
          enum:
            - not_required
            - quote_pending
            - quoted
            - finalization_pending
            - finalized
            - failed
            - voided
            - reversed
          type: string
        currency:
          type: string
        subtotal:
          type: integer
        taxTotal:
          type: integer
        total:
          type: integer
        evidenceVersion:
          type: string
        taxBehaviorPolicy:
          $ref: "#/components/schemas/TaxBehaviorPolicy"
        quotedAt:
          format: date-time
          type: string
        finalizedAt:
          format: date-time
          type: string
        lineEvidence:
          items:
            $ref: "#/components/schemas/TaxLineEvidence"
          type: array
          default: null
        jurisdictionEvidence:
          items:
            $ref: "#/components/schemas/TaxJurisdictionEvidence"
          type: array
          default: null
        failure:
          $ref: "#/components/schemas/TaxFailure"
      required:
        - currency
        - evidenceVersion
        - invoiceNumber
        - jurisdictionEvidence
        - lineEvidence
        - status
        - subtotal
        - taxBehaviorPolicy
        - taxTotal
        - total
    OpenApiInvoiceTaxResponse:
      allOf:
        - $ref: "#/components/schemas/ApiWriteEnvelope"
        - properties:
            data:
              $ref: "#/components/schemas/OpenApiInvoiceTaxResponse_allOf_data"
          required:
            - data
      example:
        data:
          tax:
            invoiceNumber: invoiceNumber
            status: not_required
            currency: currency
            subtotal: 0
            taxTotal: 6
            total: 1
            evidenceVersion: evidenceVersion
            taxBehaviorPolicy:
              behavior: exclusive
              source: request
              amountIncludesTax: true
              locationRef: locationRef
            quotedAt: 2000-01-23T04:56:07.000+00:00
            finalizedAt: 2000-01-23T04:56:07.000+00:00
            lineEvidence:
              - sourceLineId: sourceLineId
                classificationKey: classificationKey
                classificationLabel: classificationLabel
                classificationVersion: 5
                classificationMappingVersion: 5
                taxableAmount: 2
                taxAmount: 7
                taxBehavior: null
                amountIncludesTax: true
              - sourceLineId: sourceLineId
                classificationKey: classificationKey
                classificationLabel: classificationLabel
                classificationVersion: 5
                classificationMappingVersion: 5
                taxableAmount: 2
                taxAmount: 7
                taxBehavior: null
                amountIncludesTax: true
            jurisdictionEvidence:
              - jurisdictionType: jurisdictionType
                jurisdictionName: jurisdictionName
                taxableAmount: 9
                taxAmount: 3
                rate: 2.027123023002322
              - jurisdictionType: jurisdictionType
                jurisdictionName: jurisdictionName
                taxableAmount: 9
                taxAmount: 3
                rate: 2.027123023002322
            failure:
              code: tax_company_mapping_missing
              message: message
              nextAction: configure_tax_company
          replayed: true
        requestId: requestId
    InvoiceRefreshRequest:
      additionalProperties: false
      properties: {}
      type: object
    InvoiceVoidRequest:
      additionalProperties: false
      example:
        voidedByRef: voidedByRef
        reason: reason
      properties:
        voidedByRef:
          type: string
        reason:
          type: string
    InvoiceMarkUncollectibleRequest:
      additionalProperties: false
      example:
        markedUncollectibleByRef: markedUncollectibleByRef
        by: by
        reason: reason
      properties:
        markedUncollectibleByRef:
          type: string
        by:
          type: string
        reason:
          type: string
    InvoiceCollectRequest:
      additionalProperties: false
      example:
        paymentMethodId: paymentMethodId
      properties:
        paymentMethodId:
          type: string
    PaymentRequestCreateRequest:
      additionalProperties: false
      example:
        paymentRequestId: paymentRequestId
        invoiceId: invoiceId
        invoiceNumbers:
          - invoiceNumbers
          - invoiceNumbers
        createdByRef: createdByRef
        dueAt: 2000-01-23T04:56:07.000+00:00
        expiresAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        paymentRequestId:
          type: string
        invoiceId:
          type: string
        invoiceNumbers:
          items:
            type: string
          minItems: 1
          type: array
          default: null
        createdByRef:
          type: string
        dueAt:
          format: date-time
          type: string
        expiresAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
    PaymentRequestLinkCreateRequest:
      additionalProperties: false
      example:
        expiresAt: 2000-01-23T04:56:07.000+00:00
        createdByRef: createdByRef
      properties:
        expiresAt:
          format: date-time
          type: string
        createdByRef:
          type: string
    PaymentRequestCollectRequest:
      additionalProperties: false
      example:
        paymentMethodId: paymentMethodId
      properties:
        paymentMethodId:
          type: string
    BillingQuoteLineInput:
      additionalProperties: false
      example:
        priceId: priceId
        quantity: 1
        unitAmount: 0
        description: description
      properties:
        priceId:
          type: string
        quantity:
          minimum: 1
          type: integer
        unitAmount:
          description:
            Negotiated minor-unit amount. Overrides the catalog price when
            set.
          minimum: 0
          type: integer
        description:
          type: string
      required:
        - priceId
    BillingQuoteCreateRequest:
      additionalProperties: false
      example:
        quoteId: quoteId
        orderType: one_off
        customerId: customerId
        billingAccountId: billingAccountId
        subscriptionId: subscriptionId
        currency: USD
        lines:
          - priceId: priceId
            quantity: 1
            unitAmount: 0
            description: description
          - priceId: priceId
            quantity: 1
            unitAmount: 0
            description: description
        couponIds:
          - couponIds
          - couponIds
        terms: terms
        metadata:
          key: metadata
      properties:
        quoteId:
          type: string
        orderType:
          enum:
            - one_off
            - subscription_creation
            - subscription_amendment
          type: string
        customerId:
          type: string
        billingAccountId:
          type: string
        subscriptionId:
          description:
            Required for subscription_amendment quotes; the subscription
            being amended.
          type: string
        currency:
          description:
            ISO 4217 alpha-3; only currencies processable on current rails
            are accepted
          example: USD
          type: string
        lines:
          items:
            $ref: "#/components/schemas/BillingQuoteLineInput"
          type: array
          default: null
        couponIds:
          items:
            type: string
          type: array
          default: null
        terms:
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - billingAccountId
        - currency
        - customerId
        - lines
        - orderType
    BillingQuoteVersionCreateRequest:
      additionalProperties: false
      example:
        lines:
          - priceId: priceId
            quantity: 1
            unitAmount: 0
            description: description
          - priceId: priceId
            quantity: 1
            unitAmount: 0
            description: description
        couponIds:
          - couponIds
          - couponIds
        terms: terms
        currency: USD
        metadata:
          key: metadata
      properties:
        lines:
          items:
            $ref: "#/components/schemas/BillingQuoteLineInput"
          minItems: 1
          type: array
          default: null
        couponIds:
          items:
            type: string
          type: array
          default: null
        terms:
          type: string
        currency:
          description:
            ISO 4217 alpha-3; only currencies processable on current rails
            are accepted
          example: USD
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - lines
    BillingQuoteVersionUpdateRequest:
      additionalProperties: false
      example:
        lines:
          - priceId: priceId
            quantity: 1
            unitAmount: 0
            description: description
          - priceId: priceId
            quantity: 1
            unitAmount: 0
            description: description
        couponIds:
          - couponIds
          - couponIds
        terms: terms
        currency: USD
        metadata:
          key: metadata
      properties:
        lines:
          items:
            $ref: "#/components/schemas/BillingQuoteLineInput"
          type: array
          default: null
        couponIds:
          items:
            type: string
          type: array
          default: null
        terms:
          type: string
        currency:
          description:
            ISO 4217 alpha-3; only currencies processable on current rails
            are accepted
          example: USD
          type: string
        metadata:
          additionalProperties:
            type: string
    BillingQuoteVersionApproveRequest:
      additionalProperties: false
      example:
        expiresAt: 2000-01-23T04:56:07.000+00:00
      properties:
        expiresAt:
          format: date-time
          type: string
    BillingQuoteVersionVoidRequest:
      additionalProperties: false
      example:
        reason: manual
      properties:
        reason:
          enum:
            - manual
            - superseded
          type: string
    BillingOrderFormSignatureRequestRequest:
      additionalProperties: false
      example:
        signatureRequestRef: signatureRequestRef
      properties:
        signatureRequestRef:
          description:
            Opaque reference to the external signature request (e.g. a
            Vortex Sign envelope/session id). The sign call must present the same
            ref to complete.
          type: string
      required:
        - signatureRequestRef
    BillingOrderFormSignRequest:
      additionalProperties: false
      example:
        signatureRequestRef: signatureRequestRef
        signedDocumentRef: signedDocumentRef
        executionMode: execute_in_vortex
        executeAt: 2000-01-23T04:56:07.000+00:00
      properties:
        signatureRequestRef:
          description:
            The external signature request ref previously registered on
            the order form. A mismatched ref is rejected; a matching ref on an already-signed
            form replays to the existing order.
          type: string
        signedDocumentRef:
          description:
            Opaque reference to the signed document (e.g. an external e-signature
            envelope id).
          type: string
        executionMode:
          enum:
            - execute_in_vortex
            - order_only
          type: string
        executeAt:
          format: date-time
          type: string
    BillingOrderFormVoidRequest:
      additionalProperties: false
      example:
        reason: reason
      properties:
        reason:
          type: string
      required:
        - reason
    BillingOrderFormsExpireDueRequest:
      additionalProperties: false
      example:
        now: 2000-01-23T04:56:07.000+00:00
        limit: 1
      properties:
        now:
          format: date-time
          type: string
        limit:
          minimum: 1
          type: integer
    BillingOrderUpdateRequest:
      additionalProperties: false
      example:
        executionMode: execute_in_vortex
        executeAt: 2000-01-23T04:56:07.000+00:00
      properties:
        executionMode:
          enum:
            - execute_in_vortex
            - order_only
          type: string
        executeAt:
          format: date-time
          type: string
    BillingOrderExecuteRequest:
      additionalProperties: false
      properties: {}
      type: object
    BillingOrdersExecuteDueRequest:
      additionalProperties: false
      example:
        now: 2000-01-23T04:56:07.000+00:00
        limit: 1
      properties:
        now:
          format: date-time
          type: string
        limit:
          minimum: 1
          type: integer
    CheckoutSessionLineItemRequest:
      additionalProperties: false
      example:
        priceId: priceId
        quantity: 1
        metadata:
          key: metadata
      properties:
        priceId:
          type: string
        quantity:
          minimum: 1
          type: integer
        metadata:
          additionalProperties:
            type: string
      required:
        - priceId
    CreateCheckoutSessionRequest:
      additionalProperties: false
      example:
        mode: payment
        invoiceNumber: invoiceNumber
        invoiceNumbers:
          - invoiceNumbers
          - invoiceNumbers
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        subscriptionExternalId: subscriptionExternalId
        collectionMode: manual
        startAt: 2000-01-23T04:56:07.000+00:00
        lineItems:
          - priceId: priceId
            quantity: 1
            metadata:
              key: metadata
          - priceId: priceId
            quantity: 1
            metadata:
              key: metadata
        paymentRequestId: paymentRequestId
        expiresAt: 2000-01-23T04:56:07.000+00:00
        createdByRef: createdByRef
        metadata:
          key: metadata
      properties:
        mode:
          enum:
            - payment
            - subscription
          type: string
        invoiceNumber:
          type: string
        invoiceNumbers:
          items:
            type: string
          minItems: 1
          type: array
          default: null
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        subscriptionExternalId:
          type: string
        collectionMode:
          enum:
            - manual
            - automatic
          type: string
        startAt:
          format: date-time
          type: string
        lineItems:
          items:
            $ref: "#/components/schemas/CheckoutSessionLineItemRequest"
          minItems: 1
          type: array
          default: null
        paymentRequestId:
          type: string
        expiresAt:
          format: date-time
          type: string
        createdByRef:
          type: string
        metadata:
          additionalProperties:
            type: string
    CouponTargets:
      additionalProperties: false
      example:
        priceIds:
          - priceIds
          - priceIds
        meterIds:
          - meterIds
          - meterIds
      properties:
        priceIds:
          items:
            type: string
          minItems: 1
          type: array
          default: null
        meterIds:
          items:
            type: string
          minItems: 1
          type: array
          default: null
    CreateCouponRequest:
      additionalProperties: false
      example:
        couponId: couponId
        code: code
        name: name
        description: description
        discountType: fixed_amount
        amount: 1
        currency: USD
        percentage: 60.27456183070403
        duration: once
        durationInBillingPeriods: 1
        reusable: true
        targets:
          priceIds:
            - priceIds
            - priceIds
          meterIds:
            - meterIds
            - meterIds
        expiresAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        couponId:
          type: string
        code:
          type: string
        name:
          type: string
        description:
          type: string
        discountType:
          enum:
            - fixed_amount
            - percentage
          type: string
        amount:
          minimum: 1
          type: integer
        currency:
          description:
            ISO 4217 alpha-3; only currencies processable on current rails
            are accepted
          example: USD
          type: string
        percentage:
          maximum: 100
          minimum: 0
          type: number
        duration:
          enum:
            - once
            - repeating
            - forever
          type: string
        durationInBillingPeriods:
          minimum: 1
          type: integer
        reusable:
          type: boolean
        targets:
          $ref: "#/components/schemas/CouponTargets"
        expiresAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - code
        - discountType
        - duration
        - name
    ApplyCouponRequest:
      additionalProperties: false
      example:
        appliedCouponId: appliedCouponId
        customerExternalId: customerExternalId
        billingAccountId: billingAccountId
        subscriptionExternalId: subscriptionExternalId
        appliedAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        appliedCouponId:
          type: string
        customerExternalId:
          type: string
        billingAccountId:
          type: string
        subscriptionExternalId:
          type: string
        appliedAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - customerExternalId
    TerminateCouponRequest:
      additionalProperties: false
      example:
        terminatedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        terminatedAt:
          format: date-time
          type: string
    TerminateAppliedCouponRequest:
      additionalProperties: false
      example:
        terminatedAt: 2000-01-23T04:56:07.000+00:00
      properties:
        terminatedAt:
          format: date-time
          type: string
    CustomerBalanceTargets:
      additionalProperties: false
      example:
        lineTypes:
          - fixed
          - fixed
        priceIds:
          - priceIds
          - priceIds
        meterIds:
          - meterIds
          - meterIds
      properties:
        lineTypes:
          items:
            enum:
              - fixed
              - usage
              - credit
              - adjustment
              - tax
            type: string
          minItems: 1
          type: array
          default: null
        priceIds:
          items:
            type: string
          minItems: 1
          type: array
          default: null
        meterIds:
          items:
            type: string
          minItems: 1
          type: array
          default: null
    GrantCustomerBalanceRequest:
      additionalProperties: false
      example:
        entryId: entryId
        currency: currency
        amount: 1
        targets:
          lineTypes:
            - fixed
            - fixed
          priceIds:
            - priceIds
            - priceIds
          meterIds:
            - meterIds
            - meterIds
        description: description
        effectiveAt: 2000-01-23T04:56:07.000+00:00
        expiresAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        entryId:
          type: string
        currency:
          type: string
        amount:
          minimum: 1
          type: integer
        targets:
          $ref: "#/components/schemas/CustomerBalanceTargets"
        description:
          type: string
        effectiveAt:
          format: date-time
          type: string
        expiresAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - amount
        - currency
    GrantCustomerBalanceFromPaymentRequest:
      additionalProperties: false
      example:
        entryId: entryId
        paymentId: paymentId
        targets:
          lineTypes:
            - fixed
            - fixed
          priceIds:
            - priceIds
            - priceIds
          meterIds:
            - meterIds
            - meterIds
        description: description
        effectiveAt: 2000-01-23T04:56:07.000+00:00
        expiresAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        entryId:
          type: string
        paymentId:
          type: string
        targets:
          $ref: "#/components/schemas/CustomerBalanceTargets"
        description:
          type: string
        effectiveAt:
          format: date-time
          type: string
        expiresAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - paymentId
    ExpireCustomerBalanceRequest:
      additionalProperties: false
      example:
        entryId: entryId
        sourceEntryId: sourceEntryId
        amount: 1
        description: description
        effectiveAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        entryId:
          type: string
        sourceEntryId:
          type: string
        amount:
          minimum: 1
          type: integer
        description:
          type: string
        effectiveAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - sourceEntryId
    ReverseCustomerBalanceRequest:
      additionalProperties: false
      example:
        entryId: entryId
        sourceEntryId: sourceEntryId
        amount: 1
        description: description
        effectiveAt: 2000-01-23T04:56:07.000+00:00
        metadata:
          key: metadata
      properties:
        entryId:
          type: string
        sourceEntryId:
          type: string
        amount:
          minimum: 1
          type: integer
        description:
          type: string
        effectiveAt:
          format: date-time
          type: string
        metadata:
          additionalProperties:
            type: string
      required:
        - sourceEntryId
    CreditNoteCreateRequest:
      additionalProperties: false
      example:
        creditNoteId: creditNoteId
        invoiceId: invoiceId
        reason: duplicated_charge
        description: description
        creditAmount: 0
        refundAmount: 0
        offsetAmount: 0
        metadata:
          key: metadata
        lines:
          - invoiceLineId: invoiceLineId
            bucket: credit
            description: description
            amount: 1
            metadata:
              key: metadata
          - invoiceLineId: invoiceLineId
            bucket: credit
            description: description
            amount: 1
            metadata:
              key: metadata
      properties:
        creditNoteId:
          type: string
        invoiceId:
          type: string
        reason:
          $ref: "#/components/schemas/CreditNoteReason"
        description:
          type: string
        creditAmount:
          minimum: 0
          type: integer
        refundAmount:
          minimum: 0
          type: integer
        offsetAmount:
          minimum: 0
          type: integer
        metadata:
          additionalProperties:
            type: string
        lines:
          items:
            $ref: "#/components/schemas/CreditNoteCreateLine"
          minItems: 1
          type: array
          default: null
      required:
        - invoiceId
        - lines
        - reason
    CreditNoteCreateLine:
      additionalProperties: false
      example:
        invoiceLineId: invoiceLineId
        bucket: credit
        description: description
        amount: 1
        metadata:
          key: metadata
      properties:
        invoiceLineId:
          type: string
        bucket:
          enum:
            - credit
            - refund
            - offset
          type: string
        description:
          type: string
        amount:
          minimum: 1
          type: integer
        metadata:
          additionalProperties:
            type: string
      required:
        - amount
        - description
    CreditNoteRefundRequest:
      additionalProperties: false
      example:
        paymentId: paymentId
        idempotencyKey: idempotencyKey
        customerTaxAddress:
          line1: line1
          line2: line2
          city: city
          region: region
          postalCode: postalCode
          country: country
      properties:
        paymentId:
          type: string
        idempotencyKey:
          type: string
        customerTaxAddress:
          $ref: "#/components/schemas/TaxAddress"
      required:
        - paymentId
    CreditNoteRefundReconcileRequest:
      additionalProperties: false
      example:
        paymentsRefundId: paymentsRefundId
      properties:
        paymentsRefundId:
          type: string
    CreditNoteReason:
      enum:
        - duplicated_charge
        - product_unsatisfactory
        - order_change
        - order_cancellation
        - fraudulent_charge
        - other
      type: string
    CustomerResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    PayableResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    ProductResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    PriceResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    SubscriptionResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    SubscriptionItemChangePreviewResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    SubscriptionChangeResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerPaymentMethodsResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerPaymentMethodSetupResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerPaymentMethodResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerPaymentStateResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerStateResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerAccessResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerEntitlementsResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    EntitlementResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantAccountStateResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantAccountCapabilitiesResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantAccountResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantAccountSettlementsResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantAccountPayoutsResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantAccountPayoutProfileResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantTaxProfileRequest:
      additionalProperties: {}
      example:
        environment: sandbox
        displayName: displayName
        originAddress:
          line1: line1
          line2: line2
          city: city
          region: region
          postalCode: postalCode
          country: country
        registrations:
          - country: country
            region: region
          - country: country
            region: region
      properties:
        environment:
          enum:
            - sandbox
            - production
          type: string
        displayName:
          type: string
        originAddress:
          $ref: "#/components/schemas/TaxAddress"
        registrations:
          items:
            $ref: "#/components/schemas/MerchantTaxProfileRequest_registrations_inner"
          minItems: 1
          type: array
          default: null
      required:
        - environment
        - originAddress
        - registrations
    MerchantTaxProfileResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    MerchantOnboardingLinkResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookEventTypesResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookEndpointResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookEndpointListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookEndpointTestResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookEventResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookEventListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookDeliveryResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookDeliveryListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookDeliveryDispatchResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    WebhookDeliveryRedriveResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    InvoicePreviewResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    InvoiceResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    PaymentLifecycleResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    ReceiptListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    ReceiptResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    PaymentRequestListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    PaymentRequestResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    PaymentRequestLinkResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CreateCheckoutSessionResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    OpenApiCreateCheckoutSessionResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CouponResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CouponApplicationResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CouponListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    AppliedCouponResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    AppliedCouponListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerBalanceResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CustomerBalanceEntryResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CreditNoteResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    CreditNoteRefundLineageResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingQuoteResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingQuoteDetailResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingQuoteListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingQuoteVersionResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingQuoteVersionListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingQuoteVersionApproveResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingOrderFormResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingOrderFormListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingOrderFormSignResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingOrderResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    BillingOrderListResponse:
      $ref: "#/components/schemas/ApiWriteEnvelope"
    ApiWriteEnvelope:
      additionalProperties: {}
      example:
        data:
          key: ""
        requestId: requestId
      properties:
        data:
          additionalProperties: true
          type: object
        requestId:
          type: string
      required:
        - data
        - requestId
    ApiSuccess:
      additionalProperties: true
      description:
        Route-specific success envelope. Use TypeScript contract exports
        for exact promoted schemas until this OpenAPI is fully enriched.
      type: object
    ApiError:
      additionalProperties: {}
      example:
        error:
          code: bad_request
          message: message
          requestId: requestId
          docsUrl: docsUrl
          details:
            key: ""
      properties:
        error:
          $ref: "#/components/schemas/ApiError_error"
      required:
        - error
    MerchantUnderwritingData_cardVolumeDistribution:
      additionalProperties: false
      example:
        cardPresentPercentage: 7.386281948385884
        ecommercePercentage: 1.2315135367772556
        mailOrderTelephoneOrderPercentage: 1.0246457001441578
      properties:
        cardPresentPercentage:
          type: number
        ecommercePercentage:
          type: number
        mailOrderTelephoneOrderPercentage:
          type: number
    MerchantUnderwritingData_volumeDistributionByBusinessType:
      additionalProperties: false
      example:
        businessToConsumerVolumePercentage: 1.4894159098541704
        consumerToConsumerVolumePercentage: 6.84685269835264
        otherVolumePercentage: 7.457744773683766
      properties:
        businessToConsumerVolumePercentage:
          type: number
        consumerToConsumerVolumePercentage:
          type: number
        otherVolumePercentage:
          type: number
    CreateRecurringPayableRequest_cadence:
      additionalProperties: false
      example:
        interval: week
        intervalCount: 1
      properties:
        interval:
          enum:
            - week
            - month
            - year
          type: string
        intervalCount:
          minimum: 1
          type: integer
      required:
        - interval
        - intervalCount
    OpenApiInvoiceTaxResponse_allOf_data:
      example:
        tax:
          invoiceNumber: invoiceNumber
          status: not_required
          currency: currency
          subtotal: 0
          taxTotal: 6
          total: 1
          evidenceVersion: evidenceVersion
          taxBehaviorPolicy:
            behavior: exclusive
            source: request
            amountIncludesTax: true
            locationRef: locationRef
          quotedAt: 2000-01-23T04:56:07.000+00:00
          finalizedAt: 2000-01-23T04:56:07.000+00:00
          lineEvidence:
            - sourceLineId: sourceLineId
              classificationKey: classificationKey
              classificationLabel: classificationLabel
              classificationVersion: 5
              classificationMappingVersion: 5
              taxableAmount: 2
              taxAmount: 7
              taxBehavior: null
              amountIncludesTax: true
            - sourceLineId: sourceLineId
              classificationKey: classificationKey
              classificationLabel: classificationLabel
              classificationVersion: 5
              classificationMappingVersion: 5
              taxableAmount: 2
              taxAmount: 7
              taxBehavior: null
              amountIncludesTax: true
          jurisdictionEvidence:
            - jurisdictionType: jurisdictionType
              jurisdictionName: jurisdictionName
              taxableAmount: 9
              taxAmount: 3
              rate: 2.027123023002322
            - jurisdictionType: jurisdictionType
              jurisdictionName: jurisdictionName
              taxableAmount: 9
              taxAmount: 3
              rate: 2.027123023002322
          failure:
            code: tax_company_mapping_missing
            message: message
            nextAction: configure_tax_company
        replayed: true
      properties:
        tax:
          $ref: "#/components/schemas/InvoiceTax"
        replayed:
          type: boolean
      required:
        - replayed
        - tax
    MerchantTaxProfileRequest_registrations_inner:
      additionalProperties: false
      example:
        country: country
        region: region
      properties:
        country:
          type: string
        region:
          type: string
      required:
        - country
    ApiError_error:
      additionalProperties: true
      example:
        code: bad_request
        message: message
        requestId: requestId
        docsUrl: docsUrl
        details:
          key: ""
      properties:
        code:
          enum:
            - bad_request
            - unauthorized
            - forbidden
            - not_found
            - conflict
            - rate_limited
            - validation_failure
            - not_implemented
            - internal_error
          type: string
        message:
          type: string
        requestId:
          type: string
        docsUrl:
          description: Deep link to the errors guide section for this code.
          type: string
        details:
          additionalProperties: true
          type: object
      required:
        - code
        - message
  securitySchemes:
    api-key:
      description: Public API key bearer token.
      scheme: bearer
      type: http
    checkout-token:
      description: Hosted checkout/payment request token.
      in: query
      name: token
      type: apiKey
    session-cookie:
      description:
        "Better-Auth session cookie. Used by signup, which must NOT accept\
        \ an API key: a credential that can mint credentials self-replicates."
      in: cookie
      name: better-auth.session_token
      type: apiKey
    provider-signature:
      description: Provider-specific webhook signature header.
      in: header
      name: X-Provider-Signature
      type: apiKey
